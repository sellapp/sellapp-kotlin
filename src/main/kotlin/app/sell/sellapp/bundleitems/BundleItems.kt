// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.bundleitems

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.AttachBundleItemsRequestApplicationJsonPropertyResources
import app.sell.sellapp.models.ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.SdkAttachBundleItemsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkGetBundleItemResponseValue200ApplicationJson
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for BundleItems.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class BundleItems(internal val sellapp: SellApp) {
    /**
     * Retrieve a bundle item
     *
     * Retrieve a specific item from a bundle by its product variant ID. OAuth callers use the admin
     * grant and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param bundle The bundle path parameter.
     * @param item The item path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetBundleItemResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        bundle: String,
        item: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetBundleItemResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/bundles/${encodePathSegment(bundle)}/items/${encodePathSegment(item)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetBundleItemResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getBundleItem",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetBundleItemResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        bundle: String,
        item: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetBundleItemResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(bundle, item, capturingOptions)
        return captured as? RawResponse<SdkGetBundleItemResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        bundle: String,
        item: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetBundleItemResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(bundle, item, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        bundle: String,
        item: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetBundleItemResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(bundle, item, requestOptions)
        }

    /**
     * List bundle items
     *
     * Retrieve a paginated list of product variants attached to a bundle. OAuth callers use the
     * admin grant and select a store with X-STORE. Current membership and role permissions apply to
     * every request.
     *
     * @param bundle The bundle path parameter.
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        bundle: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/bundles/${encodePathSegment(bundle)}/items",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listBundleItems",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models
                    .ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        bundle: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(bundle, limit, page, capturingOptions)
        return captured
            as? RawResponse<Page<ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        bundle: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            list(bundle, limit, page, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        bundle: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(bundle, limit, page, requestOptions)
        }

    /**
     * Attach bundle items
     *
     * Attach one or more product variants to a bundle with quantities. OAuth callers use the admin
     * grant and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param bundle The bundle path parameter.
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkAttachBundleItemsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun attach(
        bundle: String,
        resources: AttachBundleItemsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): SdkAttachBundleItemsResponseValue200ApplicationJson {
        val body = bodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/bundles/${encodePathSegment(bundle)}/items/attach",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkAttachBundleItemsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkAttachBundleItemsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "attachBundleItems",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkAttachBundleItemsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun attachWithResponse(
        bundle: String,
        resources: AttachBundleItemsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkAttachBundleItemsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        attach(bundle, resources, capturingOptions)
        return captured as? RawResponse<SdkAttachBundleItemsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [attach]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [attach] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("attachSuspend")
    suspend fun attachSuspend(
        bundle: String,
        resources: AttachBundleItemsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): SdkAttachBundleItemsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            attach(bundle, resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [attachWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [attachWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("attachWithResponseSuspend")
    suspend fun attachWithResponseSuspend(
        bundle: String,
        resources: AttachBundleItemsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkAttachBundleItemsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            attachWithResponse(bundle, resources, requestOptions)
        }

    /**
     * Detach bundle items
     *
     * Detach one or more product variants from a bundle. OAuth callers use the admin grant and
     * select a store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param bundle The bundle path parameter.
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun detach(
        bundle: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ) {
        val body = bodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/bundles/${encodePathSegment(bundle)}/items/detach",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkDetachBundleItemsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkDetachBundleItemsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "detachBundleItems",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun detachWithResponse(
        bundle: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        detach(bundle, resources, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [detach]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [detach] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("detachSuspend")
    suspend fun detachSuspend(
        bundle: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            detach(bundle, resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [detachWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [detachWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("detachWithResponseSuspend")
    suspend fun detachWithResponseSuspend(
        bundle: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            detachWithResponse(bundle, resources, requestOptions)
        }
}
