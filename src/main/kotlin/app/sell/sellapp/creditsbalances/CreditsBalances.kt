// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.creditsbalances

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkGetCreditBalanceResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkListCreditBalancesResponseValue200ApplicationJson
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for CreditsBalances.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class CreditsBalances(internal val sellapp: SellApp) {
    /**
     * List credit balances
     *
     * List store-scoped customer balances for dedicated credits products. OAuth callers use the
     * admin grant and select a store with X-STORE. Current membership and role permissions apply to
     * every request.
     *
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param pagination Set to false to return at most 100 matching items in one data array without
     *   pagination links or metadata.
     * @param customerId Filter by a store customer ID.
     * @param productId Filter by a credits product ID.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        customerId: Long? = null,
        productId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkListCreditBalancesResponseValue200ApplicationJson> {
        val itemType =
            object : TypeReference<SdkListCreditBalancesResponseValue200ApplicationJson>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/credit-balances",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listCreditBalances",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.SdkListCreditBalancesResponseValue200ApplicationJson
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
            pagination?.let { add("pagination" to it.toString()) }
            customerId?.let { add("customer_id" to it.toString()) }
            productId?.let { add("product_id" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        customerId: Long? = null,
        productId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkListCreditBalancesResponseValue200ApplicationJson>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(limit, page, pagination, customerId, productId, capturingOptions)
        return captured as? RawResponse<Page<SdkListCreditBalancesResponseValue200ApplicationJson>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        customerId: Long? = null,
        productId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkListCreditBalancesResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            list(limit, page, pagination, customerId, productId, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        customerId: Long? = null,
        productId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkListCreditBalancesResponseValue200ApplicationJson>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(limit, page, pagination, customerId, productId, requestOptions)
        }

    /**
     * Retrieve a credit balance
     *
     * Retrieve one customer and credits-product balance with up to 100 recent ledger entries. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param customer The customer path parameter.
     * @param creditProduct The creditProduct path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetCreditBalanceResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        customer: String,
        creditProduct: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetCreditBalanceResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path =
                    "/v2/credit-balances/${encodePathSegment(customer)}/${encodePathSegment(creditProduct)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetCreditBalanceResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getCreditBalance",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetCreditBalanceResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        customer: String,
        creditProduct: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetCreditBalanceResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(customer, creditProduct, capturingOptions)
        return captured as? RawResponse<SdkGetCreditBalanceResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        customer: String,
        creditProduct: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetCreditBalanceResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(customer, creditProduct, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        customer: String,
        creditProduct: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetCreditBalanceResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(customer, creditProduct, requestOptions)
        }
}
