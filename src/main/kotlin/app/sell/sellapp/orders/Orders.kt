// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.orders

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.PatchField
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.http.patchBodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.CreateOrderReplacementRequestApplicationJsonPropertyProductVariants
import app.sell.sellapp.models.CreateOrderRequestApplicationJsonPropertyExtra
import app.sell.sellapp.models.CreateOrderRequestApplicationJsonPropertyProductVariants
import app.sell.sellapp.models.CreateWalletOrderRequestApplicationJsonPropertyExtra
import app.sell.sellapp.models.CreateWalletOrderRequestApplicationJsonPropertyProductVariants
import app.sell.sellapp.models.ListOrdersResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.SdkCreateOrderCheckoutResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkCreateOrderRefundResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkCreateOrderReplacementResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkCreateOrderResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkCreateWalletOrderResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkGetOrderResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkListOrderDeliverablesResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkPayOrderFromWalletResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson
import app.sell.sellapp.models.SdkRetryOrderFulfillmentResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkUpdateOrderStatusResponseValue200ApplicationJson
import app.sell.sellapp.models.SearchOrdersRequestApplicationJsonPropertyFiltersItem
import app.sell.sellapp.models.SearchOrdersRequestApplicationJsonPropertyPagination
import app.sell.sellapp.models.SearchOrdersRequestApplicationJsonPropertySortItem
import app.sell.sellapp.models.SearchOrdersResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.types.ExpectedStatus
import app.sell.sellapp.types.PaymentMethod
import app.sell.sellapp.types.SdkUpdateOrderStatusRequestApplicationJsonStatus
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for Orders.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class Orders(internal val sellapp: SellApp) {
    /**
     * List orders
     *
     * List orders for the selected store. Requires the invoice token ability. Results include
     * customer and payment-provider summaries without sensitive data, totals in integer minor
     * units, status timelines, and product-level line-item summaries from the same store. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListOrdersResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object : TypeReference<ListOrdersResponseValue200ApplicationJsonPropertyDataItem>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/orders",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listOrders",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.ListOrdersResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListOrdersResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(limit, page, capturingOptions)
        return captured
            as? RawResponse<Page<ListOrdersResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListOrdersResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            list(limit, page, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListOrdersResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(limit, page, requestOptions)
        }

    /**
     * Create an order
     *
     * Create an order with one or more product-variant line items and start checkout. This can
     * create provider payment sessions, reserve stock or wallet funds, and begin fulfillment for a
     * free or fully wallet-funded order. Requires the invoice ability or orders:write OAuth scope
     * and current store permission. Use the same Idempotency-Key and identical body after a lost
     * response; inspect the order before creating a new key. The payment.checkout_url is a handoff,
     * not proof of payment. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param customerEmail the customer email of the request.
     * @param paymentMethod the payment method of the request.
     * @param productVariants the product variants of the request.
     * @param customerIp the customer ip of the request.
     * @param coupon the coupon of the request.
     * @param vatId the vat id of the request.
     * @param country the country of the request.
     * @param affiliate the affiliate of the request.
     * @param extra the extra of the request.
     * @param customPaymentMethodId Required when payment_method is CUSTOM_PAYMENT_METHOD. ULID of
     *   an enabled custom payment method belonging to this store.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateOrderResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun create(
        customerEmail: String,
        paymentMethod: PaymentMethod,
        productVariants: CreateOrderRequestApplicationJsonPropertyProductVariants,
        customerIp: String? = null,
        coupon: String? = null,
        vatId: String? = null,
        country: String? = null,
        affiliate: String? = null,
        extra: CreateOrderRequestApplicationJsonPropertyExtra? = null,
        customPaymentMethodId: String? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateOrderResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "customer_email" to customerEmail,
                "payment_method" to paymentMethod,
                "product_variants" to productVariants,
                "customer_ip" to customerIp,
                "coupon" to coupon,
                "vat_id" to vatId,
                "country" to country,
                "affiliate" to affiliate,
                "extra" to extra,
                "custom_payment_method_id" to customPaymentMethodId,
            )
        body["customer_email"] = customerEmail
        body["payment_method"] = paymentMethod
        body["product_variants"] = productVariants
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/orders",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateOrderRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateOrderResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "createOrder",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateOrderResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createWithResponse(
        customerEmail: String,
        paymentMethod: PaymentMethod,
        productVariants: CreateOrderRequestApplicationJsonPropertyProductVariants,
        customerIp: String? = null,
        coupon: String? = null,
        vatId: String? = null,
        country: String? = null,
        affiliate: String? = null,
        extra: CreateOrderRequestApplicationJsonPropertyExtra? = null,
        customPaymentMethodId: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateOrderResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        create(
            customerEmail,
            paymentMethod,
            productVariants,
            customerIp,
            coupon,
            vatId,
            country,
            affiliate,
            extra,
            customPaymentMethodId,
            capturingOptions,
        )
        return captured as? RawResponse<SdkCreateOrderResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [create]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [create] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createSuspend")
    suspend fun createSuspend(
        customerEmail: String,
        paymentMethod: PaymentMethod,
        productVariants: CreateOrderRequestApplicationJsonPropertyProductVariants,
        customerIp: String? = null,
        coupon: String? = null,
        vatId: String? = null,
        country: String? = null,
        affiliate: String? = null,
        extra: CreateOrderRequestApplicationJsonPropertyExtra? = null,
        customPaymentMethodId: String? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateOrderResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            create(
                customerEmail,
                paymentMethod,
                productVariants,
                customerIp,
                coupon,
                vatId,
                country,
                affiliate,
                extra,
                customPaymentMethodId,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [createWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createWithResponseSuspend")
    suspend fun createWithResponseSuspend(
        customerEmail: String,
        paymentMethod: PaymentMethod,
        productVariants: CreateOrderRequestApplicationJsonPropertyProductVariants,
        customerIp: String? = null,
        coupon: String? = null,
        vatId: String? = null,
        country: String? = null,
        affiliate: String? = null,
        extra: CreateOrderRequestApplicationJsonPropertyExtra? = null,
        customPaymentMethodId: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateOrderResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createWithResponse(
                customerEmail,
                paymentMethod,
                productVariants,
                customerIp,
                coupon,
                vatId,
                country,
                affiliate,
                extra,
                customPaymentMethodId,
                requestOptions,
            )
        }

    /**
     * Search orders
     *
     * Find orders using the same filters as the list endpoint. Requires the invoice token ability.
     * OAuth callers use the admin grant and select a store with X-STORE. Current membership and
     * role permissions apply to every request.
     *
     * @param filters the filters of the request.
     * @param sort the sort of the request.
     * @param pagination the pagination of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun search(
        filters: List<SearchOrdersRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchOrdersRequestApplicationJsonPropertySortItem>? = null,
        pagination: SearchOrdersRequestApplicationJsonPropertyPagination? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SearchOrdersResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object : TypeReference<SearchOrdersResponseValue200ApplicationJsonPropertyDataItem>() {}
        return sellapp.baseClient.requestPage(
            method = "POST",
            path = "/v2/orders/search",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "searchOrders",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.SearchOrdersResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {}
    }

    @Suppress("UNCHECKED_CAST")
    fun searchWithResponse(
        filters: List<SearchOrdersRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchOrdersRequestApplicationJsonPropertySortItem>? = null,
        pagination: SearchOrdersRequestApplicationJsonPropertyPagination? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SearchOrdersResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        search(filters, sort, pagination, capturingOptions)
        return captured
            as? RawResponse<Page<SearchOrdersResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [search]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [search] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchSuspend")
    suspend fun searchSuspend(
        filters: List<SearchOrdersRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchOrdersRequestApplicationJsonPropertySortItem>? = null,
        pagination: SearchOrdersRequestApplicationJsonPropertyPagination? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SearchOrdersResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            search(filters, sort, pagination, requestOptions)
        }

    /**
     * Coroutine-aware variant of [searchWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [searchWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchWithResponseSuspend")
    suspend fun searchWithResponseSuspend(
        filters: List<SearchOrdersRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchOrdersRequestApplicationJsonPropertySortItem>? = null,
        pagination: SearchOrdersRequestApplicationJsonPropertyPagination? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SearchOrdersResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            searchWithResponse(filters, sort, pagination, requestOptions)
        }

    /**
     * Retrieve an order
     *
     * Retrieve an order and its product-level line-item summaries from the same store. Requires the
     * invoice token ability. An ID from another store returns not found. OAuth callers use the
     * admin grant and select a store with X-STORE. Current membership and role permissions apply to
     * every request.
     *
     * @param order The order path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetOrderResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        order: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetOrderResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/orders/${encodePathSegment(order)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetOrderResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getOrder",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetOrderResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        order: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetOrderResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(order, capturingOptions)
        return captured as? RawResponse<SdkGetOrderResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        order: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetOrderResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(order, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        order: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetOrderResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(order, requestOptions)
        }

    /**
     * Update order status
     *
     * Change order lifecycle state. COMPLETED finalizes fulfillment of an already
     * paid/partial/disputing order; it does not capture a pending payment. VOIDED can schedule
     * provider side effects. REVIEW and DISPUTING have state restrictions. Send expected_status to
     * prevent overwriting a changed state; an invalid transition, stale status, or busy lifecycle
     * lock returns 422. Requires the invoice ability or orders:write OAuth scope and current store
     * permission. Idempotency-Key is required. Reuse the same key and identical request after a
     * lost response; changed input or an in-progress request returns 409. OAuth callers use the
     * admin grant and select a store with X-STORE. Current membership and role permissions apply to
     * every request.
     *
     * @param order The order identifier.
     * @param status the status of the request.
     * @param expectedStatus Expected current status. A status update checks this under the order
     *   lock and returns 422 if the order has changed.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkUpdateOrderStatusResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun updateStatus(
        order: String,
        status: SdkUpdateOrderStatusRequestApplicationJsonStatus,
        expectedStatus: PatchField<ExpectedStatus?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateOrderStatusResponseValue200ApplicationJson {
        val body =
            patchBodyOf(
                "status" to status,
                "expected_status" to expectedStatus,
            )
        body["status"] = status
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v2/orders/${encodePathSegment(order)}/status",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkUpdateOrderStatusRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkUpdateOrderStatusResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "updateOrderStatus",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkUpdateOrderStatusResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun updateStatusWithResponse(
        order: String,
        status: SdkUpdateOrderStatusRequestApplicationJsonStatus,
        expectedStatus: PatchField<ExpectedStatus?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateOrderStatusResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        updateStatus(order, status, expectedStatus, capturingOptions)
        return captured as? RawResponse<SdkUpdateOrderStatusResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [updateStatus]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [updateStatus] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateStatusSuspend")
    suspend fun updateStatusSuspend(
        order: String,
        status: SdkUpdateOrderStatusRequestApplicationJsonStatus,
        expectedStatus: PatchField<ExpectedStatus?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateOrderStatusResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            updateStatus(order, status, expectedStatus, requestOptions)
        }

    /**
     * Coroutine-aware variant of [updateStatusWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [updateStatusWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateStatusWithResponseSuspend")
    suspend fun updateStatusWithResponseSuspend(
        order: String,
        status: SdkUpdateOrderStatusRequestApplicationJsonStatus,
        expectedStatus: PatchField<ExpectedStatus?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateOrderStatusResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            updateStatusWithResponse(order, status, expectedStatus, requestOptions)
        }

    /**
     * Create order checkout
     *
     * Start or reuse checkout for a PENDING order. A new nonfree checkout returns 201; an existing
     * payment session or free order returns 200. Free or fully wallet-funded checkout can mark the
     * order paid. Failures can mark the order FAILED and release wallet reservations. Do not infer
     * payment from a checkout URL or browser return. The optional expected_status field is
     * validated but is not a checkout concurrency precondition. Requires the invoice ability or
     * orders:write OAuth scope and current store permission. Idempotency-Key is required. Reuse the
     * same key and identical request after a lost response; changed input or an in-progress request
     * returns 409. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param order The order identifier.
     * @param expectedStatus Validated status value; checkout does not compare it with current
     *   status. Retrieve the order before retrying.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateOrderCheckoutResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun createCheckout(
        order: String,
        expectedStatus: ExpectedStatus? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateOrderCheckoutResponseValue200ApplicationJson {
        val body = bodyOf("expected_status" to expectedStatus)
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/orders/${encodePathSegment(order)}/checkout",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateOrderCheckoutRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateOrderCheckoutResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "createOrderCheckout",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateOrderCheckoutResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createCheckoutWithResponse(
        order: String,
        expectedStatus: ExpectedStatus? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateOrderCheckoutResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        createCheckout(order, expectedStatus, capturingOptions)
        return captured as? RawResponse<SdkCreateOrderCheckoutResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [createCheckout]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [createCheckout] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createCheckoutSuspend")
    suspend fun createCheckoutSuspend(
        order: String,
        expectedStatus: ExpectedStatus? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateOrderCheckoutResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            createCheckout(order, expectedStatus, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createCheckoutWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [createCheckoutWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createCheckoutWithResponseSuspend")
    suspend fun createCheckoutWithResponseSuspend(
        order: String,
        expectedStatus: ExpectedStatus? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateOrderCheckoutResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createCheckoutWithResponse(order, expectedStatus, requestOptions)
        }

    /**
     * Issue replacements
     *
     * Create a replacement order and begin fulfillment for selected purchased variants. Supply a
     * variant ID, an array of variant IDs, or a map of variant IDs to replacement quantities (null
     * means original quantity). Variants must belong to this order; replacement quantities cannot
     * exceed purchased quantity or available stock. Already replaced deliveries cannot be replaced
     * again. The returned ID belongs to the new replacement order. Requires the invoice ability or
     * orders:write OAuth scope and current store permission. Idempotency-Key is required. Reuse the
     * same key and identical request after a lost response; changed input or an in-progress request
     * returns 409. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param order The order identifier.
     * @param productVariants the product variants of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateOrderReplacementResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun createReplacement(
        order: String,
        productVariants: CreateOrderReplacementRequestApplicationJsonPropertyProductVariants,
        requestOptions: RequestOptions? = null,
    ): SdkCreateOrderReplacementResponseValue200ApplicationJson {
        val body = bodyOf("product_variants" to productVariants)
        body["product_variants"] = productVariants
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/orders/${encodePathSegment(order)}/replacements",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateOrderReplacementRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateOrderReplacementResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "createOrderReplacement",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateOrderReplacementResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createReplacementWithResponse(
        order: String,
        productVariants: CreateOrderReplacementRequestApplicationJsonPropertyProductVariants,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateOrderReplacementResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        createReplacement(order, productVariants, capturingOptions)
        return captured as? RawResponse<SdkCreateOrderReplacementResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [createReplacement]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createReplacement] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createReplacementSuspend")
    suspend fun createReplacementSuspend(
        order: String,
        productVariants: CreateOrderReplacementRequestApplicationJsonPropertyProductVariants,
        requestOptions: RequestOptions? = null,
    ): SdkCreateOrderReplacementResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            createReplacement(order, productVariants, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createReplacementWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [createReplacementWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("createReplacementWithResponseSuspend")
    suspend fun createReplacementWithResponseSuspend(
        order: String,
        productVariants: CreateOrderReplacementRequestApplicationJsonPropertyProductVariants,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateOrderReplacementResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createReplacementWithResponse(order, productVariants, requestOptions)
        }

    /**
     * Refund an order
     *
     * Request a real refund for a completed, nonsubscription order with refundable balance and a
     * supported provider. amount is a decimal string in the order currency: "5.00" means $5.00 in
     * USD, not 500 cents. Omitting amount, sending null, or an empty string requests the FULL
     * remaining balance. Do not send amount_cents: it is not a request field. Inspect refund.status
     * (pending/effective/failed/cancelled); a 200 response does not guarantee money has reached the
     * customer. Retrieve refund state before retrying an uncertain provider outcome. Requires the
     * invoice ability or orders:write OAuth scope and current store permission. Idempotency-Key is
     * required. Reuse the same key and identical request after a lost response; changed input or an
     * in-progress request returns 409. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param order The order identifier.
     * @param amount Decimal major-unit amount in the order currency, positive and no greater than
     *   remaining balance. Precision follows that currency. Omission/null/empty requests the full
     *   remaining balance.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateOrderRefundResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun createRefund(
        order: String,
        amount: String? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateOrderRefundResponseValue200ApplicationJson {
        val body = bodyOf("amount" to amount)
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/orders/${encodePathSegment(order)}/refunds",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateOrderRefundRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateOrderRefundResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "createOrderRefund",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateOrderRefundResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createRefundWithResponse(
        order: String,
        amount: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateOrderRefundResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        createRefund(order, amount, capturingOptions)
        return captured as? RawResponse<SdkCreateOrderRefundResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [createRefund]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [createRefund] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createRefundSuspend")
    suspend fun createRefundSuspend(
        order: String,
        amount: String? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateOrderRefundResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            createRefund(order, amount, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createRefundWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createRefundWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createRefundWithResponseSuspend")
    suspend fun createRefundWithResponseSuspend(
        order: String,
        amount: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateOrderRefundResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createRefundWithResponse(order, amount, requestOptions)
        }

    /**
     * Retry fulfillment
     *
     * Reset fulfillment effects for a PAID or PARTIAL order and retry delivery. The optional email
     * overrides the recipient. meta.effects_reset counts reset effects; it does not prove
     * fulfillment finished. Other order states and a busy lifecycle lock return 422. Requires the
     * invoice ability or orders:write OAuth scope and current store permission. Idempotency-Key is
     * required. Reuse the same key and identical request after a lost response; changed input or an
     * in-progress request returns 409. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param order The order identifier.
     * @param email Optional delivery recipient override. Omit or send null to use the order
     *   delivery email.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkRetryOrderFulfillmentResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun retryFulfillment(
        order: String,
        email: String? = null,
        requestOptions: RequestOptions? = null,
    ): SdkRetryOrderFulfillmentResponseValue200ApplicationJson {
        val body = bodyOf("email" to email)
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/orders/${encodePathSegment(order)}/fulfillment-retries",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkRetryOrderFulfillmentRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkRetryOrderFulfillmentResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "retryOrderFulfillment",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkRetryOrderFulfillmentResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun retryFulfillmentWithResponse(
        order: String,
        email: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkRetryOrderFulfillmentResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        retryFulfillment(order, email, capturingOptions)
        return captured as? RawResponse<SdkRetryOrderFulfillmentResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [retryFulfillment]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [retryFulfillment] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("retryFulfillmentSuspend")
    suspend fun retryFulfillmentSuspend(
        order: String,
        email: String? = null,
        requestOptions: RequestOptions? = null,
    ): SdkRetryOrderFulfillmentResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            retryFulfillment(order, email, requestOptions)
        }

    /**
     * Coroutine-aware variant of [retryFulfillmentWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [retryFulfillmentWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("retryFulfillmentWithResponseSuspend")
    suspend fun retryFulfillmentWithResponseSuspend(
        order: String,
        email: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkRetryOrderFulfillmentResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            retryFulfillmentWithResponse(order, email, requestOptions)
        }

    /**
     * Retry dynamic delivery
     *
     * Queue another dynamic delivery attempt for one delivered product of a COMPLETED order. The
     * delivered product, line item and currently dynamic variant must all belong to this order and
     * store, with a configured webhook. 202 and meta.queued acknowledge the retry request, not
     * successful external delivery. Requires the invoice ability or orders:write OAuth scope and
     * current store permission. Idempotency-Key is required. Reuse the same key and identical
     * request after a lost response; changed input or an in-progress request returns 409. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param order The order identifier.
     * @param deliveredProductId Delivered product record ID, not a catalog product or order
     *   line-item ID. The public order-deliverables response does not currently expose this ID;
     *   obtain it from an authorized integration context before requesting a retry.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson
     */
    @JvmOverloads
    fun retryDynamicDelivery(
        order: String,
        deliveredProductId: Long,
        requestOptions: RequestOptions? = null,
    ): SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson {
        val body = bodyOf("delivered_product_id" to deliveredProductId)
        body["delivered_product_id"] = deliveredProductId
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/orders/${encodePathSegment(order)}/dynamic-delivery-retries",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkRetryOrderDynamicDeliveryRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson
                        .runtimeSchema,
                operationId = "retryOrderDynamicDelivery",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun retryDynamicDeliveryWithResponse(
        order: String,
        deliveredProductId: Long,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        retryDynamicDelivery(order, deliveredProductId, capturingOptions)
        return captured as? RawResponse<SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [retryDynamicDelivery]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [retryDynamicDelivery] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("retryDynamicDeliverySuspend")
    suspend fun retryDynamicDeliverySuspend(
        order: String,
        deliveredProductId: Long,
        requestOptions: RequestOptions? = null,
    ): SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson =
        sellapp.baseClient.runCancellable {
            retryDynamicDelivery(order, deliveredProductId, requestOptions)
        }

    /**
     * Coroutine-aware variant of [retryDynamicDeliveryWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [retryDynamicDeliveryWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("retryDynamicDeliveryWithResponseSuspend")
    suspend fun retryDynamicDeliveryWithResponseSuspend(
        order: String,
        deliveredProductId: Long,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson> =
        sellapp.baseClient.runCancellable {
            retryDynamicDeliveryWithResponse(order, deliveredProductId, requestOptions)
        }

    /**
     * Send fulfillment notifications
     *
     * Send delivery notifications for each selected purchased variant. Omit product_variant_ids to
     * select all variants. A supplied list must contain distinct positive IDs belonging to this
     * order and store. Noncompleted orders or a missing recipient return meta.notifications_sent=0.
     * The count records notifications requested, not email delivery confirmation. Requires the
     * invoice ability or orders:write OAuth scope and current store permission. Idempotency-Key is
     * required. Reuse the same key and identical request after a lost response; changed input or an
     * in-progress request returns 409. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param order The order identifier.
     * @param email Optional delivery recipient override. Omit or send null to use the order
     *   delivery email.
     * @param productVariantIds the product variant ids of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun sendFulfillmentNotifications(
        order: String,
        email: String? = null,
        productVariantIds: List<Long>? = null,
        requestOptions: RequestOptions? = null,
    ): SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "email" to email,
                "product_variant_ids" to productVariantIds,
            )
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/orders/${encodePathSegment(order)}/fulfillment-notifications",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models
                        .SdkSendOrderFulfillmentNotificationsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "sendOrderFulfillmentNotifications",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun sendFulfillmentNotificationsWithResponse(
        order: String,
        email: String? = null,
        productVariantIds: List<Long>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        sendFulfillmentNotifications(order, email, productVariantIds, capturingOptions)
        return captured
            as? RawResponse<SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [sendFulfillmentNotifications]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [sendFulfillmentNotifications] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("sendFulfillmentNotificationsSuspend")
    suspend fun sendFulfillmentNotificationsSuspend(
        order: String,
        email: String? = null,
        productVariantIds: List<Long>? = null,
        requestOptions: RequestOptions? = null,
    ): SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            sendFulfillmentNotifications(order, email, productVariantIds, requestOptions)
        }

    /**
     * Coroutine-aware variant of [sendFulfillmentNotificationsWithResponse]. Use this from a
     * `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [sendFulfillmentNotificationsWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("sendFulfillmentNotificationsWithResponseSuspend")
    suspend fun sendFulfillmentNotificationsWithResponseSuspend(
        order: String,
        email: String? = null,
        productVariantIds: List<Long>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            sendFulfillmentNotificationsWithResponse(
                order,
                email,
                productVariantIds,
                requestOptions,
            )
        }

    /**
     * List order deliverables
     *
     * List seller-visible fulfillment records, expanding purchased bundle snapshots into their
     * component variants. File download links expire after one hour. This payload includes
     * line-item and variant IDs but does not expose the delivered_product_id required for dynamic
     * delivery retries. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param order The order identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkListOrderDeliverablesResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun listDeliverables(
        order: String,
        requestOptions: RequestOptions? = null,
    ): SdkListOrderDeliverablesResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/orders/${encodePathSegment(order)}/deliverables",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkListOrderDeliverablesResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "listOrderDeliverables",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkListOrderDeliverablesResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun listDeliverablesWithResponse(
        order: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkListOrderDeliverablesResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        listDeliverables(order, capturingOptions)
        return captured as? RawResponse<SdkListOrderDeliverablesResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [listDeliverables]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listDeliverables] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listDeliverablesSuspend")
    suspend fun listDeliverablesSuspend(
        order: String,
        requestOptions: RequestOptions? = null,
    ): SdkListOrderDeliverablesResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            listDeliverables(order, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listDeliverablesWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [listDeliverablesWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listDeliverablesWithResponseSuspend")
    suspend fun listDeliverablesWithResponseSuspend(
        order: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkListOrderDeliverablesResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            listDeliverablesWithResponse(order, requestOptions)
        }

    /**
     * Create and pay an order from a wallet
     *
     * Create an order and debit the customer's wallet in one atomic call. This spends real wallet
     * funds and queues normal fulfillment; PAID means payment was captured, while COMPLETED depends
     * on fulfillment. Requires both invoice and wallet:write API-key abilities and both store
     * permissions. Wallets must be enabled, the customer must already exist in this store, and an
     * active wallet must cover the entire positive total after discounts and taxes. Subscriptions
     * and partial wallet payments are not supported. Omit payment_method and
     * custom_payment_method_id. An insufficient balance or invalid cart returns 422 without
     * creating an order or debit. Send the required Idempotency-Key; retry an identical request
     * with the same key after a lost response. Reusing it with different input returns 409. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param customerEmail the customer email of the request.
     * @param productVariants the product variants of the request.
     * @param customerIp the customer ip of the request.
     * @param coupon the coupon of the request.
     * @param vatId the vat id of the request.
     * @param country the country of the request.
     * @param affiliate the affiliate of the request.
     * @param extra the extra of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateWalletOrderResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun createFromWallet(
        customerEmail: String,
        productVariants: CreateWalletOrderRequestApplicationJsonPropertyProductVariants,
        customerIp: String? = null,
        coupon: String? = null,
        vatId: String? = null,
        country: String? = null,
        affiliate: String? = null,
        extra: CreateWalletOrderRequestApplicationJsonPropertyExtra? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateWalletOrderResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "customer_email" to customerEmail,
                "product_variants" to productVariants,
                "customer_ip" to customerIp,
                "coupon" to coupon,
                "vat_id" to vatId,
                "country" to country,
                "affiliate" to affiliate,
                "extra" to extra,
            )
        body["customer_email"] = customerEmail
        body["product_variants"] = productVariants
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/orders/wallet",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateWalletOrderRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateWalletOrderResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "createWalletOrder",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateWalletOrderResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createFromWalletWithResponse(
        customerEmail: String,
        productVariants: CreateWalletOrderRequestApplicationJsonPropertyProductVariants,
        customerIp: String? = null,
        coupon: String? = null,
        vatId: String? = null,
        country: String? = null,
        affiliate: String? = null,
        extra: CreateWalletOrderRequestApplicationJsonPropertyExtra? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateWalletOrderResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        createFromWallet(
            customerEmail,
            productVariants,
            customerIp,
            coupon,
            vatId,
            country,
            affiliate,
            extra,
            capturingOptions,
        )
        return captured as? RawResponse<SdkCreateWalletOrderResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [createFromWallet]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createFromWallet] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createFromWalletSuspend")
    suspend fun createFromWalletSuspend(
        customerEmail: String,
        productVariants: CreateWalletOrderRequestApplicationJsonPropertyProductVariants,
        customerIp: String? = null,
        coupon: String? = null,
        vatId: String? = null,
        country: String? = null,
        affiliate: String? = null,
        extra: CreateWalletOrderRequestApplicationJsonPropertyExtra? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateWalletOrderResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            createFromWallet(
                customerEmail,
                productVariants,
                customerIp,
                coupon,
                vatId,
                country,
                affiliate,
                extra,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [createFromWalletWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [createFromWalletWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createFromWalletWithResponseSuspend")
    suspend fun createFromWalletWithResponseSuspend(
        customerEmail: String,
        productVariants: CreateWalletOrderRequestApplicationJsonPropertyProductVariants,
        customerIp: String? = null,
        coupon: String? = null,
        vatId: String? = null,
        country: String? = null,
        affiliate: String? = null,
        extra: CreateWalletOrderRequestApplicationJsonPropertyExtra? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateWalletOrderResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createFromWalletWithResponse(
                customerEmail,
                productVariants,
                customerIp,
                coupon,
                vatId,
                country,
                affiliate,
                extra,
                requestOptions,
            )
        }

    /**
     * Pay an existing order from its customer wallet
     *
     * Debit the order's customer wallet for the complete stored order total and queue normal
     * fulfillment. Requires both invoice and wallet:write API-key abilities and both store
     * permissions. Only PENDING orders with pending lines, no subscription, and no existing payment
     * method or checkout session are eligible. Active provider checkouts cannot be replaced. The
     * store must enable wallets and the customer's active wallet must cover the full amount. The
     * order ID determines the customer and amount; neither can be overridden. Send the required
     * Idempotency-Key and reuse it with identical input after a lost response. A changed
     * expected_status returns 409; an ineligible order or insufficient balance returns 422 without
     * a debit. PAID does not guarantee fulfillment has completed. OAuth callers use the admin grant
     * and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param order the order of the request.
     * @param expectedStatus the expected status of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkPayOrderFromWalletResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun payFromWallet(
        order: String,
        expectedStatus: ExpectedStatus? = null,
        requestOptions: RequestOptions? = null,
    ): SdkPayOrderFromWalletResponseValue200ApplicationJson {
        val body = bodyOf("expected_status" to expectedStatus)
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/orders/${encodePathSegment(order)}/wallet-payments",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkPayOrderFromWalletRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkPayOrderFromWalletResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "payOrderFromWallet",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkPayOrderFromWalletResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun payFromWalletWithResponse(
        order: String,
        expectedStatus: ExpectedStatus? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkPayOrderFromWalletResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        payFromWallet(order, expectedStatus, capturingOptions)
        return captured as? RawResponse<SdkPayOrderFromWalletResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [payFromWallet]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [payFromWallet] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("payFromWalletSuspend")
    suspend fun payFromWalletSuspend(
        order: String,
        expectedStatus: ExpectedStatus? = null,
        requestOptions: RequestOptions? = null,
    ): SdkPayOrderFromWalletResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            payFromWallet(order, expectedStatus, requestOptions)
        }

    /**
     * Coroutine-aware variant of [payFromWalletWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [payFromWalletWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("payFromWalletWithResponseSuspend")
    suspend fun payFromWalletWithResponseSuspend(
        order: String,
        expectedStatus: ExpectedStatus? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkPayOrderFromWalletResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            payFromWalletWithResponse(order, expectedStatus, requestOptions)
        }
}
