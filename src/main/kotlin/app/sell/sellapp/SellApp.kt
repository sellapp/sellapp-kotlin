// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp

import app.sell.sellapp.account.Account
import app.sell.sellapp.addons.AddOns
import app.sell.sellapp.addonsparentproducts.AddOnsParentProducts
import app.sell.sellapp.affiliatepayouts.AffiliatePayouts
import app.sell.sellapp.affiliateprogram.AffiliateProgram
import app.sell.sellapp.affiliatereferrals.AffiliateReferrals
import app.sell.sellapp.affiliatereferralsessions.AffiliateReferralSessions
import app.sell.sellapp.affiliates.Affiliates
import app.sell.sellapp.blacklists.Blacklists
import app.sell.sellapp.bookings.Bookings
import app.sell.sellapp.bookingscalendarevents.BookingsCalendarEvents
import app.sell.sellapp.bundleitems.BundleItems
import app.sell.sellapp.cashbackrules.CashbackRules
import app.sell.sellapp.charges.Charges
import app.sell.sellapp.common.http.BaseClient
import app.sell.sellapp.communityconnections.CommunityConnections
import app.sell.sellapp.coupons.Coupons
import app.sell.sellapp.courses.Courses
import app.sell.sellapp.courseslessons.CoursesLessons
import app.sell.sellapp.coursessections.CoursesSections
import app.sell.sellapp.credits.Credits
import app.sell.sellapp.creditsbalances.CreditsBalances
import app.sell.sellapp.creditsbalancestransactions.CreditsBalancesTransactions
import app.sell.sellapp.creditsproducts.CreditsProducts
import app.sell.sellapp.customerportal.CustomerPortal
import app.sell.sellapp.customers.Customers
import app.sell.sellapp.customersessions.CustomerSessions
import app.sell.sellapp.disputes.Disputes
import app.sell.sellapp.entitlements.Entitlements
import app.sell.sellapp.events.Events
import app.sell.sellapp.exports.Exports
import app.sell.sellapp.feedback.Feedback
import app.sell.sellapp.groups.Groups
import app.sell.sellapp.groupsproducts.GroupsProducts
import app.sell.sellapp.highlights.Highlights
import app.sell.sellapp.highlightsmedia.HighlightsMedia
import app.sell.sellapp.invoices.Invoices
import app.sell.sellapp.licenseinstances.LicenseInstances
import app.sell.sellapp.licenses.Licenses
import app.sell.sellapp.licensesinstances.LicensesInstances
import app.sell.sellapp.lineitems.LineItems
import app.sell.sellapp.oauth.OAuth
import app.sell.sellapp.oauthmanagement.OAuthManagement
import app.sell.sellapp.orders.Orders
import app.sell.sellapp.orderslineitems.OrdersLineItems
import app.sell.sellapp.paymentmethods.PaymentMethods
import app.sell.sellapp.paymentmethodscustom.PaymentMethodsCustom
import app.sell.sellapp.products.Products
import app.sell.sellapp.productsaddons.ProductsAddons
import app.sell.sellapp.productvariants.ProductVariants
import app.sell.sellapp.productvariantsbooking.ProductVariantsBooking
import app.sell.sellapp.productvariantspricing.ProductVariantsPricing
import app.sell.sellapp.promotions.Promotions
import app.sell.sellapp.promotionsphases.PromotionsPhases
import app.sell.sellapp.refunds.Refunds
import app.sell.sellapp.rewardcoupontemplates.RewardCouponTemplates
import app.sell.sellapp.rewardgrants.RewardGrants
import app.sell.sellapp.rewardrules.RewardRules
import app.sell.sellapp.sections.Sections
import app.sell.sellapp.storecustomdomains.StoreCustomDomains
import app.sell.sellapp.storenotificationchannels.StoreNotificationChannels
import app.sell.sellapp.storesettings.StoreSettings
import app.sell.sellapp.subscriptions.Subscriptions
import app.sell.sellapp.tickets.Tickets
import app.sell.sellapp.ticketsmessages.TicketsMessages
import app.sell.sellapp.upselloffers.UpsellOffers
import app.sell.sellapp.variantdeliverablefiles.VariantDeliverableFiles
import app.sell.sellapp.variantdeliverablefolders.VariantDeliverableFolders
import app.sell.sellapp.variantdeliverables.VariantDeliverables
import app.sell.sellapp.variantserials.VariantSerials
import app.sell.sellapp.wallet.Wallet
import app.sell.sellapp.walletbonustiers.WalletBonusTiers
import app.sell.sellapp.walletsettings.WalletSettings
import app.sell.sellapp.wallettopups.WalletTopups
import app.sell.sellapp.wallettransactions.WalletTransactions
import app.sell.sellapp.webhookchannels.WebhookChannels
import app.sell.sellapp.webhookplatform.WebhookPlatform
import java.util.concurrent.ConcurrentHashMap
import kotlin.reflect.KClass
import okhttp3.OkHttpClient

open class SellApp
@JvmOverloads
constructor(
    apiKey: String? = null,
    store: String? = null,
    baseUrl: String = "https://sell.app/api",
    timeoutSeconds: Long = 60,
    maxRetries: Int = 3,
    httpClient: OkHttpClient? = null,
    requestHook: ((app.sell.sellapp.common.http.RequestEvent) -> Unit)? = null,
    accessToken: String? = null,
    customerSession: String? = null,
    browserSession: String? = null,
    clientId: String? = null,
    clientSecret: String? = null,
) : AutoCloseable {
    val apiKey: String? =
        if (accessToken != null) null else apiKey ?: System.getenv("SELLAPP_API_KEY")
    val store: String? = store ?: System.getenv("SELLAPP_STORE")
    internal val baseClient =
        BaseClient(
            this.apiKey,
            this.store,
            baseUrl,
            timeoutSeconds,
            maxRetries,
            httpClient,
            requestHook,
            accessToken = accessToken,
            customerSession = customerSession,
            browserSession = browserSession,
            clientId = clientId,
            clientSecret = clientSecret,
        )
    private val services = ConcurrentHashMap<KClass<*>, Any>()

    @Suppress("UNCHECKED_CAST")
    internal fun <T : Any> service(type: KClass<T>, factory: () -> T): T =
        services.getOrPut(type, factory) as T

    /** Lazily-constructed [Charges] accessor for this [SellApp] client. */
    val charges: Charges
        get() =
            service(Charges::class) {
                Charges(this)
            }

    /** Lazily-constructed [Groups] accessor for this [SellApp] client. */
    val groups: Groups
        get() =
            service(Groups::class) {
                Groups(this)
            }

    /** Lazily-constructed [GroupsProducts] accessor for this [SellApp] client. */
    val groupsProducts: GroupsProducts
        get() =
            service(GroupsProducts::class) {
                GroupsProducts(this)
            }

    /** Lazily-constructed [Invoices] accessor for this [SellApp] client. */
    val invoices: Invoices
        get() =
            service(Invoices::class) {
                Invoices(this)
            }

    /** Lazily-constructed [Licenses] accessor for this [SellApp] client. */
    val licenses: Licenses
        get() =
            service(Licenses::class) {
                Licenses(this)
            }

    /** Lazily-constructed [LicensesInstances] accessor for this [SellApp] client. */
    val licensesInstances: LicensesInstances
        get() =
            service(LicensesInstances::class) {
                LicensesInstances(this)
            }

    /** Lazily-constructed [LicenseInstances] accessor for this [SellApp] client. */
    val licenseInstances: LicenseInstances
        get() =
            service(LicenseInstances::class) {
                LicenseInstances(this)
            }

    /** Lazily-constructed [Products] accessor for this [SellApp] client. */
    val products: Products
        get() =
            service(Products::class) {
                Products(this)
            }

    /** Lazily-constructed [ProductVariants] accessor for this [SellApp] client. */
    val productVariants: ProductVariants
        get() =
            service(ProductVariants::class) {
                ProductVariants(this)
            }

    /** Lazily-constructed [ProductVariantsPricing] accessor for this [SellApp] client. */
    val productVariantsPricing: ProductVariantsPricing
        get() =
            service(ProductVariantsPricing::class) {
                ProductVariantsPricing(this)
            }

    /** Lazily-constructed [ProductVariantsBooking] accessor for this [SellApp] client. */
    val productVariantsBooking: ProductVariantsBooking
        get() =
            service(ProductVariantsBooking::class) {
                ProductVariantsBooking(this)
            }

    /** Lazily-constructed [Subscriptions] accessor for this [SellApp] client. */
    val subscriptions: Subscriptions
        get() =
            service(Subscriptions::class) {
                Subscriptions(this)
            }

    /** Lazily-constructed [Bookings] accessor for this [SellApp] client. */
    val bookings: Bookings
        get() =
            service(Bookings::class) {
                Bookings(this)
            }

    /** Lazily-constructed [BookingsCalendarEvents] accessor for this [SellApp] client. */
    val bookingsCalendarEvents: BookingsCalendarEvents
        get() =
            service(BookingsCalendarEvents::class) {
                BookingsCalendarEvents(this)
            }

    /** Lazily-constructed [BundleItems] accessor for this [SellApp] client. */
    val bundleItems: BundleItems
        get() =
            service(BundleItems::class) {
                BundleItems(this)
            }

    /** Lazily-constructed [Promotions] accessor for this [SellApp] client. */
    val promotions: Promotions
        get() =
            service(Promotions::class) {
                Promotions(this)
            }

    /** Lazily-constructed [PromotionsPhases] accessor for this [SellApp] client. */
    val promotionsPhases: PromotionsPhases
        get() =
            service(PromotionsPhases::class) {
                PromotionsPhases(this)
            }

    /** Lazily-constructed [AddOns] accessor for this [SellApp] client. */
    val addOns: AddOns
        get() =
            service(AddOns::class) {
                AddOns(this)
            }

    /** Lazily-constructed [AddOnsParentProducts] accessor for this [SellApp] client. */
    val addOnsParentProducts: AddOnsParentProducts
        get() =
            service(AddOnsParentProducts::class) {
                AddOnsParentProducts(this)
            }

    /** Lazily-constructed [ProductsAddons] accessor for this [SellApp] client. */
    val productsAddons: ProductsAddons
        get() =
            service(ProductsAddons::class) {
                ProductsAddons(this)
            }

    /** Lazily-constructed [Highlights] accessor for this [SellApp] client. */
    val highlights: Highlights
        get() =
            service(Highlights::class) {
                Highlights(this)
            }

    /** Lazily-constructed [HighlightsMedia] accessor for this [SellApp] client. */
    val highlightsMedia: HighlightsMedia
        get() =
            service(HighlightsMedia::class) {
                HighlightsMedia(this)
            }

    /** Lazily-constructed [WebhookChannels] accessor for this [SellApp] client. */
    val webhookChannels: WebhookChannels
        get() =
            service(WebhookChannels::class) {
                WebhookChannels(this)
            }

    /** Lazily-constructed [Customers] accessor for this [SellApp] client. */
    val customers: Customers
        get() =
            service(Customers::class) {
                Customers(this)
            }

    /** Lazily-constructed [Orders] accessor for this [SellApp] client. */
    val orders: Orders
        get() =
            service(Orders::class) {
                Orders(this)
            }

    /** Lazily-constructed [LineItems] accessor for this [SellApp] client. */
    val lineItems: LineItems
        get() =
            service(LineItems::class) {
                LineItems(this)
            }

    /** Lazily-constructed [OrdersLineItems] accessor for this [SellApp] client. */
    val ordersLineItems: OrdersLineItems
        get() =
            service(OrdersLineItems::class) {
                OrdersLineItems(this)
            }

    /** Lazily-constructed [VariantDeliverables] accessor for this [SellApp] client. */
    val variantDeliverables: VariantDeliverables
        get() =
            service(VariantDeliverables::class) {
                VariantDeliverables(this)
            }

    /** Lazily-constructed [VariantDeliverableFiles] accessor for this [SellApp] client. */
    val variantDeliverableFiles: VariantDeliverableFiles
        get() =
            service(VariantDeliverableFiles::class) {
                VariantDeliverableFiles(this)
            }

    /** Lazily-constructed [VariantDeliverableFolders] accessor for this [SellApp] client. */
    val variantDeliverableFolders: VariantDeliverableFolders
        get() =
            service(VariantDeliverableFolders::class) {
                VariantDeliverableFolders(this)
            }

    /** Lazily-constructed [VariantSerials] accessor for this [SellApp] client. */
    val variantSerials: VariantSerials
        get() =
            service(VariantSerials::class) {
                VariantSerials(this)
            }

    /** Lazily-constructed [AffiliateProgram] accessor for this [SellApp] client. */
    val affiliateProgram: AffiliateProgram
        get() =
            service(AffiliateProgram::class) {
                AffiliateProgram(this)
            }

    /** Lazily-constructed [RewardRules] accessor for this [SellApp] client. */
    val rewardRules: RewardRules
        get() =
            service(RewardRules::class) {
                RewardRules(this)
            }

    /** Lazily-constructed [RewardCouponTemplates] accessor for this [SellApp] client. */
    val rewardCouponTemplates: RewardCouponTemplates
        get() =
            service(RewardCouponTemplates::class) {
                RewardCouponTemplates(this)
            }

    /** Lazily-constructed [RewardGrants] accessor for this [SellApp] client. */
    val rewardGrants: RewardGrants
        get() =
            service(RewardGrants::class) {
                RewardGrants(this)
            }

    /** Lazily-constructed [UpsellOffers] accessor for this [SellApp] client. */
    val upsellOffers: UpsellOffers
        get() =
            service(UpsellOffers::class) {
                UpsellOffers(this)
            }

    /** Lazily-constructed [CommunityConnections] accessor for this [SellApp] client. */
    val communityConnections: CommunityConnections
        get() =
            service(CommunityConnections::class) {
                CommunityConnections(this)
            }

    /** Lazily-constructed [WalletSettings] accessor for this [SellApp] client. */
    val walletSettings: WalletSettings
        get() =
            service(WalletSettings::class) {
                WalletSettings(this)
            }

    /** Lazily-constructed [Wallet] accessor for this [SellApp] client. */
    val wallet: Wallet
        get() =
            service(Wallet::class) {
                Wallet(this)
            }

    /** Lazily-constructed [WalletTransactions] accessor for this [SellApp] client. */
    val walletTransactions: WalletTransactions
        get() =
            service(WalletTransactions::class) {
                WalletTransactions(this)
            }

    /** Lazily-constructed [WalletTopups] accessor for this [SellApp] client. */
    val walletTopups: WalletTopups
        get() =
            service(WalletTopups::class) {
                WalletTopups(this)
            }

    /** Lazily-constructed [CashbackRules] accessor for this [SellApp] client. */
    val cashbackRules: CashbackRules
        get() =
            service(CashbackRules::class) {
                CashbackRules(this)
            }

    /** Lazily-constructed [WalletBonusTiers] accessor for this [SellApp] client. */
    val walletBonusTiers: WalletBonusTiers
        get() =
            service(WalletBonusTiers::class) {
                WalletBonusTiers(this)
            }

    /** Lazily-constructed [Courses] accessor for this [SellApp] client. */
    val courses: Courses
        get() =
            service(Courses::class) {
                Courses(this)
            }

    /** Lazily-constructed [CoursesSections] accessor for this [SellApp] client. */
    val coursesSections: CoursesSections
        get() =
            service(CoursesSections::class) {
                CoursesSections(this)
            }

    /** Lazily-constructed [CoursesLessons] accessor for this [SellApp] client. */
    val coursesLessons: CoursesLessons
        get() =
            service(CoursesLessons::class) {
                CoursesLessons(this)
            }

    /** Lazily-constructed [PaymentMethods] accessor for this [SellApp] client. */
    val paymentMethods: PaymentMethods
        get() =
            service(PaymentMethods::class) {
                PaymentMethods(this)
            }

    /** Lazily-constructed [PaymentMethodsCustom] accessor for this [SellApp] client. */
    val paymentMethodsCustom: PaymentMethodsCustom
        get() =
            service(PaymentMethodsCustom::class) {
                PaymentMethodsCustom(this)
            }

    /** Lazily-constructed [StoreSettings] accessor for this [SellApp] client. */
    val storeSettings: StoreSettings
        get() =
            service(StoreSettings::class) {
                StoreSettings(this)
            }

    /** Lazily-constructed [StoreNotificationChannels] accessor for this [SellApp] client. */
    val storeNotificationChannels: StoreNotificationChannels
        get() =
            service(StoreNotificationChannels::class) {
                StoreNotificationChannels(this)
            }

    /** Lazily-constructed [StoreCustomDomains] accessor for this [SellApp] client. */
    val storeCustomDomains: StoreCustomDomains
        get() =
            service(StoreCustomDomains::class) {
                StoreCustomDomains(this)
            }

    /** Lazily-constructed [CreditsProducts] accessor for this [SellApp] client. */
    val creditsProducts: CreditsProducts
        get() =
            service(CreditsProducts::class) {
                CreditsProducts(this)
            }

    /** Lazily-constructed [CreditsBalances] accessor for this [SellApp] client. */
    val creditsBalances: CreditsBalances
        get() =
            service(CreditsBalances::class) {
                CreditsBalances(this)
            }

    /** Lazily-constructed [Credits] accessor for this [SellApp] client. */
    val credits: Credits
        get() =
            service(Credits::class) {
                Credits(this)
            }

    /** Lazily-constructed [CreditsBalancesTransactions] accessor for this [SellApp] client. */
    val creditsBalancesTransactions: CreditsBalancesTransactions
        get() =
            service(CreditsBalancesTransactions::class) {
                CreditsBalancesTransactions(this)
            }

    /** Lazily-constructed [Affiliates] accessor for this [SellApp] client. */
    val affiliates: Affiliates
        get() =
            service(Affiliates::class) {
                Affiliates(this)
            }

    /** Lazily-constructed [AffiliateReferrals] accessor for this [SellApp] client. */
    val affiliateReferrals: AffiliateReferrals
        get() =
            service(AffiliateReferrals::class) {
                AffiliateReferrals(this)
            }

    /** Lazily-constructed [AffiliateReferralSessions] accessor for this [SellApp] client. */
    val affiliateReferralSessions: AffiliateReferralSessions
        get() =
            service(AffiliateReferralSessions::class) {
                AffiliateReferralSessions(this)
            }

    /** Lazily-constructed [AffiliatePayouts] accessor for this [SellApp] client. */
    val affiliatePayouts: AffiliatePayouts
        get() =
            service(AffiliatePayouts::class) {
                AffiliatePayouts(this)
            }

    /** Lazily-constructed [Refunds] accessor for this [SellApp] client. */
    val refunds: Refunds
        get() =
            service(Refunds::class) {
                Refunds(this)
            }

    /** Lazily-constructed [Exports] accessor for this [SellApp] client. */
    val exports: Exports
        get() =
            service(Exports::class) {
                Exports(this)
            }

    /** Lazily-constructed [Disputes] accessor for this [SellApp] client. */
    val disputes: Disputes
        get() =
            service(Disputes::class) {
                Disputes(this)
            }

    /** Lazily-constructed [Entitlements] accessor for this [SellApp] client. */
    val entitlements: Entitlements
        get() =
            service(Entitlements::class) {
                Entitlements(this)
            }

    /** Lazily-constructed [CustomerSessions] accessor for this [SellApp] client. */
    val customerSessions: CustomerSessions
        get() =
            service(CustomerSessions::class) {
                CustomerSessions(this)
            }

    /** Lazily-constructed [CustomerPortal] accessor for this [SellApp] client. */
    val customerPortal: CustomerPortal
        get() =
            service(CustomerPortal::class) {
                CustomerPortal(this)
            }

    /** Lazily-constructed [Events] accessor for this [SellApp] client. */
    val events: Events
        get() =
            service(Events::class) {
                Events(this)
            }

    /** Lazily-constructed [WebhookPlatform] accessor for this [SellApp] client. */
    val webhookPlatform: WebhookPlatform
        get() =
            service(WebhookPlatform::class) {
                WebhookPlatform(this)
            }

    /** Lazily-constructed [Account] accessor for this [SellApp] client. */
    val account: Account
        get() =
            service(Account::class) {
                Account(this)
            }

    /** Lazily-constructed [OAuthManagement] accessor for this [SellApp] client. */
    val oauthManagement: OAuthManagement
        get() =
            service(OAuthManagement::class) {
                OAuthManagement(this)
            }

    /** Lazily-constructed [Blacklists] accessor for this [SellApp] client. */
    val blacklists: Blacklists
        get() =
            service(Blacklists::class) {
                Blacklists(this)
            }

    /** Lazily-constructed [Coupons] accessor for this [SellApp] client. */
    val coupons: Coupons
        get() =
            service(Coupons::class) {
                Coupons(this)
            }

    /** Lazily-constructed [Feedback] accessor for this [SellApp] client. */
    val feedback: Feedback
        get() =
            service(Feedback::class) {
                Feedback(this)
            }

    /** Lazily-constructed [Sections] accessor for this [SellApp] client. */
    val sections: Sections
        get() =
            service(Sections::class) {
                Sections(this)
            }

    /** Lazily-constructed [Tickets] accessor for this [SellApp] client. */
    val tickets: Tickets
        get() =
            service(Tickets::class) {
                Tickets(this)
            }

    /** Lazily-constructed [TicketsMessages] accessor for this [SellApp] client. */
    val ticketsMessages: TicketsMessages
        get() =
            service(TicketsMessages::class) {
                TicketsMessages(this)
            }

    /** Lazily-constructed [OAuth] accessor for this [SellApp] client. */
    val oauth: OAuth
        get() =
            service(OAuth::class) {
                OAuth(this)
            }

    fun request(
        method: String,
        path: String,
        body: Any? = null,
        query: List<Pair<String, String>> = emptyList(),
        requestOptions: app.sell.sellapp.common.http.RequestOptions? = null,
    ): app.sell.sellapp.common.http.RawResponse<com.fasterxml.jackson.databind.JsonNode> =
        baseClient.customRequest(method, path, body, query, requestOptions)

    suspend fun requestSuspend(
        method: String,
        path: String,
        body: Any? = null,
        query: List<Pair<String, String>> = emptyList(),
        requestOptions: app.sell.sellapp.common.http.RequestOptions? = null,
    ): app.sell.sellapp.common.http.RawResponse<com.fasterxml.jackson.databind.JsonNode> =
        baseClient.runCancellable {
            request(method, path, body, query, requestOptions)
        }

    override fun close() = baseClient.close()
}
