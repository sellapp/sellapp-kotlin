// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.oauth

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.addIfNotNull
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkExchangeOAuthTokenResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson
import app.sell.sellapp.types.SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencodedTokenTypeHint

/**
 * API accessor for Oauth.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class OAuth(internal val sellapp: SellApp) {
    /**
     * Read OAuth server metadata
     *
     * Discover the official CLI authorization, token and revocation endpoints and supported scopes.
     * The CLI uses public client authentication without a secret.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun getAuthorizationServerMetadata(
        requestOptions: RequestOptions? = null
    ): SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/.well-known/oauth-authorization-server",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getOAuthAuthorizationServerMetadata",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getAuthorizationServerMetadataWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        getAuthorizationServerMetadata(capturingOptions)
        return captured
            as? RawResponse<SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [getAuthorizationServerMetadata]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [getAuthorizationServerMetadata] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("getAuthorizationServerMetadataSuspend")
    suspend fun getAuthorizationServerMetadataSuspend(
        requestOptions: RequestOptions? = null
    ): SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            getAuthorizationServerMetadata(requestOptions)
        }

    /**
     * Coroutine-aware variant of [getAuthorizationServerMetadataWithResponse]. Use this from a
     * `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [getAuthorizationServerMetadataWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("getAuthorizationServerMetadataWithResponseSuspend")
    suspend fun getAuthorizationServerMetadataWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getAuthorizationServerMetadataWithResponse(requestOptions)
        }

    /**
     * Review CLI authorization
     *
     * Open in the user's browser to authorize the official CLI using existing store permissions.
     * Only the configured public CLI registration is supported. PKCE S256 and non-empty state are
     * required. The callback may vary only the port of the registered numeric loopback URL;
     * localhost, wildcard hosts, fragments and non-loopback HTTP are rejected. Unsupported clients
     * receive invalid_client and unsupported scopes receive invalid_scope.
     *
     * @param responseType the response type of the request.
     * @param clientId the client id of the request.
     * @param redirectUri the redirect uri of the request.
     * @param state the state of the request.
     * @param codeChallenge the code challenge of the request.
     * @param codeChallengeMethod the code challenge method of the request.
     * @param scope the scope of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun getAuthorizationRequest(
        responseType: String,
        clientId: String,
        redirectUri: String,
        state: String,
        codeChallenge: String,
        codeChallengeMethod: String,
        scope: String? = null,
        requestOptions: RequestOptions? = null,
    ): String {
        val params = mutableListOf<Pair<String, String>>()
        params += "response_type" to responseType.toString()
        params += "client_id" to clientId
        params += "redirect_uri" to redirectUri
        params += "state" to state
        params += "code_challenge" to codeChallenge
        params += "code_challenge_method" to codeChallengeMethod.toString()
        params.addIfNotNull("scope", scope)
        val config =
            RequestConfig(
                method = "GET",
                path = "/oauth/authorize",
                queryParams = params,
                requestOptions = requestOptions,
                responseSchema = runtimeSchema("{\"kind\":\"primitive\",\"type\":\"string\"}"),
                operationId = "getOAuthAuthorizationRequest",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(config, String::class.java)
    }

    @Suppress("UNCHECKED_CAST")
    fun getAuthorizationRequestWithResponse(
        responseType: String,
        clientId: String,
        redirectUri: String,
        state: String,
        codeChallenge: String,
        codeChallengeMethod: String,
        scope: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<String> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        getAuthorizationRequest(
            responseType,
            clientId,
            redirectUri,
            state,
            codeChallenge,
            codeChallengeMethod,
            scope,
            capturingOptions,
        )
        return captured as? RawResponse<String> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [getAuthorizationRequest]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [getAuthorizationRequest] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getAuthorizationRequestSuspend")
    suspend fun getAuthorizationRequestSuspend(
        responseType: String,
        clientId: String,
        redirectUri: String,
        state: String,
        codeChallenge: String,
        codeChallengeMethod: String,
        scope: String? = null,
        requestOptions: RequestOptions? = null,
    ): String =
        sellapp.baseClient.runCancellable {
            getAuthorizationRequest(
                responseType,
                clientId,
                redirectUri,
                state,
                codeChallenge,
                codeChallengeMethod,
                scope,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [getAuthorizationRequestWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [getAuthorizationRequestWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("getAuthorizationRequestWithResponseSuspend")
    suspend fun getAuthorizationRequestWithResponseSuspend(
        responseType: String,
        clientId: String,
        redirectUri: String,
        state: String,
        codeChallenge: String,
        codeChallengeMethod: String,
        scope: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<String> =
        sellapp.baseClient.runCancellable {
            getAuthorizationRequestWithResponse(
                responseType,
                clientId,
                redirectUri,
                state,
                codeChallenge,
                codeChallengeMethod,
                scope,
                requestOptions,
            )
        }

    /**
     * Approve CLI access
     *
     * Submit the official CLI consent form in the same logged-in browser session. The admin grant
     * covers every store you can access now or later, including when you currently have no stores.
     * It also permits store creation. Local agents connected through the CLI share this connection.
     * Approval never adds store role permissions. Current client eligibility, membership and
     * permissions are rechecked, including for stale consent.
     *
     * @param authToken the auth token of the request.
     * @param clientId the client id of the request.
     * @param state the state of the request.
     * @param token the token of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun approveAuthorization(
        authToken: String,
        clientId: String,
        state: String,
        token: String,
        requestOptions: RequestOptions? = null,
    ) {
        val body =
            bodyOf(
                "auth_token" to authToken,
                "client_id" to clientId,
                "state" to state,
                "_token" to token,
            )
        body["auth_token"] = authToken
        body["client_id"] = clientId
        body["state"] = state
        body["_token"] = token
        val config =
            RequestConfig(
                method = "POST",
                path = "/oauth/authorize",
                body = body,
                bodyEncoding = "form-urlencoded",
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models
                        .SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded
                        .runtimeSchema,
                responseSchema = runtimeSchema("{\"kind\":\"primitive\",\"type\":\"unknown\"}"),
                operationId = "approveOAuthAuthorization",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun approveAuthorizationWithResponse(
        authToken: String,
        clientId: String,
        state: String,
        token: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        approveAuthorization(authToken, clientId, state, token, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [approveAuthorization]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [approveAuthorization] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("approveAuthorizationSuspend")
    suspend fun approveAuthorizationSuspend(
        authToken: String,
        clientId: String,
        state: String,
        token: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            approveAuthorization(authToken, clientId, state, token, requestOptions)
        }

    /**
     * Coroutine-aware variant of [approveAuthorizationWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [approveAuthorizationWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("approveAuthorizationWithResponseSuspend")
    suspend fun approveAuthorizationWithResponseSuspend(
        authToken: String,
        clientId: String,
        state: String,
        token: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            approveAuthorizationWithResponse(authToken, clientId, state, token, requestOptions)
        }

    /**
     * Deny CLI access
     *
     * Submit the denial form from the same browser session. The callback receives access_denied and
     * the original state.
     *
     * @param authToken the auth token of the request.
     * @param token the token of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun denyAuthorization(
        authToken: String,
        token: String,
        requestOptions: RequestOptions? = null,
    ) {
        val body =
            bodyOf(
                "auth_token" to authToken,
                "_token" to token,
            )
        body["auth_token"] = authToken
        body["_token"] = token
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/oauth/authorize",
                body = body,
                bodyEncoding = "form-urlencoded",
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models
                        .SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded
                        .runtimeSchema,
                responseSchema = runtimeSchema("{\"kind\":\"primitive\",\"type\":\"unknown\"}"),
                operationId = "denyOAuthAuthorization",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun denyAuthorizationWithResponse(
        authToken: String,
        token: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        denyAuthorization(authToken, token, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [denyAuthorization]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [denyAuthorization] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("denyAuthorizationSuspend")
    suspend fun denyAuthorizationSuspend(
        authToken: String,
        token: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            denyAuthorization(authToken, token, requestOptions)
        }

    /**
     * Coroutine-aware variant of [denyAuthorizationWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [denyAuthorizationWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("denyAuthorizationWithResponseSuspend")
    suspend fun denyAuthorizationWithResponseSuspend(
        authToken: String,
        token: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            denyAuthorizationWithResponse(authToken, token, requestOptions)
        }

    /**
     * Exchange or refresh OAuth tokens
     *
     * Exchange an official CLI authorization code with the original redirect URI and PKCE verifier,
     * or rotate a refresh token. Send the configured client_id without a secret. Client eligibility
     * and current grant validity are rechecked. Access tokens last 60 minutes; refresh token
     * families last 90 days. Atomically replace rotated refresh tokens. Reuse may revoke the
     * family; reauthorize after invalid_grant. Fix registration or scope configuration for
     * invalid_client or invalid_scope instead of repeating login.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkExchangeOAuthTokenResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun exchangeToken(
        requestBody: Any,
        requestOptions: RequestOptions? = null,
    ): SdkExchangeOAuthTokenResponseValue200ApplicationJson {
        val body = requestBody
        val config =
            RequestConfig(
                method = "POST",
                path = "/oauth/token",
                body = body,
                bodyEncoding = "form-urlencoded",
                requestOptions = requestOptions,
                requestSchema =
                    runtimeSchema(
                        "{\"kind\":\"union\",\"variants\":[{\"kind\":\"object\",\"modelName\":\"ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue1\",\"properties\":{\"client_id\":{\"kind\":\"primitive\",\"type\":\"string\",\"format\":\"uuid\"},\"client_secret\":{\"kind\":\"primitive\",\"type\":\"string\"},\"grant_type\":{\"kind\":\"literal\",\"value\":\"authorization_code\"},\"code\":{\"kind\":\"primitive\",\"type\":\"string\"},\"redirect_uri\":{\"kind\":\"primitive\",\"type\":\"string\",\"format\":\"uri\"},\"code_verifier\":{\"kind\":\"primitive\",\"type\":\"string\",\"constraints\":{\"minLength\":43,\"maxLength\":128,\"pattern\":\"^[A-Za-z0-9._~-]+\$\"}}},\"required\":[\"grant_type\",\"code\",\"redirect_uri\",\"code_verifier\"],\"additionalProperties\":true},{\"kind\":\"object\",\"modelName\":\"ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue2\",\"properties\":{\"client_id\":{\"kind\":\"primitive\",\"type\":\"string\",\"format\":\"uuid\"},\"client_secret\":{\"kind\":\"primitive\",\"type\":\"string\"},\"grant_type\":{\"kind\":\"literal\",\"value\":\"refresh_token\"},\"refresh_token\":{\"kind\":\"primitive\",\"type\":\"string\"}},\"required\":[\"grant_type\",\"refresh_token\"],\"additionalProperties\":true}],\"composition\":\"oneOf\"}"
                    ),
                responseSchema =
                    app.sell.sellapp.models.SdkExchangeOAuthTokenResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "exchangeOAuthToken",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkExchangeOAuthTokenResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun exchangeTokenWithResponse(
        requestBody: Any,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkExchangeOAuthTokenResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        exchangeToken(requestBody, capturingOptions)
        return captured as? RawResponse<SdkExchangeOAuthTokenResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [exchangeToken]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [exchangeToken] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("exchangeTokenSuspend")
    suspend fun exchangeTokenSuspend(
        requestBody: Any,
        requestOptions: RequestOptions? = null,
    ): SdkExchangeOAuthTokenResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            exchangeToken(requestBody, requestOptions)
        }

    /**
     * Coroutine-aware variant of [exchangeTokenWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [exchangeTokenWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("exchangeTokenWithResponseSuspend")
    suspend fun exchangeTokenWithResponseSuspend(
        requestBody: Any,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkExchangeOAuthTokenResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            exchangeTokenWithResponse(requestBody, requestOptions)
        }

    /**
     * Revoke an OAuth token
     *
     * Revoke the matching personal CLI installation and all its token families. Send the configured
     * public client_id without a secret. Unknown tokens also succeed without disclosing whether a
     * token existed.
     *
     * @param token the token of the request.
     * @param clientId The configured official CLI public client ID. It identifies the registration,
     *   not the executable.
     * @param clientSecret Legacy SDK field retained for compatibility. Omit for the official CLI;
     *   it has no client secret.
     * @param tokenTypeHint the token type hint of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun revokeToken(
        token: String,
        clientId: String? = null,
        clientSecret: String? = null,
        tokenTypeHint: SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencodedTokenTypeHint? = null,
        requestOptions: RequestOptions? = null,
    ) {
        val body =
            bodyOf(
                "token" to token,
                "client_id" to clientId,
                "client_secret" to clientSecret,
                "token_type_hint" to tokenTypeHint,
            )
        body["token"] = token
        val config =
            RequestConfig(
                method = "POST",
                path = "/oauth/revoke",
                body = body,
                bodyEncoding = "form-urlencoded",
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded
                        .runtimeSchema,
                responseSchema =
                    runtimeSchema(
                        "{\"kind\":\"array\",\"items\":{\"kind\":\"primitive\",\"type\":\"unknown\"},\"constraints\":{\"maxItems\":0}}"
                    ),
                operationId = "revokeOAuthToken",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun revokeTokenWithResponse(
        token: String,
        clientId: String? = null,
        clientSecret: String? = null,
        tokenTypeHint: SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencodedTokenTypeHint? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        revokeToken(token, clientId, clientSecret, tokenTypeHint, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [revokeToken]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [revokeToken] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("revokeTokenSuspend")
    suspend fun revokeTokenSuspend(
        token: String,
        clientId: String? = null,
        clientSecret: String? = null,
        tokenTypeHint: SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencodedTokenTypeHint? = null,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            revokeToken(token, clientId, clientSecret, tokenTypeHint, requestOptions)
        }

    /**
     * Coroutine-aware variant of [revokeTokenWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [revokeTokenWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("revokeTokenWithResponseSuspend")
    suspend fun revokeTokenWithResponseSuspend(
        token: String,
        clientId: String? = null,
        clientSecret: String? = null,
        tokenTypeHint: SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencodedTokenTypeHint? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            revokeTokenWithResponse(token, clientId, clientSecret, tokenTypeHint, requestOptions)
        }
}
