// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.bookings

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.PatchField
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.http.patchBodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkCancelAppointmentResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkGetAppointmentResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkListAppointmentsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkSearchAppointmentsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkUpdateAppointmentResponseValue200ApplicationJson
import app.sell.sellapp.models.SearchAppointmentsRequestApplicationJsonPropertyFiltersItem
import app.sell.sellapp.models.SearchAppointmentsRequestApplicationJsonPropertyIncludesItem
import app.sell.sellapp.models.SearchAppointmentsRequestApplicationJsonPropertySearch
import app.sell.sellapp.models.SearchAppointmentsRequestApplicationJsonPropertySortItem
import com.fasterxml.jackson.core.type.TypeReference
import java.time.OffsetDateTime

/**
 * API accessor for Bookings.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class Bookings(internal val sellapp: SellApp) {
    /**
     * List appointments
     *
     * List appointments for the selected store. Requires the `invoice` Sanctum ability and invoice
     * permission. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param pagination Set to false to return at most 100 matching items in one data array without
     *   pagination links or metadata.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkListAppointmentsResponseValue200ApplicationJson> {
        val itemType =
            object : TypeReference<SdkListAppointmentsResponseValue200ApplicationJson>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/bookings",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listAppointments",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.SdkListAppointmentsResponseValue200ApplicationJson
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
            pagination?.let { add("pagination" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkListAppointmentsResponseValue200ApplicationJson>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(limit, page, pagination, capturingOptions)
        return captured as? RawResponse<Page<SdkListAppointmentsResponseValue200ApplicationJson>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkListAppointmentsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            list(limit, page, pagination, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkListAppointmentsResponseValue200ApplicationJson>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(limit, page, pagination, requestOptions)
        }

    /**
     * Search appointments
     *
     * Search appointment UUIDs or customer email addresses and compose store-scoped filters and
     * sorting. The public `id` filter targets the UUID returned as `id`; `uuid` remains an
     * equivalent compatibility filter. Requires the `invoice` Sanctum ability. OAuth callers use
     * the admin grant and select a store with X-STORE. Current membership and role permissions
     * apply to every request.
     *
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param pagination Set to false to return at most 100 matching items in one data array without
     *   pagination links or metadata.
     * @param filters the filters of the request.
     * @param sort the sort of the request.
     * @param search the search of the request.
     * @param includes the includes of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun search(
        limit: Long? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        filters: List<SearchAppointmentsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchAppointmentsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchAppointmentsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchAppointmentsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkSearchAppointmentsResponseValue200ApplicationJson> {
        val itemType =
            object : TypeReference<SdkSearchAppointmentsResponseValue200ApplicationJson>() {}
        return sellapp.baseClient.requestPage(
            method = "POST",
            path = "/v2/bookings/search",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "searchAppointments",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.SdkSearchAppointmentsResponseValue200ApplicationJson
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
            pagination?.let { add("pagination" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun searchWithResponse(
        limit: Long? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        filters: List<SearchAppointmentsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchAppointmentsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchAppointmentsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchAppointmentsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkSearchAppointmentsResponseValue200ApplicationJson>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        search(limit, page, pagination, filters, sort, search, includes, capturingOptions)
        return captured as? RawResponse<Page<SdkSearchAppointmentsResponseValue200ApplicationJson>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [search]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [search] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchSuspend")
    suspend fun searchSuspend(
        limit: Long? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        filters: List<SearchAppointmentsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchAppointmentsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchAppointmentsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchAppointmentsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkSearchAppointmentsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            search(limit, page, pagination, filters, sort, search, includes, requestOptions)
        }

    /**
     * Coroutine-aware variant of [searchWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [searchWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchWithResponseSuspend")
    suspend fun searchWithResponseSuspend(
        limit: Long? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        filters: List<SearchAppointmentsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchAppointmentsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchAppointmentsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchAppointmentsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkSearchAppointmentsResponseValue200ApplicationJson>> =
        sellapp.baseClient.runCancellable {
            searchWithResponse(
                limit,
                page,
                pagination,
                filters,
                sort,
                search,
                includes,
                requestOptions,
            )
        }

    /**
     * Retrieve an appointment
     *
     * Retrieve a store-scoped appointment by UUID or numeric ID. Provider error details are
     * intentionally not exposed. Requires the `invoice` Sanctum ability. OAuth callers use the
     * admin grant and select a store with X-STORE. Current membership and role permissions apply to
     * every request.
     *
     * @param booking The appointment UUID or numeric ID.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetAppointmentResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        booking: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetAppointmentResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/bookings/${encodePathSegment(booking)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetAppointmentResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getAppointment",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetAppointmentResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        booking: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetAppointmentResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(booking, capturingOptions)
        return captured as? RawResponse<SdkGetAppointmentResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        booking: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetAppointmentResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(booking, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        booking: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetAppointmentResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(booking, requestOptions)
        }

    /**
     * Update an appointment
     *
     * Reschedule an appointment atomically. Current availability is verified under the same
     * conflict lock used by checkout holds. Requires the `invoice` Sanctum ability. OAuth callers
     * use the admin grant and select a store with X-STORE. Current membership and role permissions
     * apply to every request.
     *
     * @param booking The appointment UUID or numeric ID.
     * @param slotStartAt A future slot start including an explicit UTC offset.
     * @param timezone IANA timezone used for appointment display and availability rules.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkUpdateAppointmentResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun update(
        booking: String,
        slotStartAt: OffsetDateTime,
        timezone: PatchField<String?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateAppointmentResponseValue200ApplicationJson {
        val body =
            patchBodyOf(
                "slot_start_at" to slotStartAt,
                "timezone" to timezone,
            )
        body["slot_start_at"] = slotStartAt
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v2/bookings/${encodePathSegment(booking)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkUpdateAppointmentRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkUpdateAppointmentResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "updateAppointment",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkUpdateAppointmentResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun updateWithResponse(
        booking: String,
        slotStartAt: OffsetDateTime,
        timezone: PatchField<String?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateAppointmentResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        update(booking, slotStartAt, timezone, capturingOptions)
        return captured as? RawResponse<SdkUpdateAppointmentResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [update]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [update] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateSuspend")
    suspend fun updateSuspend(
        booking: String,
        slotStartAt: OffsetDateTime,
        timezone: PatchField<String?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateAppointmentResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            update(booking, slotStartAt, timezone, requestOptions)
        }

    /**
     * Coroutine-aware variant of [updateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [updateWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateWithResponseSuspend")
    suspend fun updateWithResponseSuspend(
        booking: String,
        slotStartAt: OffsetDateTime,
        timezone: PatchField<String?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateAppointmentResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            updateWithResponse(booking, slotStartAt, timezone, requestOptions)
        }

    /**
     * Cancel an appointment
     *
     * Cancel a store-owned appointment. Cancellation is the only supported administrative status
     * transition. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param booking The appointment UUID or numeric ID.
     * @param status the status of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCancelAppointmentResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun cancel(
        booking: String,
        status: String,
        requestOptions: RequestOptions? = null,
    ): SdkCancelAppointmentResponseValue200ApplicationJson {
        val body = patchBodyOf("status" to status)
        body["status"] = status
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v2/bookings/${encodePathSegment(booking)}/status",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCancelAppointmentRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCancelAppointmentResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "cancelAppointment",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkCancelAppointmentResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun cancelWithResponse(
        booking: String,
        status: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCancelAppointmentResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        cancel(booking, status, capturingOptions)
        return captured as? RawResponse<SdkCancelAppointmentResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [cancel]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [cancel] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("cancelSuspend")
    suspend fun cancelSuspend(
        booking: String,
        status: String,
        requestOptions: RequestOptions? = null,
    ): SdkCancelAppointmentResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            cancel(booking, status, requestOptions)
        }

    /**
     * Coroutine-aware variant of [cancelWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [cancelWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("cancelWithResponseSuspend")
    suspend fun cancelWithResponseSuspend(
        booking: String,
        status: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCancelAppointmentResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            cancelWithResponse(booking, status, requestOptions)
        }
}
