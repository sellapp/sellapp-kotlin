// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.productvariantspricing

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.PatchField
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.http.patchBodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.ReplaceProductVariantPricingRequestApplicationJsonPropertyBulkDiscountItem
import app.sell.sellapp.models.ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing
import app.sell.sellapp.models.SdkReplaceProductVariantPricingResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkUpdateProductVariantPricingResponseValue200ApplicationJson
import app.sell.sellapp.models.UpdateProductVariantPricingRequestApplicationJsonPropertyBulkDiscountItem
import app.sell.sellapp.models.UpdateProductVariantPricingRequestApplicationJsonPropertyPricing
import app.sell.sellapp.types.PaymentMethod
import java.time.OffsetDateTime

/**
 * API accessor for ProductVariantsPricing.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class ProductVariantsPricing(internal val sellapp: SellApp) {
    /**
     * Replace product variant pricing
     *
     * Replace a variant's pricing and payment-method selection. Omitted `compare_at_price` and
     * `bulk_discount` values are cleared. An empty payment-method array is accepted only when the
     * resulting published variant is genuinely free, including having no paid credit-rate tiers.
     * Requires the `listing` Sanctum ability. Relationship IDs are scoped to the current store. Use
     * the ISO 8601 string `expected_updated_at` to reject stale writes. OAuth callers use the admin
     * grant and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param product The product path parameter.
     * @param variant The variant path parameter.
     * @param pricing the pricing of the request.
     * @param paymentMethods Complete payment-method selection. May be empty only when the resulting
     *   published variant is genuinely free, including having no paid credit-rate tiers.
     * @param customPaymentMethodIds Enabled custom payment method ULIDs owned by the current store.
     *   Required when CUSTOM_PAYMENT_METHOD is selected.
     * @param bulkDiscount Quantity pricing tiers for one-time variants. Subscription variants
     *   cannot use these tiers.
     * @param expectedUpdatedAt Optional optimistic-concurrency snapshot. The token advances for
     *   pricing and custom payment-method selection changes; a stale value returns 422 without
     *   changing either selection.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceProductVariantPricingResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        product: String,
        variant: String,
        pricing: ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing,
        paymentMethods: List<PaymentMethod>,
        customPaymentMethodIds: List<String>? = null,
        bulkDiscount:
            List<ReplaceProductVariantPricingRequestApplicationJsonPropertyBulkDiscountItem>? =
            null,
        expectedUpdatedAt: OffsetDateTime? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceProductVariantPricingResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "pricing" to pricing,
                "payment_methods" to paymentMethods,
                "custom_payment_method_ids" to customPaymentMethodIds,
                "bulk_discount" to bulkDiscount,
                "expected_updated_at" to expectedUpdatedAt,
            )
        body["pricing"] = pricing
        body["payment_methods"] = paymentMethods
        val config =
            RequestConfig(
                method = "PUT",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/pricing",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkReplaceProductVariantPricingRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkReplaceProductVariantPricingResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceProductVariantPricing",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceProductVariantPricingResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        product: String,
        variant: String,
        pricing: ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing,
        paymentMethods: List<PaymentMethod>,
        customPaymentMethodIds: List<String>? = null,
        bulkDiscount:
            List<ReplaceProductVariantPricingRequestApplicationJsonPropertyBulkDiscountItem>? =
            null,
        expectedUpdatedAt: OffsetDateTime? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceProductVariantPricingResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(
            product,
            variant,
            pricing,
            paymentMethods,
            customPaymentMethodIds,
            bulkDiscount,
            expectedUpdatedAt,
            capturingOptions,
        )
        return captured
            as? RawResponse<SdkReplaceProductVariantPricingResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        product: String,
        variant: String,
        pricing: ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing,
        paymentMethods: List<PaymentMethod>,
        customPaymentMethodIds: List<String>? = null,
        bulkDiscount:
            List<ReplaceProductVariantPricingRequestApplicationJsonPropertyBulkDiscountItem>? =
            null,
        expectedUpdatedAt: OffsetDateTime? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceProductVariantPricingResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(
                product,
                variant,
                pricing,
                paymentMethods,
                customPaymentMethodIds,
                bulkDiscount,
                expectedUpdatedAt,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        product: String,
        variant: String,
        pricing: ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing,
        paymentMethods: List<PaymentMethod>,
        customPaymentMethodIds: List<String>? = null,
        bulkDiscount:
            List<ReplaceProductVariantPricingRequestApplicationJsonPropertyBulkDiscountItem>? =
            null,
        expectedUpdatedAt: OffsetDateTime? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceProductVariantPricingResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(
                product,
                variant,
                pricing,
                paymentMethods,
                customPaymentMethodIds,
                bulkDiscount,
                expectedUpdatedAt,
                requestOptions,
            )
        }

    /**
     * Partially update product variant pricing
     *
     * Update only the supplied pricing, subscription cadence, quantity pricing tier, or
     * payment-method fields; omitted fields retain their current values. An empty payment-method
     * array is accepted only when the resulting published variant is genuinely free, including
     * having no paid credit-rate tiers. Requires the `listing` Sanctum ability. Relationship IDs
     * are scoped to the current store. Use the ISO 8601 string `expected_updated_at` to reject
     * stale writes. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param product The product path parameter.
     * @param variant The variant path parameter.
     * @param pricing the pricing of the request.
     * @param paymentMethods Complete payment-method selection. May be empty only when the resulting
     *   published variant is genuinely free, including having no paid credit-rate tiers.
     * @param customPaymentMethodIds Enabled custom payment method ULIDs owned by the current store.
     *   Required when CUSTOM_PAYMENT_METHOD is selected.
     * @param bulkDiscount Quantity pricing tiers for one-time variants. Subscription variants
     *   cannot use these tiers.
     * @param expectedUpdatedAt Optional optimistic-concurrency snapshot. The token advances for
     *   pricing and custom payment-method selection changes; a stale value returns 422 without
     *   changing either selection.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkUpdateProductVariantPricingResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun update(
        product: String,
        variant: String,
        pricing: PatchField<UpdateProductVariantPricingRequestApplicationJsonPropertyPricing> =
            PatchField.Absent,
        paymentMethods: PatchField<List<PaymentMethod>> = PatchField.Absent,
        customPaymentMethodIds: PatchField<List<String>> = PatchField.Absent,
        bulkDiscount:
            PatchField<
                List<UpdateProductVariantPricingRequestApplicationJsonPropertyBulkDiscountItem>
            > =
            PatchField.Absent,
        expectedUpdatedAt: PatchField<OffsetDateTime> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateProductVariantPricingResponseValue200ApplicationJson {
        val body =
            patchBodyOf(
                "pricing" to pricing,
                "payment_methods" to paymentMethods,
                "custom_payment_method_ids" to customPaymentMethodIds,
                "bulk_discount" to bulkDiscount,
                "expected_updated_at" to expectedUpdatedAt,
            )
        val config =
            RequestConfig(
                method = "PATCH",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/pricing",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkUpdateProductVariantPricingRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkUpdateProductVariantPricingResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "updateProductVariantPricing",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkUpdateProductVariantPricingResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun updateWithResponse(
        product: String,
        variant: String,
        pricing: PatchField<UpdateProductVariantPricingRequestApplicationJsonPropertyPricing> =
            PatchField.Absent,
        paymentMethods: PatchField<List<PaymentMethod>> = PatchField.Absent,
        customPaymentMethodIds: PatchField<List<String>> = PatchField.Absent,
        bulkDiscount:
            PatchField<
                List<UpdateProductVariantPricingRequestApplicationJsonPropertyBulkDiscountItem>
            > =
            PatchField.Absent,
        expectedUpdatedAt: PatchField<OffsetDateTime> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateProductVariantPricingResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        update(
            product,
            variant,
            pricing,
            paymentMethods,
            customPaymentMethodIds,
            bulkDiscount,
            expectedUpdatedAt,
            capturingOptions,
        )
        return captured
            as? RawResponse<SdkUpdateProductVariantPricingResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [update]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [update] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateSuspend")
    suspend fun updateSuspend(
        product: String,
        variant: String,
        pricing: PatchField<UpdateProductVariantPricingRequestApplicationJsonPropertyPricing> =
            PatchField.Absent,
        paymentMethods: PatchField<List<PaymentMethod>> = PatchField.Absent,
        customPaymentMethodIds: PatchField<List<String>> = PatchField.Absent,
        bulkDiscount:
            PatchField<
                List<UpdateProductVariantPricingRequestApplicationJsonPropertyBulkDiscountItem>
            > =
            PatchField.Absent,
        expectedUpdatedAt: PatchField<OffsetDateTime> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateProductVariantPricingResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            update(
                product,
                variant,
                pricing,
                paymentMethods,
                customPaymentMethodIds,
                bulkDiscount,
                expectedUpdatedAt,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [updateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [updateWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateWithResponseSuspend")
    suspend fun updateWithResponseSuspend(
        product: String,
        variant: String,
        pricing: PatchField<UpdateProductVariantPricingRequestApplicationJsonPropertyPricing> =
            PatchField.Absent,
        paymentMethods: PatchField<List<PaymentMethod>> = PatchField.Absent,
        customPaymentMethodIds: PatchField<List<String>> = PatchField.Absent,
        bulkDiscount:
            PatchField<
                List<UpdateProductVariantPricingRequestApplicationJsonPropertyBulkDiscountItem>
            > =
            PatchField.Absent,
        expectedUpdatedAt: PatchField<OffsetDateTime> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateProductVariantPricingResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            updateWithResponse(
                product,
                variant,
                pricing,
                paymentMethods,
                customPaymentMethodIds,
                bulkDiscount,
                expectedUpdatedAt,
                requestOptions,
            )
        }
}
