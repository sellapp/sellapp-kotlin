// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.coupons

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.PatchField
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.http.patchBodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem
import app.sell.sellapp.models.BatchUpdateCouponsRequestApplicationJsonPropertyResources
import app.sell.sellapp.models.ListCouponsResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.SdkBatchCreateCouponsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkBatchUpdateCouponsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkCreateCouponResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkGetCouponResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkReplaceCouponResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkUpdateCouponResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkV2BatchCreateCouponsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkV2BatchUpdateCouponsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkV2CreateCouponResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkV2GetCouponResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkV2ReplaceCouponResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkV2UpdateCouponResponseValue200ApplicationJson
import app.sell.sellapp.models.SearchCouponsRequestApplicationJsonPropertyFiltersItem
import app.sell.sellapp.models.SearchCouponsRequestApplicationJsonPropertyIncludesItem
import app.sell.sellapp.models.SearchCouponsRequestApplicationJsonPropertySearch
import app.sell.sellapp.models.SearchCouponsRequestApplicationJsonPropertySortItem
import app.sell.sellapp.models.SearchCouponsResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.V2BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem
import app.sell.sellapp.models.V2BatchUpdateCouponsRequestApplicationJsonPropertyResources
import app.sell.sellapp.models.V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.V2SearchCouponsRequestApplicationJsonPropertyFiltersItem
import app.sell.sellapp.models.V2SearchCouponsRequestApplicationJsonPropertyIncludesItem
import app.sell.sellapp.models.V2SearchCouponsRequestApplicationJsonPropertySearch
import app.sell.sellapp.models.V2SearchCouponsRequestApplicationJsonPropertySortItem
import app.sell.sellapp.models.V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.types.SdkCreateCouponRequestApplicationJsonType
import com.fasterxml.jackson.core.type.TypeReference
import java.time.OffsetDateTime

/**
 * API accessor for Coupons.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class Coupons(internal val sellapp: SellApp) {
    /**
     * List all coupons
     *
     * List your store's coupons, 15 per page by default.
     *
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        limit: Int? = null,
        page: Long? = null,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListCouponsResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object : TypeReference<ListCouponsResponseValue200ApplicationJsonPropertyDataItem>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v1/coupons",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listCoupons",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.ListCouponsResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
            withTrashed?.let { add("with_trashed" to it.toString()) }
            onlyTrashed?.let { add("only_trashed" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        limit: Int? = null,
        page: Long? = null,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListCouponsResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(limit, page, withTrashed, onlyTrashed, capturingOptions)
        return captured
            as? RawResponse<Page<ListCouponsResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        limit: Int? = null,
        page: Long? = null,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListCouponsResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            list(limit, page, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        limit: Int? = null,
        page: Long? = null,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListCouponsResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(limit, page, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Create a coupon
     *
     * Create a discount code for your store.
     *
     * @param code the code of the request.
     * @param type the type of the request.
     * @param discount A decimal discount value of at least 1. Percentage discounts cannot exceed
     *   100; amount discounts use the store currency's major unit.
     * @param storeWide the store wide of the request.
     * @param products Product IDs the coupon applies to when store_wide is false.
     * @param productVariants Optional variant restrictions. Every variant must belong to a selected
     *   product. Products without listed variants remain eligible on all variants.
     * @param limit the limit of the request.
     * @param expiresAt A future date and time, or null for no expiry.
     * @param minimumAmount A decimal minimum order amount in the store currency's major unit, or
     *   null for no minimum.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateCouponResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun create(
        code: String,
        type: SdkCreateCouponRequestApplicationJsonType,
        discount: Any,
        storeWide: Boolean,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateCouponResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "code" to code,
                "type" to type,
                "discount" to discount,
                "store_wide" to storeWide,
                "products" to products,
                "product_variants" to productVariants,
                "limit" to limit,
                "expires_at" to expiresAt,
                "minimum_amount" to minimumAmount,
            )
        body["code"] = code
        body["type"] = type
        body["discount"] = discount
        body["store_wide"] = storeWide
        val config =
            RequestConfig(
                method = "POST",
                path = "/v1/coupons",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateCouponRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateCouponResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "createCoupon",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateCouponResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createWithResponse(
        code: String,
        type: SdkCreateCouponRequestApplicationJsonType,
        discount: Any,
        storeWide: Boolean,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateCouponResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        create(
            code,
            type,
            discount,
            storeWide,
            products,
            productVariants,
            limit,
            expiresAt,
            minimumAmount,
            capturingOptions,
        )
        return captured as? RawResponse<SdkCreateCouponResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [create]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [create] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createSuspend")
    suspend fun createSuspend(
        code: String,
        type: SdkCreateCouponRequestApplicationJsonType,
        discount: Any,
        storeWide: Boolean,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateCouponResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            create(
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [createWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createWithResponseSuspend")
    suspend fun createWithResponseSuspend(
        code: String,
        type: SdkCreateCouponRequestApplicationJsonType,
        discount: Any,
        storeWide: Boolean,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateCouponResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createWithResponse(
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Retrieve a coupon
     *
     * Retrieve a coupon by its ID.
     *
     * @param coupon The coupon path parameter.
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetCouponResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): SdkGetCouponResponseValue200ApplicationJson {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val config =
            RequestConfig(
                method = "GET",
                path = "/v1/coupons/${encodePathSegment(coupon)}",
                queryParams = params,
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetCouponResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getCoupon",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetCouponResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetCouponResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(coupon, withTrashed, onlyTrashed, capturingOptions)
        return captured as? RawResponse<SdkGetCouponResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): SdkGetCouponResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(coupon, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetCouponResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(coupon, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Update a coupon
     *
     * Change a coupon's discount, limits, or eligible products.
     *
     * @param coupon The coupon path parameter.
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param code the code of the request.
     * @param type the type of the request.
     * @param discount A decimal discount value of at least 1. Percentage discounts cannot exceed
     *   100; amount discounts use the store currency's major unit.
     * @param storeWide the store wide of the request.
     * @param products Product IDs the coupon applies to when store_wide is false.
     * @param productVariants Optional variant restrictions. Omit to preserve existing restrictions
     *   or send an empty array to allow every variant of the selected products.
     * @param limit the limit of the request.
     * @param expiresAt A future date and time, or null for no expiry.
     * @param minimumAmount A decimal minimum order amount in the store currency's major unit, or
     *   null for no minimum.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceCouponResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: String? = null,
        type: SdkCreateCouponRequestApplicationJsonType? = null,
        discount: Any? = null,
        storeWide: Boolean? = null,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceCouponResponseValue200ApplicationJson {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val body =
            bodyOf(
                "code" to code,
                "type" to type,
                "discount" to discount,
                "store_wide" to storeWide,
                "products" to products,
                "product_variants" to productVariants,
                "limit" to limit,
                "expires_at" to expiresAt,
                "minimum_amount" to minimumAmount,
            )
        val config =
            RequestConfig(
                method = "PUT",
                path = "/v1/coupons/${encodePathSegment(coupon)}",
                queryParams = params,
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkReplaceCouponRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkReplaceCouponResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceCoupon",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceCouponResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: String? = null,
        type: SdkCreateCouponRequestApplicationJsonType? = null,
        discount: Any? = null,
        storeWide: Boolean? = null,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceCouponResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(
            coupon,
            withTrashed,
            onlyTrashed,
            code,
            type,
            discount,
            storeWide,
            products,
            productVariants,
            limit,
            expiresAt,
            minimumAmount,
            capturingOptions,
        )
        return captured as? RawResponse<SdkReplaceCouponResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: String? = null,
        type: SdkCreateCouponRequestApplicationJsonType? = null,
        discount: Any? = null,
        storeWide: Boolean? = null,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceCouponResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(
                coupon,
                withTrashed,
                onlyTrashed,
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: String? = null,
        type: SdkCreateCouponRequestApplicationJsonType? = null,
        discount: Any? = null,
        storeWide: Boolean? = null,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceCouponResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(
                coupon,
                withTrashed,
                onlyTrashed,
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Update a coupon
     *
     * Change a coupon's discount, limits, or eligible products.
     *
     * @param coupon The coupon path parameter.
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param code the code of the request.
     * @param type the type of the request.
     * @param discount A decimal discount value of at least 1. Percentage discounts cannot exceed
     *   100; amount discounts use the store currency's major unit.
     * @param storeWide the store wide of the request.
     * @param products Product IDs the coupon applies to when store_wide is false.
     * @param productVariants Optional variant restrictions. Omit to preserve existing restrictions
     *   or send an empty array to allow every variant of the selected products.
     * @param limit the limit of the request.
     * @param expiresAt A future date and time, or null for no expiry.
     * @param minimumAmount A decimal minimum order amount in the store currency's major unit, or
     *   null for no minimum.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkUpdateCouponResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun update(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: PatchField<String> = PatchField.Absent,
        type: PatchField<SdkCreateCouponRequestApplicationJsonType> = PatchField.Absent,
        discount: PatchField<Any> = PatchField.Absent,
        storeWide: PatchField<Boolean> = PatchField.Absent,
        products: PatchField<List<Long>> = PatchField.Absent,
        productVariants: PatchField<List<Long>> = PatchField.Absent,
        limit: PatchField<Long?> = PatchField.Absent,
        expiresAt: PatchField<OffsetDateTime?> = PatchField.Absent,
        minimumAmount: PatchField<Any?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateCouponResponseValue200ApplicationJson {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val body =
            patchBodyOf(
                "code" to code,
                "type" to type,
                "discount" to discount,
                "store_wide" to storeWide,
                "products" to products,
                "product_variants" to productVariants,
                "limit" to limit,
                "expires_at" to expiresAt,
                "minimum_amount" to minimumAmount,
            )
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v1/coupons/${encodePathSegment(coupon)}",
                queryParams = params,
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkUpdateCouponRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkUpdateCouponResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "updateCoupon",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkUpdateCouponResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun updateWithResponse(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: PatchField<String> = PatchField.Absent,
        type: PatchField<SdkCreateCouponRequestApplicationJsonType> = PatchField.Absent,
        discount: PatchField<Any> = PatchField.Absent,
        storeWide: PatchField<Boolean> = PatchField.Absent,
        products: PatchField<List<Long>> = PatchField.Absent,
        productVariants: PatchField<List<Long>> = PatchField.Absent,
        limit: PatchField<Long?> = PatchField.Absent,
        expiresAt: PatchField<OffsetDateTime?> = PatchField.Absent,
        minimumAmount: PatchField<Any?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateCouponResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        update(
            coupon,
            withTrashed,
            onlyTrashed,
            code,
            type,
            discount,
            storeWide,
            products,
            productVariants,
            limit,
            expiresAt,
            minimumAmount,
            capturingOptions,
        )
        return captured as? RawResponse<SdkUpdateCouponResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [update]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [update] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateSuspend")
    suspend fun updateSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: PatchField<String> = PatchField.Absent,
        type: PatchField<SdkCreateCouponRequestApplicationJsonType> = PatchField.Absent,
        discount: PatchField<Any> = PatchField.Absent,
        storeWide: PatchField<Boolean> = PatchField.Absent,
        products: PatchField<List<Long>> = PatchField.Absent,
        productVariants: PatchField<List<Long>> = PatchField.Absent,
        limit: PatchField<Long?> = PatchField.Absent,
        expiresAt: PatchField<OffsetDateTime?> = PatchField.Absent,
        minimumAmount: PatchField<Any?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateCouponResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            update(
                coupon,
                withTrashed,
                onlyTrashed,
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [updateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [updateWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateWithResponseSuspend")
    suspend fun updateWithResponseSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: PatchField<String> = PatchField.Absent,
        type: PatchField<SdkCreateCouponRequestApplicationJsonType> = PatchField.Absent,
        discount: PatchField<Any> = PatchField.Absent,
        storeWide: PatchField<Boolean> = PatchField.Absent,
        products: PatchField<List<Long>> = PatchField.Absent,
        productVariants: PatchField<List<Long>> = PatchField.Absent,
        limit: PatchField<Long?> = PatchField.Absent,
        expiresAt: PatchField<OffsetDateTime?> = PatchField.Absent,
        minimumAmount: PatchField<Any?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateCouponResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            updateWithResponse(
                coupon,
                withTrashed,
                onlyTrashed,
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Delete a coupon
     *
     * Soft-delete a coupon. The returned coupon includes its deleted_at timestamp; use with_trashed
     * or only_trashed to retrieve soft-deleted coupons later.
     *
     * @param coupon The coupon path parameter.
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun delete(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ) {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v1/coupons/${encodePathSegment(coupon)}",
                queryParams = params,
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkDeleteCouponResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "deleteCoupon",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun deleteWithResponse(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        delete(coupon, withTrashed, onlyTrashed, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [delete]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [delete] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteSuspend")
    suspend fun deleteSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            delete(coupon, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Coroutine-aware variant of [deleteWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [deleteWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteWithResponseSuspend")
    suspend fun deleteWithResponseSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            deleteWithResponse(coupon, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Search coupons
     *
     * Search coupons using JSON body filters, search terms, includes, and sort instructions.
     *
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param filters the filters of the request.
     * @param sort the sort of the request.
     * @param search the search of the request.
     * @param includes the includes of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun search(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        filters: List<SearchCouponsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchCouponsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchCouponsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchCouponsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SearchCouponsResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<SearchCouponsResponseValue200ApplicationJsonPropertyDataItem>() {}
        return sellapp.baseClient.requestPage(
            method = "POST",
            path = "/v1/coupons/search",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "searchCoupons",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.SearchCouponsResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            withTrashed?.let { add("with_trashed" to it.toString()) }
            onlyTrashed?.let { add("only_trashed" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun searchWithResponse(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        filters: List<SearchCouponsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchCouponsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchCouponsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchCouponsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SearchCouponsResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        search(withTrashed, onlyTrashed, filters, sort, search, includes, capturingOptions)
        return captured
            as? RawResponse<Page<SearchCouponsResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [search]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [search] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchSuspend")
    suspend fun searchSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        filters: List<SearchCouponsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchCouponsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchCouponsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchCouponsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SearchCouponsResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            search(withTrashed, onlyTrashed, filters, sort, search, includes, requestOptions)
        }

    /**
     * Coroutine-aware variant of [searchWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [searchWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchWithResponseSuspend")
    suspend fun searchWithResponseSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        filters: List<SearchCouponsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchCouponsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchCouponsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchCouponsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SearchCouponsResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            searchWithResponse(
                withTrashed,
                onlyTrashed,
                filters,
                sort,
                search,
                includes,
                requestOptions,
            )
        }

    /**
     * Batch create coupons
     *
     * Create multiple coupons in one request by sending a `resources` array.
     *
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkBatchCreateCouponsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun batchCreate(
        resources: List<BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem>,
        requestOptions: RequestOptions? = null,
    ): SdkBatchCreateCouponsResponseValue200ApplicationJson {
        val body = bodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "POST",
                path = "/v1/coupons/batch",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkBatchCreateCouponsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkBatchCreateCouponsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "batchCreateCoupons",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkBatchCreateCouponsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun batchCreateWithResponse(
        resources: List<BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkBatchCreateCouponsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        batchCreate(resources, capturingOptions)
        return captured as? RawResponse<SdkBatchCreateCouponsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [batchCreate]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [batchCreate] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("batchCreateSuspend")
    suspend fun batchCreateSuspend(
        resources: List<BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem>,
        requestOptions: RequestOptions? = null,
    ): SdkBatchCreateCouponsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            batchCreate(resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [batchCreateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [batchCreateWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("batchCreateWithResponseSuspend")
    suspend fun batchCreateWithResponseSuspend(
        resources: List<BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkBatchCreateCouponsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            batchCreateWithResponse(resources, requestOptions)
        }

    /**
     * Batch update coupons
     *
     * Update multiple coupons in one request by sending a `resources` object keyed by coupon ID.
     *
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkBatchUpdateCouponsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun batchUpdate(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): SdkBatchUpdateCouponsResponseValue200ApplicationJson {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val body = patchBodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v1/coupons/batch",
                queryParams = params,
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkBatchUpdateCouponsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkBatchUpdateCouponsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "batchUpdateCoupons",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkBatchUpdateCouponsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun batchUpdateWithResponse(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkBatchUpdateCouponsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        batchUpdate(withTrashed, onlyTrashed, resources, capturingOptions)
        return captured as? RawResponse<SdkBatchUpdateCouponsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [batchUpdate]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [batchUpdate] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("batchUpdateSuspend")
    suspend fun batchUpdateSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): SdkBatchUpdateCouponsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            batchUpdate(withTrashed, onlyTrashed, resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [batchUpdateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [batchUpdateWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("batchUpdateWithResponseSuspend")
    suspend fun batchUpdateWithResponseSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkBatchUpdateCouponsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            batchUpdateWithResponse(withTrashed, onlyTrashed, resources, requestOptions)
        }

    /**
     * Batch delete coupons
     *
     * Soft-delete multiple coupons by sending their IDs in `resources`.
     *
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun batchDelete(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ) {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val body = bodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v1/coupons/batch",
                queryParams = params,
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkBatchDeleteCouponsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkBatchDeleteCouponsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "batchDeleteCoupons",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun batchDeleteWithResponse(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        batchDelete(withTrashed, onlyTrashed, resources, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [batchDelete]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [batchDelete] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("batchDeleteSuspend")
    suspend fun batchDeleteSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            batchDelete(withTrashed, onlyTrashed, resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [batchDeleteWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [batchDeleteWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("batchDeleteWithResponseSuspend")
    suspend fun batchDeleteWithResponseSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            batchDeleteWithResponse(withTrashed, onlyTrashed, resources, requestOptions)
        }

    /**
     * List all coupons
     *
     * List your store's coupons, 15 per page by default. This v2 route is store-scoped. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun v2ListCoupons(
        limit: Long? = null,
        page: Long? = null,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/coupons",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "v2ListCoupons",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
            withTrashed?.let { add("with_trashed" to it.toString()) }
            onlyTrashed?.let { add("only_trashed" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun v2ListCouponsWithResponse(
        limit: Long? = null,
        page: Long? = null,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2ListCoupons(limit, page, withTrashed, onlyTrashed, capturingOptions)
        return captured
            as? RawResponse<Page<V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2ListCoupons]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [v2ListCoupons] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2ListCouponsSuspend")
    suspend fun v2ListCouponsSuspend(
        limit: Long? = null,
        page: Long? = null,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            v2ListCoupons(limit, page, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2ListCouponsWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [v2ListCouponsWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2ListCouponsWithResponseSuspend")
    suspend fun v2ListCouponsWithResponseSuspend(
        limit: Long? = null,
        page: Long? = null,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            v2ListCouponsWithResponse(limit, page, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Create a coupon
     *
     * Create a discount code for your store. This v2 route is store-scoped. OAuth callers use the
     * admin grant and select a store with X-STORE. Current membership and role permissions apply to
     * every request.
     *
     * @param code the code of the request.
     * @param type the type of the request.
     * @param discount A decimal discount value of at least 1. Percentage discounts cannot exceed
     *   100; amount discounts use the store currency's major unit.
     * @param storeWide the store wide of the request.
     * @param products Product IDs the coupon applies to when store_wide is false.
     * @param productVariants Optional variant restrictions. Every variant must belong to a selected
     *   product. Products without listed variants remain eligible on all variants.
     * @param limit the limit of the request.
     * @param expiresAt A future date and time, or null for no expiry.
     * @param minimumAmount A decimal minimum order amount in the store currency's major unit, or
     *   null for no minimum.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkV2CreateCouponResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun v2CreateCoupon(
        code: String,
        type: SdkCreateCouponRequestApplicationJsonType,
        discount: Any,
        storeWide: Boolean,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): SdkV2CreateCouponResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "code" to code,
                "type" to type,
                "discount" to discount,
                "store_wide" to storeWide,
                "products" to products,
                "product_variants" to productVariants,
                "limit" to limit,
                "expires_at" to expiresAt,
                "minimum_amount" to minimumAmount,
            )
        body["code"] = code
        body["type"] = type
        body["discount"] = discount
        body["store_wide"] = storeWide
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/coupons",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkV2CreateCouponRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkV2CreateCouponResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "v2CreateCoupon",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkV2CreateCouponResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun v2CreateCouponWithResponse(
        code: String,
        type: SdkCreateCouponRequestApplicationJsonType,
        discount: Any,
        storeWide: Boolean,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2CreateCouponResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2CreateCoupon(
            code,
            type,
            discount,
            storeWide,
            products,
            productVariants,
            limit,
            expiresAt,
            minimumAmount,
            capturingOptions,
        )
        return captured as? RawResponse<SdkV2CreateCouponResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2CreateCoupon]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [v2CreateCoupon] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2CreateCouponSuspend")
    suspend fun v2CreateCouponSuspend(
        code: String,
        type: SdkCreateCouponRequestApplicationJsonType,
        discount: Any,
        storeWide: Boolean,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): SdkV2CreateCouponResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            v2CreateCoupon(
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [v2CreateCouponWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [v2CreateCouponWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2CreateCouponWithResponseSuspend")
    suspend fun v2CreateCouponWithResponseSuspend(
        code: String,
        type: SdkCreateCouponRequestApplicationJsonType,
        discount: Any,
        storeWide: Boolean,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2CreateCouponResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            v2CreateCouponWithResponse(
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Search coupons
     *
     * Search coupons using JSON body filters, search terms, includes, and sort instructions. This
     * v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param filters the filters of the request.
     * @param sort the sort of the request.
     * @param search the search of the request.
     * @param includes the includes of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun v2SearchCoupons(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        filters: List<V2SearchCouponsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<V2SearchCouponsRequestApplicationJsonPropertySortItem>? = null,
        search: V2SearchCouponsRequestApplicationJsonPropertySearch? = null,
        includes: List<V2SearchCouponsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem>() {}
        return sellapp.baseClient.requestPage(
            method = "POST",
            path = "/v2/coupons/search",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "v2SearchCoupons",
            supportsIdempotency = true,
            itemSchema =
                app.sell.sellapp.models
                    .V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            withTrashed?.let { add("with_trashed" to it.toString()) }
            onlyTrashed?.let { add("only_trashed" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun v2SearchCouponsWithResponse(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        filters: List<V2SearchCouponsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<V2SearchCouponsRequestApplicationJsonPropertySortItem>? = null,
        search: V2SearchCouponsRequestApplicationJsonPropertySearch? = null,
        includes: List<V2SearchCouponsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2SearchCoupons(withTrashed, onlyTrashed, filters, sort, search, includes, capturingOptions)
        return captured
            as? RawResponse<Page<V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2SearchCoupons]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [v2SearchCoupons] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2SearchCouponsSuspend")
    suspend fun v2SearchCouponsSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        filters: List<V2SearchCouponsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<V2SearchCouponsRequestApplicationJsonPropertySortItem>? = null,
        search: V2SearchCouponsRequestApplicationJsonPropertySearch? = null,
        includes: List<V2SearchCouponsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            v2SearchCoupons(
                withTrashed,
                onlyTrashed,
                filters,
                sort,
                search,
                includes,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [v2SearchCouponsWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [v2SearchCouponsWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2SearchCouponsWithResponseSuspend")
    suspend fun v2SearchCouponsWithResponseSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        filters: List<V2SearchCouponsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<V2SearchCouponsRequestApplicationJsonPropertySortItem>? = null,
        search: V2SearchCouponsRequestApplicationJsonPropertySearch? = null,
        includes: List<V2SearchCouponsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            v2SearchCouponsWithResponse(
                withTrashed,
                onlyTrashed,
                filters,
                sort,
                search,
                includes,
                requestOptions,
            )
        }

    /**
     * Retrieve a coupon
     *
     * Retrieve a coupon by its ID. This v2 route is store-scoped. OAuth callers use the admin grant
     * and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param coupon The coupon path parameter.
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkV2GetCouponResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun v2GetCoupon(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): SdkV2GetCouponResponseValue200ApplicationJson {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/coupons/${encodePathSegment(coupon)}",
                queryParams = params,
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkV2GetCouponResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "v2GetCoupon",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkV2GetCouponResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun v2GetCouponWithResponse(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2GetCouponResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2GetCoupon(coupon, withTrashed, onlyTrashed, capturingOptions)
        return captured as? RawResponse<SdkV2GetCouponResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2GetCoupon]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [v2GetCoupon] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2GetCouponSuspend")
    suspend fun v2GetCouponSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): SdkV2GetCouponResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            v2GetCoupon(coupon, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2GetCouponWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [v2GetCouponWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2GetCouponWithResponseSuspend")
    suspend fun v2GetCouponWithResponseSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2GetCouponResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            v2GetCouponWithResponse(coupon, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Update a coupon
     *
     * Change a coupon's discount, limits, or eligible products. This v2 route is store-scoped.
     * OAuth callers use the admin grant and select a store with X-STORE. Current membership and
     * role permissions apply to every request.
     *
     * @param coupon The coupon path parameter.
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param code the code of the request.
     * @param type the type of the request.
     * @param discount A decimal discount value of at least 1. Percentage discounts cannot exceed
     *   100; amount discounts use the store currency's major unit.
     * @param storeWide the store wide of the request.
     * @param products Product IDs the coupon applies to when store_wide is false.
     * @param productVariants Optional variant restrictions. Omit to preserve existing restrictions
     *   or send an empty array to allow every variant of the selected products.
     * @param limit the limit of the request.
     * @param expiresAt A future date and time, or null for no expiry.
     * @param minimumAmount A decimal minimum order amount in the store currency's major unit, or
     *   null for no minimum.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkV2ReplaceCouponResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun v2ReplaceCoupon(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: String? = null,
        type: SdkCreateCouponRequestApplicationJsonType? = null,
        discount: Any? = null,
        storeWide: Boolean? = null,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): SdkV2ReplaceCouponResponseValue200ApplicationJson {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val body =
            bodyOf(
                "code" to code,
                "type" to type,
                "discount" to discount,
                "store_wide" to storeWide,
                "products" to products,
                "product_variants" to productVariants,
                "limit" to limit,
                "expires_at" to expiresAt,
                "minimum_amount" to minimumAmount,
            )
        val config =
            RequestConfig(
                method = "PUT",
                path = "/v2/coupons/${encodePathSegment(coupon)}",
                queryParams = params,
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkV2ReplaceCouponRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkV2ReplaceCouponResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "v2ReplaceCoupon",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkV2ReplaceCouponResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun v2ReplaceCouponWithResponse(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: String? = null,
        type: SdkCreateCouponRequestApplicationJsonType? = null,
        discount: Any? = null,
        storeWide: Boolean? = null,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2ReplaceCouponResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2ReplaceCoupon(
            coupon,
            withTrashed,
            onlyTrashed,
            code,
            type,
            discount,
            storeWide,
            products,
            productVariants,
            limit,
            expiresAt,
            minimumAmount,
            capturingOptions,
        )
        return captured as? RawResponse<SdkV2ReplaceCouponResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2ReplaceCoupon]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [v2ReplaceCoupon] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2ReplaceCouponSuspend")
    suspend fun v2ReplaceCouponSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: String? = null,
        type: SdkCreateCouponRequestApplicationJsonType? = null,
        discount: Any? = null,
        storeWide: Boolean? = null,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): SdkV2ReplaceCouponResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            v2ReplaceCoupon(
                coupon,
                withTrashed,
                onlyTrashed,
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [v2ReplaceCouponWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [v2ReplaceCouponWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2ReplaceCouponWithResponseSuspend")
    suspend fun v2ReplaceCouponWithResponseSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: String? = null,
        type: SdkCreateCouponRequestApplicationJsonType? = null,
        discount: Any? = null,
        storeWide: Boolean? = null,
        products: List<Long>? = null,
        productVariants: List<Long>? = null,
        limit: Long? = null,
        expiresAt: OffsetDateTime? = null,
        minimumAmount: Any? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2ReplaceCouponResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            v2ReplaceCouponWithResponse(
                coupon,
                withTrashed,
                onlyTrashed,
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Update a coupon
     *
     * Change a coupon's discount, limits, or eligible products. This v2 route is store-scoped.
     * OAuth callers use the admin grant and select a store with X-STORE. Current membership and
     * role permissions apply to every request.
     *
     * @param coupon The coupon path parameter.
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param code the code of the request.
     * @param type the type of the request.
     * @param discount A decimal discount value of at least 1. Percentage discounts cannot exceed
     *   100; amount discounts use the store currency's major unit.
     * @param storeWide the store wide of the request.
     * @param products Product IDs the coupon applies to when store_wide is false.
     * @param productVariants Optional variant restrictions. Omit to preserve existing restrictions
     *   or send an empty array to allow every variant of the selected products.
     * @param limit the limit of the request.
     * @param expiresAt A future date and time, or null for no expiry.
     * @param minimumAmount A decimal minimum order amount in the store currency's major unit, or
     *   null for no minimum.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkV2UpdateCouponResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun v2UpdateCoupon(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: PatchField<String> = PatchField.Absent,
        type: PatchField<SdkCreateCouponRequestApplicationJsonType> = PatchField.Absent,
        discount: PatchField<Any> = PatchField.Absent,
        storeWide: PatchField<Boolean> = PatchField.Absent,
        products: PatchField<List<Long>> = PatchField.Absent,
        productVariants: PatchField<List<Long>> = PatchField.Absent,
        limit: PatchField<Long?> = PatchField.Absent,
        expiresAt: PatchField<OffsetDateTime?> = PatchField.Absent,
        minimumAmount: PatchField<Any?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkV2UpdateCouponResponseValue200ApplicationJson {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val body =
            patchBodyOf(
                "code" to code,
                "type" to type,
                "discount" to discount,
                "store_wide" to storeWide,
                "products" to products,
                "product_variants" to productVariants,
                "limit" to limit,
                "expires_at" to expiresAt,
                "minimum_amount" to minimumAmount,
            )
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v2/coupons/${encodePathSegment(coupon)}",
                queryParams = params,
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkV2UpdateCouponRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkV2UpdateCouponResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "v2UpdateCoupon",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkV2UpdateCouponResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun v2UpdateCouponWithResponse(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: PatchField<String> = PatchField.Absent,
        type: PatchField<SdkCreateCouponRequestApplicationJsonType> = PatchField.Absent,
        discount: PatchField<Any> = PatchField.Absent,
        storeWide: PatchField<Boolean> = PatchField.Absent,
        products: PatchField<List<Long>> = PatchField.Absent,
        productVariants: PatchField<List<Long>> = PatchField.Absent,
        limit: PatchField<Long?> = PatchField.Absent,
        expiresAt: PatchField<OffsetDateTime?> = PatchField.Absent,
        minimumAmount: PatchField<Any?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2UpdateCouponResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2UpdateCoupon(
            coupon,
            withTrashed,
            onlyTrashed,
            code,
            type,
            discount,
            storeWide,
            products,
            productVariants,
            limit,
            expiresAt,
            minimumAmount,
            capturingOptions,
        )
        return captured as? RawResponse<SdkV2UpdateCouponResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2UpdateCoupon]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [v2UpdateCoupon] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2UpdateCouponSuspend")
    suspend fun v2UpdateCouponSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: PatchField<String> = PatchField.Absent,
        type: PatchField<SdkCreateCouponRequestApplicationJsonType> = PatchField.Absent,
        discount: PatchField<Any> = PatchField.Absent,
        storeWide: PatchField<Boolean> = PatchField.Absent,
        products: PatchField<List<Long>> = PatchField.Absent,
        productVariants: PatchField<List<Long>> = PatchField.Absent,
        limit: PatchField<Long?> = PatchField.Absent,
        expiresAt: PatchField<OffsetDateTime?> = PatchField.Absent,
        minimumAmount: PatchField<Any?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkV2UpdateCouponResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            v2UpdateCoupon(
                coupon,
                withTrashed,
                onlyTrashed,
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [v2UpdateCouponWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [v2UpdateCouponWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2UpdateCouponWithResponseSuspend")
    suspend fun v2UpdateCouponWithResponseSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        code: PatchField<String> = PatchField.Absent,
        type: PatchField<SdkCreateCouponRequestApplicationJsonType> = PatchField.Absent,
        discount: PatchField<Any> = PatchField.Absent,
        storeWide: PatchField<Boolean> = PatchField.Absent,
        products: PatchField<List<Long>> = PatchField.Absent,
        productVariants: PatchField<List<Long>> = PatchField.Absent,
        limit: PatchField<Long?> = PatchField.Absent,
        expiresAt: PatchField<OffsetDateTime?> = PatchField.Absent,
        minimumAmount: PatchField<Any?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2UpdateCouponResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            v2UpdateCouponWithResponse(
                coupon,
                withTrashed,
                onlyTrashed,
                code,
                type,
                discount,
                storeWide,
                products,
                productVariants,
                limit,
                expiresAt,
                minimumAmount,
                requestOptions,
            )
        }

    /**
     * Delete a coupon
     *
     * Soft-delete a coupon. The returned coupon includes its deleted_at timestamp; use with_trashed
     * or only_trashed to retrieve soft-deleted coupons later. This v2 route is store-scoped. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param coupon The coupon path parameter.
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun v2DeleteCoupon(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ) {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/coupons/${encodePathSegment(coupon)}",
                queryParams = params,
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkV2DeleteCouponResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "v2DeleteCoupon",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun v2DeleteCouponWithResponse(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2DeleteCoupon(coupon, withTrashed, onlyTrashed, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2DeleteCoupon]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [v2DeleteCoupon] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2DeleteCouponSuspend")
    suspend fun v2DeleteCouponSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            v2DeleteCoupon(coupon, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2DeleteCouponWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [v2DeleteCouponWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2DeleteCouponWithResponseSuspend")
    suspend fun v2DeleteCouponWithResponseSuspend(
        coupon: String,
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            v2DeleteCouponWithResponse(coupon, withTrashed, onlyTrashed, requestOptions)
        }

    /**
     * Batch create coupons
     *
     * Create multiple coupons in one request by sending a `resources` array. This v2 route is
     * store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkV2BatchCreateCouponsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun v2BatchCreateCoupons(
        resources: List<V2BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem>,
        requestOptions: RequestOptions? = null,
    ): SdkV2BatchCreateCouponsResponseValue200ApplicationJson {
        val body = bodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/coupons/batch",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkV2BatchCreateCouponsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkV2BatchCreateCouponsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "v2BatchCreateCoupons",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkV2BatchCreateCouponsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun v2BatchCreateCouponsWithResponse(
        resources: List<V2BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2BatchCreateCouponsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2BatchCreateCoupons(resources, capturingOptions)
        return captured as? RawResponse<SdkV2BatchCreateCouponsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2BatchCreateCoupons]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [v2BatchCreateCoupons] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2BatchCreateCouponsSuspend")
    suspend fun v2BatchCreateCouponsSuspend(
        resources: List<V2BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem>,
        requestOptions: RequestOptions? = null,
    ): SdkV2BatchCreateCouponsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            v2BatchCreateCoupons(resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2BatchCreateCouponsWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [v2BatchCreateCouponsWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("v2BatchCreateCouponsWithResponseSuspend")
    suspend fun v2BatchCreateCouponsWithResponseSuspend(
        resources: List<V2BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2BatchCreateCouponsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            v2BatchCreateCouponsWithResponse(resources, requestOptions)
        }

    /**
     * Batch update coupons
     *
     * Update multiple coupons in one request by sending a `resources` object keyed by coupon ID.
     * This v2 route is store-scoped. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkV2BatchUpdateCouponsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun v2BatchUpdateCoupons(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: V2BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): SdkV2BatchUpdateCouponsResponseValue200ApplicationJson {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val body = patchBodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v2/coupons/batch",
                queryParams = params,
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkV2BatchUpdateCouponsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkV2BatchUpdateCouponsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "v2BatchUpdateCoupons",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkV2BatchUpdateCouponsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun v2BatchUpdateCouponsWithResponse(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: V2BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2BatchUpdateCouponsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2BatchUpdateCoupons(withTrashed, onlyTrashed, resources, capturingOptions)
        return captured as? RawResponse<SdkV2BatchUpdateCouponsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2BatchUpdateCoupons]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [v2BatchUpdateCoupons] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2BatchUpdateCouponsSuspend")
    suspend fun v2BatchUpdateCouponsSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: V2BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): SdkV2BatchUpdateCouponsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            v2BatchUpdateCoupons(withTrashed, onlyTrashed, resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2BatchUpdateCouponsWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [v2BatchUpdateCouponsWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("v2BatchUpdateCouponsWithResponseSuspend")
    suspend fun v2BatchUpdateCouponsWithResponseSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: V2BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkV2BatchUpdateCouponsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            v2BatchUpdateCouponsWithResponse(withTrashed, onlyTrashed, resources, requestOptions)
        }

    /**
     * Batch delete coupons
     *
     * Soft-delete multiple coupons by sending their IDs in `resources`. This v2 route is
     * store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param withTrashed Include soft-deleted resources in the result.
     * @param onlyTrashed Return only soft-deleted resources. Ignored when with_trashed is true.
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun v2BatchDeleteCoupons(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ) {
        val params = mutableListOf<Pair<String, String>>()
        withTrashed?.let { params += "with_trashed" to it.toString() }
        onlyTrashed?.let { params += "only_trashed" to it.toString() }
        val body = bodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/coupons/batch",
                queryParams = params,
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkV2BatchDeleteCouponsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkV2BatchDeleteCouponsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "v2BatchDeleteCoupons",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun v2BatchDeleteCouponsWithResponse(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2BatchDeleteCoupons(withTrashed, onlyTrashed, resources, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2BatchDeleteCoupons]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [v2BatchDeleteCoupons] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2BatchDeleteCouponsSuspend")
    suspend fun v2BatchDeleteCouponsSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            v2BatchDeleteCoupons(withTrashed, onlyTrashed, resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2BatchDeleteCouponsWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [v2BatchDeleteCouponsWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("v2BatchDeleteCouponsWithResponseSuspend")
    suspend fun v2BatchDeleteCouponsWithResponseSuspend(
        withTrashed: Boolean? = null,
        onlyTrashed: Boolean? = null,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            v2BatchDeleteCouponsWithResponse(withTrashed, onlyTrashed, resources, requestOptions)
        }
}
