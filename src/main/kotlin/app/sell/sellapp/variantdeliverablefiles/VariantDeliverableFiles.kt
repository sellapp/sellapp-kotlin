// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.variantdeliverablefiles

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.PatchField
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.http.patchBodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.ListVariantDeliverableFilesResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.SdkGetVariantDeliverableFileResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkUploadVariantDeliverableFileResponseValue201ApplicationJson
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for VariantDeliverableFiles.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class VariantDeliverableFiles(internal val sellapp: SellApp) {
    /**
     * List variant deliverable files
     *
     * List variant deliverable files. Requires the listing Sanctum ability. Lists managed files
     * without revealing storage keys. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        product: String,
        variant: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListVariantDeliverableFilesResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<
                    ListVariantDeliverableFilesResponseValue200ApplicationJsonPropertyDataItem
                >() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path =
                "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/files",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listVariantDeliverableFiles",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models
                    .ListVariantDeliverableFilesResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        product: String,
        variant: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<
        Page<ListVariantDeliverableFilesResponseValue200ApplicationJsonPropertyDataItem>
    > {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(product, variant, limit, page, capturingOptions)
        return captured
            as?
            RawResponse<
                Page<ListVariantDeliverableFilesResponseValue200ApplicationJsonPropertyDataItem>
            > ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        product: String,
        variant: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListVariantDeliverableFilesResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            list(product, variant, limit, page, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        product: String,
        variant: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<
        Page<ListVariantDeliverableFilesResponseValue200ApplicationJsonPropertyDataItem>
    > =
        sellapp.baseClient.runCancellable {
            listWithResponse(product, variant, limit, page, requestOptions)
        }

    /**
     * Upload a variant deliverable file
     *
     * Upload a variant deliverable file. Requires the listing Sanctum ability. Accepts an uploaded
     * file, stores it under a server-generated key, and queues the durable move after commit.
     * Uploads may be staged before DOWNLOADABLE is activated; inactive pending files are retained
     * temporarily and scheduled for cleanup if activation is abandoned. Client-supplied storage
     * paths are not accepted. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param file the file of the request.
     * @param folderId the folder id of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkUploadVariantDeliverableFileResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun upload(
        product: String,
        variant: String,
        file: ByteArray,
        folderId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): SdkUploadVariantDeliverableFileResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "file" to file,
                "folder_id" to folderId,
            )
        body["file"] = file
        val config =
            RequestConfig(
                method = "POST",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/files",
                body = body,
                bodyEncoding = "form-data",
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkUploadVariantDeliverableFileRequestMultipartFormData
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkUploadVariantDeliverableFileResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "uploadVariantDeliverableFile",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkUploadVariantDeliverableFileResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun uploadWithResponse(
        product: String,
        variant: String,
        file: ByteArray,
        folderId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUploadVariantDeliverableFileResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        upload(product, variant, file, folderId, capturingOptions)
        return captured
            as? RawResponse<SdkUploadVariantDeliverableFileResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [upload]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [upload] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("uploadSuspend")
    suspend fun uploadSuspend(
        product: String,
        variant: String,
        file: ByteArray,
        folderId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): SdkUploadVariantDeliverableFileResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            upload(product, variant, file, folderId, requestOptions)
        }

    /**
     * Coroutine-aware variant of [uploadWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [uploadWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("uploadWithResponseSuspend")
    suspend fun uploadWithResponseSuspend(
        product: String,
        variant: String,
        file: ByteArray,
        folderId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUploadVariantDeliverableFileResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            uploadWithResponse(product, variant, file, folderId, requestOptions)
        }

    /**
     * Retrieve a variant deliverable file
     *
     * Retrieve a variant deliverable file. Requires the listing Sanctum ability. The file is scoped
     * through the authenticated store, product, and variant. Internal storage keys are immutable
     * and never returned. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param file The managed deliverable file identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetVariantDeliverableFileResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        product: String,
        variant: String,
        file: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetVariantDeliverableFileResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/files/${encodePathSegment(file)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkGetVariantDeliverableFileResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getVariantDeliverableFile",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetVariantDeliverableFileResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        product: String,
        variant: String,
        file: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetVariantDeliverableFileResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(product, variant, file, capturingOptions)
        return captured as? RawResponse<SdkGetVariantDeliverableFileResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        product: String,
        variant: String,
        file: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetVariantDeliverableFileResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(product, variant, file, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        product: String,
        variant: String,
        file: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetVariantDeliverableFileResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(product, variant, file, requestOptions)
        }

    /**
     * Replace variant deliverable file settings
     *
     * Replace variant deliverable file settings. Requires the listing Sanctum ability. The file is
     * scoped through the authenticated store, product, and variant. Internal storage keys are
     * immutable and never returned. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param file The managed deliverable file identifier.
     * @param customName the custom name of the request.
     * @param folderId the folder id of the request.
     * @param watermark the watermark of the request.
     * @param maxDownloads the max downloads of the request.
     * @param limitToPurchaseIp the limit to purchase ip of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        product: String,
        variant: String,
        file: String,
        customName: String? = null,
        folderId: Long? = null,
        watermark: Boolean? = null,
        maxDownloads: Long? = null,
        limitToPurchaseIp: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "custom_name" to customName,
                "folder_id" to folderId,
                "watermark" to watermark,
                "max_downloads" to maxDownloads,
                "limit_to_purchase_ip" to limitToPurchaseIp,
            )
        val config =
            RequestConfig(
                method = "PUT",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/files/${encodePathSegment(file)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models
                        .SdkReplaceVariantDeliverableFileSettingsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceVariantDeliverableFileSettings",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        product: String,
        variant: String,
        file: String,
        customName: String? = null,
        folderId: Long? = null,
        watermark: Boolean? = null,
        maxDownloads: Long? = null,
        limitToPurchaseIp: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(
            product,
            variant,
            file,
            customName,
            folderId,
            watermark,
            maxDownloads,
            limitToPurchaseIp,
            capturingOptions,
        )
        return captured
            as? RawResponse<SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        product: String,
        variant: String,
        file: String,
        customName: String? = null,
        folderId: Long? = null,
        watermark: Boolean? = null,
        maxDownloads: Long? = null,
        limitToPurchaseIp: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(
                product,
                variant,
                file,
                customName,
                folderId,
                watermark,
                maxDownloads,
                limitToPurchaseIp,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        product: String,
        variant: String,
        file: String,
        customName: String? = null,
        folderId: Long? = null,
        watermark: Boolean? = null,
        maxDownloads: Long? = null,
        limitToPurchaseIp: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(
                product,
                variant,
                file,
                customName,
                folderId,
                watermark,
                maxDownloads,
                limitToPurchaseIp,
                requestOptions,
            )
        }

    /**
     * Update a variant deliverable file
     *
     * Update a variant deliverable file. Requires the listing Sanctum ability. The file is scoped
     * through the authenticated store, product, and variant. Internal storage keys are immutable
     * and never returned. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param file The managed deliverable file identifier.
     * @param customName the custom name of the request.
     * @param folderId the folder id of the request.
     * @param watermark the watermark of the request.
     * @param maxDownloads the max downloads of the request.
     * @param limitToPurchaseIp the limit to purchase ip of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun update(
        product: String,
        variant: String,
        file: String,
        customName: PatchField<String?> = PatchField.Absent,
        folderId: PatchField<Long?> = PatchField.Absent,
        watermark: PatchField<Boolean> = PatchField.Absent,
        maxDownloads: PatchField<Long?> = PatchField.Absent,
        limitToPurchaseIp: PatchField<Boolean> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson {
        val body =
            patchBodyOf(
                "custom_name" to customName,
                "folder_id" to folderId,
                "watermark" to watermark,
                "max_downloads" to maxDownloads,
                "limit_to_purchase_ip" to limitToPurchaseIp,
            )
        val config =
            RequestConfig(
                method = "PATCH",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/files/${encodePathSegment(file)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkUpdateVariantDeliverableFileRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "updateVariantDeliverableFile",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun updateWithResponse(
        product: String,
        variant: String,
        file: String,
        customName: PatchField<String?> = PatchField.Absent,
        folderId: PatchField<Long?> = PatchField.Absent,
        watermark: PatchField<Boolean> = PatchField.Absent,
        maxDownloads: PatchField<Long?> = PatchField.Absent,
        limitToPurchaseIp: PatchField<Boolean> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        update(
            product,
            variant,
            file,
            customName,
            folderId,
            watermark,
            maxDownloads,
            limitToPurchaseIp,
            capturingOptions,
        )
        return captured
            as? RawResponse<SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [update]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [update] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateSuspend")
    suspend fun updateSuspend(
        product: String,
        variant: String,
        file: String,
        customName: PatchField<String?> = PatchField.Absent,
        folderId: PatchField<Long?> = PatchField.Absent,
        watermark: PatchField<Boolean> = PatchField.Absent,
        maxDownloads: PatchField<Long?> = PatchField.Absent,
        limitToPurchaseIp: PatchField<Boolean> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            update(
                product,
                variant,
                file,
                customName,
                folderId,
                watermark,
                maxDownloads,
                limitToPurchaseIp,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [updateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [updateWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateWithResponseSuspend")
    suspend fun updateWithResponseSuspend(
        product: String,
        variant: String,
        file: String,
        customName: PatchField<String?> = PatchField.Absent,
        folderId: PatchField<Long?> = PatchField.Absent,
        watermark: PatchField<Boolean> = PatchField.Absent,
        maxDownloads: PatchField<Long?> = PatchField.Absent,
        limitToPurchaseIp: PatchField<Boolean> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            updateWithResponse(
                product,
                variant,
                file,
                customName,
                folderId,
                watermark,
                maxDownloads,
                limitToPurchaseIp,
                requestOptions,
            )
        }

    /**
     * Delete a variant deliverable file
     *
     * Delete a variant deliverable file. Requires the listing Sanctum ability. The file is scoped
     * through the authenticated store, product, and variant. Internal storage keys are immutable
     * and never returned. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param file The managed deliverable file identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun delete(
        product: String,
        variant: String,
        file: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/files/${encodePathSegment(file)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkDeleteVariantDeliverableFileResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "deleteVariantDeliverableFile",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun deleteWithResponse(
        product: String,
        variant: String,
        file: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        delete(product, variant, file, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [delete]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [delete] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteSuspend")
    suspend fun deleteSuspend(
        product: String,
        variant: String,
        file: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            delete(product, variant, file, requestOptions)
        }

    /**
     * Coroutine-aware variant of [deleteWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [deleteWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteWithResponseSuspend")
    suspend fun deleteWithResponseSuspend(
        product: String,
        variant: String,
        file: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            deleteWithResponse(product, variant, file, requestOptions)
        }
}
