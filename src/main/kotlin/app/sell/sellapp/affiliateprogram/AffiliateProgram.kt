// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.affiliateprogram

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.ListPendingAffiliateInvitationsResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.ReplaceAffiliateProgramConfigurationRequestApplicationJsonPropertyProductsItem
import app.sell.sellapp.models.ReplaceAffiliateProgramConfigurationRequestApplicationJsonPropertySettings
import app.sell.sellapp.models.SdkGetAffiliateProgramConfigurationResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkInviteAffiliateResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkReplaceAffiliateProgramConfigurationResponseValue200ApplicationJson
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for AffiliateProgram.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class AffiliateProgram(internal val sellapp: SellApp) {
    /**
     * Retrieve affiliate program configuration
     *
     * Retrieve the authenticated store's affiliate-program enablement, commissions, tracking
     * settings, payout preferences, and selected product variants. Requires the `affiliate` token
     * ability. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetAffiliateProgramConfigurationResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        requestOptions: RequestOptions? = null
    ): SdkGetAffiliateProgramConfigurationResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/affiliate-program",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkGetAffiliateProgramConfigurationResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getAffiliateProgramConfiguration",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetAffiliateProgramConfigurationResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetAffiliateProgramConfigurationResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(capturingOptions)
        return captured
            as? RawResponse<SdkGetAffiliateProgramConfigurationResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        requestOptions: RequestOptions? = null
    ): SdkGetAffiliateProgramConfigurationResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetAffiliateProgramConfigurationResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(requestOptions)
        }

    /**
     * Replace affiliate program configuration
     *
     * Replace the complete affiliate-program configuration. The products array is authoritative:
     * enabled entries replace existing store-level product restrictions, and every variant ID must
     * belong to the authenticated store. Requires the `affiliate` token ability. OAuth callers use
     * the admin grant and select a store with X-STORE. Current membership and role permissions
     * apply to every request.
     *
     * @param enabled the enabled of the request.
     * @param settings the settings of the request.
     * @param products the products of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceAffiliateProgramConfigurationResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        enabled: Boolean,
        settings: ReplaceAffiliateProgramConfigurationRequestApplicationJsonPropertySettings,
        products:
            List<ReplaceAffiliateProgramConfigurationRequestApplicationJsonPropertyProductsItem>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceAffiliateProgramConfigurationResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "enabled" to enabled,
                "settings" to settings,
                "products" to products,
            )
        body["enabled"] = enabled
        body["settings"] = settings
        body["products"] = products
        val config =
            RequestConfig(
                method = "PUT",
                path = "/v2/affiliate-program",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models
                        .SdkReplaceAffiliateProgramConfigurationRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkReplaceAffiliateProgramConfigurationResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceAffiliateProgramConfiguration",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceAffiliateProgramConfigurationResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        enabled: Boolean,
        settings: ReplaceAffiliateProgramConfigurationRequestApplicationJsonPropertySettings,
        products:
            List<ReplaceAffiliateProgramConfigurationRequestApplicationJsonPropertyProductsItem>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceAffiliateProgramConfigurationResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(enabled, settings, products, capturingOptions)
        return captured
            as? RawResponse<SdkReplaceAffiliateProgramConfigurationResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        enabled: Boolean,
        settings: ReplaceAffiliateProgramConfigurationRequestApplicationJsonPropertySettings,
        products:
            List<ReplaceAffiliateProgramConfigurationRequestApplicationJsonPropertyProductsItem>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceAffiliateProgramConfigurationResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(enabled, settings, products, requestOptions)
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        enabled: Boolean,
        settings: ReplaceAffiliateProgramConfigurationRequestApplicationJsonPropertySettings,
        products:
            List<ReplaceAffiliateProgramConfigurationRequestApplicationJsonPropertyProductsItem>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceAffiliateProgramConfigurationResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(enabled, settings, products, requestOptions)
        }

    /**
     * List pending affiliate invitations
     *
     * List pending invitations that have not completed affiliate onboarding. The one-time
     * onboarding link is delivered by email and is never returned by the API. Requires the
     * `affiliate` token ability. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListPendingAffiliateInvitationsResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<
                    ListPendingAffiliateInvitationsResponseValue200ApplicationJsonPropertyDataItem
                >() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/affiliate-invitations",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listPendingAffiliateInvitations",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models
                    .ListPendingAffiliateInvitationsResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<
        Page<ListPendingAffiliateInvitationsResponseValue200ApplicationJsonPropertyDataItem>
    > {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(limit, page, capturingOptions)
        return captured
            as?
            RawResponse<
                Page<ListPendingAffiliateInvitationsResponseValue200ApplicationJsonPropertyDataItem>
            > ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListPendingAffiliateInvitationsResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            list(limit, page, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<
        Page<ListPendingAffiliateInvitationsResponseValue200ApplicationJsonPropertyDataItem>
    > =
        sellapp.baseClient.runCancellable {
            listWithResponse(limit, page, requestOptions)
        }

    /**
     * Invite an affiliate
     *
     * Create a pending affiliate invitation and email a single-use onboarding link. Duplicate
     * invitations are rejected and invites are rate limited per store. The affiliate program must
     * be enabled. Requires the `affiliate` token ability. OAuth callers use the admin grant and
     * select a store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param email the email of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkInviteAffiliateResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun invite(
        email: String,
        requestOptions: RequestOptions? = null,
    ): SdkInviteAffiliateResponseValue201ApplicationJson {
        val body = bodyOf("email" to email)
        body["email"] = email
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/affiliate-invitations",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkInviteAffiliateRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkInviteAffiliateResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "inviteAffiliate",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkInviteAffiliateResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun inviteWithResponse(
        email: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkInviteAffiliateResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        invite(email, capturingOptions)
        return captured as? RawResponse<SdkInviteAffiliateResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [invite]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [invite] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("inviteSuspend")
    suspend fun inviteSuspend(
        email: String,
        requestOptions: RequestOptions? = null,
    ): SdkInviteAffiliateResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            invite(email, requestOptions)
        }

    /**
     * Coroutine-aware variant of [inviteWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [inviteWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("inviteWithResponseSuspend")
    suspend fun inviteWithResponseSuspend(
        email: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkInviteAffiliateResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            inviteWithResponse(email, requestOptions)
        }
}
