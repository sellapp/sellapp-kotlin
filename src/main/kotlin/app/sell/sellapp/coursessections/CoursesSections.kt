// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.coursessections

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.PatchField
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.http.patchBodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkCreateCourseSectionResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkReorderCourseSectionsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkReplaceCourseSectionResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkUpdateCourseSectionResponseValue200ApplicationJson
import java.time.OffsetDateTime

/**
 * API accessor for CoursesSections.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class CoursesSections(internal val sellapp: SellApp) {
    /**
     * Create a course section
     *
     * Append a section to a course curriculum. OAuth callers use the admin grant and select a store
     * with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param course The course path parameter.
     * @param title the title of the request.
     * @param description the description of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateCourseSectionResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun create(
        course: String,
        title: String,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateCourseSectionResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "title" to title,
                "description" to description,
            )
        body["title"] = title
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/courses/${encodePathSegment(course)}/sections",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateCourseSectionRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateCourseSectionResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "createCourseSection",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateCourseSectionResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createWithResponse(
        course: String,
        title: String,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateCourseSectionResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        create(course, title, description, capturingOptions)
        return captured as? RawResponse<SdkCreateCourseSectionResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [create]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [create] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createSuspend")
    suspend fun createSuspend(
        course: String,
        title: String,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateCourseSectionResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            create(course, title, description, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createWithResponseSuspend")
    suspend fun createWithResponseSuspend(
        course: String,
        title: String,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateCourseSectionResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createWithResponse(course, title, description, requestOptions)
        }

    /**
     * Update a course section
     *
     * Update a section title or sanitized rich-text description. OAuth callers use the admin grant
     * and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param course The course path parameter.
     * @param section The section path parameter.
     * @param title the title of the request.
     * @param description the description of the request.
     * @param expectedUpdatedAt the expected updated at of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceCourseSectionResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        course: String,
        section: String,
        title: String? = null,
        description: String? = null,
        expectedUpdatedAt: OffsetDateTime? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceCourseSectionResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "title" to title,
                "description" to description,
                "expected_updated_at" to expectedUpdatedAt,
            )
        val config =
            RequestConfig(
                method = "PUT",
                path =
                    "/v2/courses/${encodePathSegment(course)}/sections/${encodePathSegment(section)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkReplaceCourseSectionRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkReplaceCourseSectionResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceCourseSection",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceCourseSectionResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        course: String,
        section: String,
        title: String? = null,
        description: String? = null,
        expectedUpdatedAt: OffsetDateTime? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceCourseSectionResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(course, section, title, description, expectedUpdatedAt, capturingOptions)
        return captured as? RawResponse<SdkReplaceCourseSectionResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        course: String,
        section: String,
        title: String? = null,
        description: String? = null,
        expectedUpdatedAt: OffsetDateTime? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceCourseSectionResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(course, section, title, description, expectedUpdatedAt, requestOptions)
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        course: String,
        section: String,
        title: String? = null,
        description: String? = null,
        expectedUpdatedAt: OffsetDateTime? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceCourseSectionResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(
                course,
                section,
                title,
                description,
                expectedUpdatedAt,
                requestOptions,
            )
        }

    /**
     * Update a course section
     *
     * Update a section title or sanitized rich-text description. OAuth callers use the admin grant
     * and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param course The course path parameter.
     * @param section The section path parameter.
     * @param title the title of the request.
     * @param description the description of the request.
     * @param expectedUpdatedAt the expected updated at of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkUpdateCourseSectionResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun update(
        course: String,
        section: String,
        title: PatchField<String> = PatchField.Absent,
        description: PatchField<String?> = PatchField.Absent,
        expectedUpdatedAt: PatchField<OffsetDateTime> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateCourseSectionResponseValue200ApplicationJson {
        val body =
            patchBodyOf(
                "title" to title,
                "description" to description,
                "expected_updated_at" to expectedUpdatedAt,
            )
        val config =
            RequestConfig(
                method = "PATCH",
                path =
                    "/v2/courses/${encodePathSegment(course)}/sections/${encodePathSegment(section)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkUpdateCourseSectionRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkUpdateCourseSectionResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "updateCourseSection",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkUpdateCourseSectionResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun updateWithResponse(
        course: String,
        section: String,
        title: PatchField<String> = PatchField.Absent,
        description: PatchField<String?> = PatchField.Absent,
        expectedUpdatedAt: PatchField<OffsetDateTime> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateCourseSectionResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        update(course, section, title, description, expectedUpdatedAt, capturingOptions)
        return captured as? RawResponse<SdkUpdateCourseSectionResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [update]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [update] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateSuspend")
    suspend fun updateSuspend(
        course: String,
        section: String,
        title: PatchField<String> = PatchField.Absent,
        description: PatchField<String?> = PatchField.Absent,
        expectedUpdatedAt: PatchField<OffsetDateTime> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateCourseSectionResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            update(course, section, title, description, expectedUpdatedAt, requestOptions)
        }

    /**
     * Coroutine-aware variant of [updateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [updateWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateWithResponseSuspend")
    suspend fun updateWithResponseSuspend(
        course: String,
        section: String,
        title: PatchField<String> = PatchField.Absent,
        description: PatchField<String?> = PatchField.Absent,
        expectedUpdatedAt: PatchField<OffsetDateTime> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateCourseSectionResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            updateWithResponse(
                course,
                section,
                title,
                description,
                expectedUpdatedAt,
                requestOptions,
            )
        }

    /**
     * Delete a course section
     *
     * Delete a section, its lessons, structured questions, attachments, and video references, then
     * normalize remaining section order. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param course The course path parameter.
     * @param section The section path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun delete(
        course: String,
        section: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path =
                    "/v2/courses/${encodePathSegment(course)}/sections/${encodePathSegment(section)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkDeleteCourseSectionResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "deleteCourseSection",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun deleteWithResponse(
        course: String,
        section: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        delete(course, section, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [delete]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [delete] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteSuspend")
    suspend fun deleteSuspend(
        course: String,
        section: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            delete(course, section, requestOptions)
        }

    /**
     * Coroutine-aware variant of [deleteWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [deleteWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteWithResponseSuspend")
    suspend fun deleteWithResponseSuspend(
        course: String,
        section: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            deleteWithResponse(course, section, requestOptions)
        }

    /**
     * Reorder course sections
     *
     * Replace the complete section order. Send every section ID exactly once in the desired order.
     * OAuth callers use the admin grant and select a store with X-STORE. Current membership and
     * role permissions apply to every request.
     *
     * @param course The course path parameter.
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReorderCourseSectionsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun reorder(
        course: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkReorderCourseSectionsResponseValue200ApplicationJson {
        val body = patchBodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v2/courses/${encodePathSegment(course)}/sections/reorder",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkReorderCourseSectionsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkReorderCourseSectionsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "reorderCourseSections",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReorderCourseSectionsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun reorderWithResponse(
        course: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReorderCourseSectionsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        reorder(course, resources, capturingOptions)
        return captured as? RawResponse<SdkReorderCourseSectionsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [reorder]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [reorder] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("reorderSuspend")
    suspend fun reorderSuspend(
        course: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkReorderCourseSectionsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            reorder(course, resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [reorderWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [reorderWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("reorderWithResponseSuspend")
    suspend fun reorderWithResponseSuspend(
        course: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReorderCourseSectionsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            reorderWithResponse(course, resources, requestOptions)
        }
}
