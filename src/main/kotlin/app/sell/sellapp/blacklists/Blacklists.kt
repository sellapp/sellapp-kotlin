// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.blacklists

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.PatchField
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.http.patchBodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.Blacklist
import app.sell.sellapp.models.BlacklistResponse
import app.sell.sellapp.types.BlacklistType
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for Blacklists.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class Blacklists(internal val sellapp: SellApp) {
    /**
     * List all blacklist rules
     *
     * Retrieve a paginated list of blacklist rules for your store. By default, a maximum of fifteen
     * blacklist rules are returned per page.
     *
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<Blacklist> {
        val itemType = object : TypeReference<Blacklist>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v1/blacklists",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listBlacklists",
            supportsIdempotency = false,
            itemSchema = app.sell.sellapp.models.Blacklist.runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<Blacklist>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(limit, page, capturingOptions)
        return captured as? RawResponse<Page<Blacklist>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<Blacklist> =
        sellapp.baseClient.runCancellable {
            list(limit, page, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<Blacklist>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(limit, page, requestOptions)
        }

    /**
     * Create a blacklist rule
     *
     * Create a new blacklist rule for your store.
     *
     * @param type the type of the request.
     * @param data The value to blacklist.
     * @param description Why this rule is being created.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the BlacklistResponse
     */
    @JvmOverloads
    fun create(
        type: BlacklistType,
        data: String,
        description: String,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse {
        val body =
            bodyOf(
                "type" to type,
                "data" to data,
                "description" to description,
            )
        body["type"] = type
        body["data"] = data
        body["description"] = description
        val config =
            RequestConfig(
                method = "POST",
                path = "/v1/blacklists",
                body = body,
                requestOptions = requestOptions,
                requestSchema = app.sell.sellapp.models.BlacklistCreateRequest.runtimeSchema,
                responseSchema = app.sell.sellapp.models.BlacklistResponse.runtimeSchema,
                operationId = "createBlacklist",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(config, BlacklistResponse::class.java)
    }

    @Suppress("UNCHECKED_CAST")
    fun createWithResponse(
        type: BlacklistType,
        data: String,
        description: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        create(type, data, description, capturingOptions)
        return captured as? RawResponse<BlacklistResponse>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [create]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [create] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createSuspend")
    suspend fun createSuspend(
        type: BlacklistType,
        data: String,
        description: String,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse =
        sellapp.baseClient.runCancellable {
            create(type, data, description, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createWithResponseSuspend")
    suspend fun createWithResponseSuspend(
        type: BlacklistType,
        data: String,
        description: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> =
        sellapp.baseClient.runCancellable {
            createWithResponse(type, data, description, requestOptions)
        }

    /**
     * Retrieve a blacklist rule
     *
     * Retrieve a specific blacklist rule by its unique identifier.
     *
     * @param blacklist The unique identifier of the blacklist rule.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the BlacklistResponse
     */
    @JvmOverloads
    fun get(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v1/blacklists/${encodePathSegment(blacklist)}",
                requestOptions = requestOptions,
                responseSchema = app.sell.sellapp.models.BlacklistResponse.runtimeSchema,
                operationId = "getBlacklist",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(config, BlacklistResponse::class.java)
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(blacklist, capturingOptions)
        return captured as? RawResponse<BlacklistResponse>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse =
        sellapp.baseClient.runCancellable {
            get(blacklist, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> =
        sellapp.baseClient.runCancellable {
            getWithResponse(blacklist, requestOptions)
        }

    /**
     * Update a blacklist rule
     *
     * Update one or more attributes on an existing blacklist rule.
     *
     * @param blacklist The unique identifier of the blacklist rule.
     * @param type the type of the request.
     * @param data The updated value to blacklist.
     * @param description The updated reason for this rule.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the BlacklistResponse
     */
    @JvmOverloads
    fun update(
        blacklist: String,
        type: PatchField<BlacklistType> = PatchField.Absent,
        data: PatchField<String> = PatchField.Absent,
        description: PatchField<String> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse {
        val body =
            patchBodyOf(
                "type" to type,
                "data" to data,
                "description" to description,
            )
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v1/blacklists/${encodePathSegment(blacklist)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema = app.sell.sellapp.models.BlacklistUpdateRequest.runtimeSchema,
                responseSchema = app.sell.sellapp.models.BlacklistResponse.runtimeSchema,
                operationId = "updateBlacklist",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(config, BlacklistResponse::class.java)
    }

    @Suppress("UNCHECKED_CAST")
    fun updateWithResponse(
        blacklist: String,
        type: PatchField<BlacklistType> = PatchField.Absent,
        data: PatchField<String> = PatchField.Absent,
        description: PatchField<String> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        update(blacklist, type, data, description, capturingOptions)
        return captured as? RawResponse<BlacklistResponse>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [update]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [update] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateSuspend")
    suspend fun updateSuspend(
        blacklist: String,
        type: PatchField<BlacklistType> = PatchField.Absent,
        data: PatchField<String> = PatchField.Absent,
        description: PatchField<String> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse =
        sellapp.baseClient.runCancellable {
            update(blacklist, type, data, description, requestOptions)
        }

    /**
     * Coroutine-aware variant of [updateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [updateWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateWithResponseSuspend")
    suspend fun updateWithResponseSuspend(
        blacklist: String,
        type: PatchField<BlacklistType> = PatchField.Absent,
        data: PatchField<String> = PatchField.Absent,
        description: PatchField<String> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> =
        sellapp.baseClient.runCancellable {
            updateWithResponse(blacklist, type, data, description, requestOptions)
        }

    /**
     * Delete a blacklist rule
     *
     * Permanently delete a blacklist rule from your store.
     *
     * @param blacklist The unique identifier of the blacklist rule.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun delete(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v1/blacklists/${encodePathSegment(blacklist)}",
                requestOptions = requestOptions,
                responseSchema = app.sell.sellapp.models.BlacklistResponse.runtimeSchema,
                operationId = "deleteBlacklist",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun deleteWithResponse(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        delete(blacklist, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [delete]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [delete] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteSuspend")
    suspend fun deleteSuspend(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            delete(blacklist, requestOptions)
        }

    /**
     * Coroutine-aware variant of [deleteWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [deleteWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteWithResponseSuspend")
    suspend fun deleteWithResponseSuspend(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            deleteWithResponse(blacklist, requestOptions)
        }

    /**
     * List blacklist rules
     *
     * List store-scoped blacklist rules. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun v2ListBlacklists(requestOptions: RequestOptions? = null): Page<Blacklist> {
        val itemType = object : TypeReference<Blacklist>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/blacklists",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "v2ListBlacklists",
            supportsIdempotency = false,
            itemSchema = app.sell.sellapp.models.Blacklist.runtimeSchema,
            before = null,
            after = null,
        ) {}
    }

    @Suppress("UNCHECKED_CAST")
    fun v2ListBlacklistsWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<Page<Blacklist>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2ListBlacklists(capturingOptions)
        return captured as? RawResponse<Page<Blacklist>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2ListBlacklists]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [v2ListBlacklists] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2ListBlacklistsSuspend")
    suspend fun v2ListBlacklistsSuspend(requestOptions: RequestOptions? = null): Page<Blacklist> =
        sellapp.baseClient.runCancellable {
            v2ListBlacklists(requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2ListBlacklistsWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [v2ListBlacklistsWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2ListBlacklistsWithResponseSuspend")
    suspend fun v2ListBlacklistsWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<Page<Blacklist>> =
        sellapp.baseClient.runCancellable {
            v2ListBlacklistsWithResponse(requestOptions)
        }

    /**
     * Create a blacklist rule
     *
     * Create a store-scoped blacklist rule. OAuth callers use the admin grant and select a store
     * with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param type the type of the request.
     * @param data The value to blacklist.
     * @param description Why this rule is being created.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the BlacklistResponse
     */
    @JvmOverloads
    fun v2CreateBlacklist(
        type: BlacklistType,
        data: String,
        description: String,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse {
        val body =
            bodyOf(
                "type" to type,
                "data" to data,
                "description" to description,
            )
        body["type"] = type
        body["data"] = data
        body["description"] = description
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/blacklists",
                body = body,
                requestOptions = requestOptions,
                requestSchema = app.sell.sellapp.models.BlacklistCreateRequest.runtimeSchema,
                responseSchema = app.sell.sellapp.models.BlacklistResponse.runtimeSchema,
                operationId = "v2CreateBlacklist",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(config, BlacklistResponse::class.java)
    }

    @Suppress("UNCHECKED_CAST")
    fun v2CreateBlacklistWithResponse(
        type: BlacklistType,
        data: String,
        description: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2CreateBlacklist(type, data, description, capturingOptions)
        return captured as? RawResponse<BlacklistResponse>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2CreateBlacklist]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [v2CreateBlacklist] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2CreateBlacklistSuspend")
    suspend fun v2CreateBlacklistSuspend(
        type: BlacklistType,
        data: String,
        description: String,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse =
        sellapp.baseClient.runCancellable {
            v2CreateBlacklist(type, data, description, requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2CreateBlacklistWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [v2CreateBlacklistWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("v2CreateBlacklistWithResponseSuspend")
    suspend fun v2CreateBlacklistWithResponseSuspend(
        type: BlacklistType,
        data: String,
        description: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> =
        sellapp.baseClient.runCancellable {
            v2CreateBlacklistWithResponse(type, data, description, requestOptions)
        }

    /**
     * Retrieve a blacklist rule
     *
     * Retrieve a blacklist rule in the selected store. OAuth callers use the admin grant and select
     * a store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param blacklist The blacklist identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the BlacklistResponse
     */
    @JvmOverloads
    fun v2GetBlacklist(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/blacklists/${encodePathSegment(blacklist)}",
                requestOptions = requestOptions,
                responseSchema = app.sell.sellapp.models.BlacklistResponse.runtimeSchema,
                operationId = "v2GetBlacklist",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(config, BlacklistResponse::class.java)
    }

    @Suppress("UNCHECKED_CAST")
    fun v2GetBlacklistWithResponse(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2GetBlacklist(blacklist, capturingOptions)
        return captured as? RawResponse<BlacklistResponse>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2GetBlacklist]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [v2GetBlacklist] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2GetBlacklistSuspend")
    suspend fun v2GetBlacklistSuspend(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse =
        sellapp.baseClient.runCancellable {
            v2GetBlacklist(blacklist, requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2GetBlacklistWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [v2GetBlacklistWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2GetBlacklistWithResponseSuspend")
    suspend fun v2GetBlacklistWithResponseSuspend(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> =
        sellapp.baseClient.runCancellable {
            v2GetBlacklistWithResponse(blacklist, requestOptions)
        }

    /**
     * Replace a blacklist rule
     *
     * Replace a blacklist rule in the selected store. OAuth callers use the admin grant and select
     * a store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param blacklist The blacklist identifier.
     * @param type the type of the request.
     * @param data The updated value to blacklist.
     * @param description The updated reason for this rule.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the BlacklistResponse
     */
    @JvmOverloads
    fun v2ReplaceBlacklist(
        blacklist: String,
        type: BlacklistType? = null,
        data: String? = null,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse {
        val body =
            bodyOf(
                "type" to type,
                "data" to data,
                "description" to description,
            )
        val config =
            RequestConfig(
                method = "PUT",
                path = "/v2/blacklists/${encodePathSegment(blacklist)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema = app.sell.sellapp.models.BlacklistUpdateRequest.runtimeSchema,
                responseSchema = app.sell.sellapp.models.BlacklistResponse.runtimeSchema,
                operationId = "v2ReplaceBlacklist",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(config, BlacklistResponse::class.java)
    }

    @Suppress("UNCHECKED_CAST")
    fun v2ReplaceBlacklistWithResponse(
        blacklist: String,
        type: BlacklistType? = null,
        data: String? = null,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2ReplaceBlacklist(blacklist, type, data, description, capturingOptions)
        return captured as? RawResponse<BlacklistResponse>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2ReplaceBlacklist]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [v2ReplaceBlacklist] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2ReplaceBlacklistSuspend")
    suspend fun v2ReplaceBlacklistSuspend(
        blacklist: String,
        type: BlacklistType? = null,
        data: String? = null,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse =
        sellapp.baseClient.runCancellable {
            v2ReplaceBlacklist(blacklist, type, data, description, requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2ReplaceBlacklistWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [v2ReplaceBlacklistWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("v2ReplaceBlacklistWithResponseSuspend")
    suspend fun v2ReplaceBlacklistWithResponseSuspend(
        blacklist: String,
        type: BlacklistType? = null,
        data: String? = null,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> =
        sellapp.baseClient.runCancellable {
            v2ReplaceBlacklistWithResponse(blacklist, type, data, description, requestOptions)
        }

    /**
     * Update a blacklist rule
     *
     * Update a blacklist rule in the selected store. OAuth callers use the admin grant and select a
     * store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param blacklist The blacklist identifier.
     * @param type the type of the request.
     * @param data The updated value to blacklist.
     * @param description The updated reason for this rule.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the BlacklistResponse
     */
    @JvmOverloads
    fun v2UpdateBlacklist(
        blacklist: String,
        type: PatchField<BlacklistType> = PatchField.Absent,
        data: PatchField<String> = PatchField.Absent,
        description: PatchField<String> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse {
        val body =
            patchBodyOf(
                "type" to type,
                "data" to data,
                "description" to description,
            )
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v2/blacklists/${encodePathSegment(blacklist)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema = app.sell.sellapp.models.BlacklistUpdateRequest.runtimeSchema,
                responseSchema = app.sell.sellapp.models.BlacklistResponse.runtimeSchema,
                operationId = "v2UpdateBlacklist",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(config, BlacklistResponse::class.java)
    }

    @Suppress("UNCHECKED_CAST")
    fun v2UpdateBlacklistWithResponse(
        blacklist: String,
        type: PatchField<BlacklistType> = PatchField.Absent,
        data: PatchField<String> = PatchField.Absent,
        description: PatchField<String> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2UpdateBlacklist(blacklist, type, data, description, capturingOptions)
        return captured as? RawResponse<BlacklistResponse>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2UpdateBlacklist]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [v2UpdateBlacklist] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2UpdateBlacklistSuspend")
    suspend fun v2UpdateBlacklistSuspend(
        blacklist: String,
        type: PatchField<BlacklistType> = PatchField.Absent,
        data: PatchField<String> = PatchField.Absent,
        description: PatchField<String> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): BlacklistResponse =
        sellapp.baseClient.runCancellable {
            v2UpdateBlacklist(blacklist, type, data, description, requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2UpdateBlacklistWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [v2UpdateBlacklistWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("v2UpdateBlacklistWithResponseSuspend")
    suspend fun v2UpdateBlacklistWithResponseSuspend(
        blacklist: String,
        type: PatchField<BlacklistType> = PatchField.Absent,
        data: PatchField<String> = PatchField.Absent,
        description: PatchField<String> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<BlacklistResponse> =
        sellapp.baseClient.runCancellable {
            v2UpdateBlacklistWithResponse(blacklist, type, data, description, requestOptions)
        }

    /**
     * Delete a blacklist rule
     *
     * Delete a blacklist rule in the selected store. OAuth callers use the admin grant and select a
     * store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param blacklist The blacklist identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun v2DeleteBlacklist(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/blacklists/${encodePathSegment(blacklist)}",
                requestOptions = requestOptions,
                responseSchema = app.sell.sellapp.models.BlacklistResponse.runtimeSchema,
                operationId = "v2DeleteBlacklist",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun v2DeleteBlacklistWithResponse(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        v2DeleteBlacklist(blacklist, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [v2DeleteBlacklist]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [v2DeleteBlacklist] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("v2DeleteBlacklistSuspend")
    suspend fun v2DeleteBlacklistSuspend(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            v2DeleteBlacklist(blacklist, requestOptions)
        }

    /**
     * Coroutine-aware variant of [v2DeleteBlacklistWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [v2DeleteBlacklistWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("v2DeleteBlacklistWithResponseSuspend")
    suspend fun v2DeleteBlacklistWithResponseSuspend(
        blacklist: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            v2DeleteBlacklistWithResponse(blacklist, requestOptions)
        }
}
