// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.exports

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.CreateExportRequestApplicationJsonPropertyParameters
import app.sell.sellapp.models.ListExportsResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.SdkCreateExportResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkGetExportResponseValue200ApplicationJson
import app.sell.sellapp.types.SdkCreateExportRequestApplicationJsonFormat
import app.sell.sellapp.types.SdkCreateExportRequestApplicationJsonType
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for Exports.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class Exports(internal val sellapp: SellApp) {
    /**
     * List exports
     *
     * List permitted exports, 50 per page. OAuth callers use the admin grant and select a store
     * with X-STORE. Current membership and role permissions apply to every request. Sales, tax and
     * customer reports require invoice store permission. Affiliate payout reports require affiliate
     * and affiliate:payout permissions. The category selects the applicable permission checks;
     * listing returns only authorized categories.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        requestOptions: RequestOptions? = null
    ): Page<ListExportsResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object : TypeReference<ListExportsResponseValue200ApplicationJsonPropertyDataItem>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/exports",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listExports",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.ListExportsResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {}
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<Page<ListExportsResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(capturingOptions)
        return captured
            as? RawResponse<Page<ListExportsResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        requestOptions: RequestOptions? = null
    ): Page<ListExportsResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            list(requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<Page<ListExportsResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(requestOptions)
        }

    /**
     * Create an export
     *
     * Create a queued sales, tax, customer or affiliate payout report. One export may be queued or
     * running per store, and two per requesting account across all credentials and stores. When
     * capacity is full, HTTP 429 includes Retry-After: 60; poll the existing export and wait before
     * creating another. Date ranges include both end dates and may span at most 366 calendar days.
     * Omitted or null to defaults to today; omitted or null from defaults to 365 days before to.
     * Sales and tax use sale update dates; customers and affiliate payouts use creation dates.
     * Reports are limited to 10,000 source rows, 5 MiB of serialized source data and a 10 MiB
     * output file. Tax summaries count source sales toward these limits. A report that exceeds a
     * limit fails without publishing a partial file; request a smaller date range. Jobs may wait up
     * to 15 minutes to start and run for at most 60 seconds. The requesting actor is recorded but
     * is not returned in this response. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request. Sales, tax and
     * customer reports require invoice store permission. Affiliate payout reports require affiliate
     * and affiliate:payout permissions. The category selects the applicable permission checks;
     * listing returns only authorized categories.
     *
     * @param type the type of the request.
     * @param format the format of the request.
     * @param parameters the parameters of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateExportResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun create(
        type: SdkCreateExportRequestApplicationJsonType,
        format: SdkCreateExportRequestApplicationJsonFormat,
        parameters: CreateExportRequestApplicationJsonPropertyParameters? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateExportResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "type" to type,
                "format" to format,
                "parameters" to parameters,
            )
        body["type"] = type
        body["format"] = format
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/exports",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateExportRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateExportResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "createExport",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateExportResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createWithResponse(
        type: SdkCreateExportRequestApplicationJsonType,
        format: SdkCreateExportRequestApplicationJsonFormat,
        parameters: CreateExportRequestApplicationJsonPropertyParameters? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateExportResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        create(type, format, parameters, capturingOptions)
        return captured as? RawResponse<SdkCreateExportResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [create]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [create] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createSuspend")
    suspend fun createSuspend(
        type: SdkCreateExportRequestApplicationJsonType,
        format: SdkCreateExportRequestApplicationJsonFormat,
        parameters: CreateExportRequestApplicationJsonPropertyParameters? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateExportResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            create(type, format, parameters, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createWithResponseSuspend")
    suspend fun createWithResponseSuspend(
        type: SdkCreateExportRequestApplicationJsonType,
        format: SdkCreateExportRequestApplicationJsonFormat,
        parameters: CreateExportRequestApplicationJsonPropertyParameters? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateExportResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createWithResponse(type, format, parameters, requestOptions)
        }

    /**
     * Retrieve an export
     *
     * Poll until completed, failed, or expired. OAuth callers use the admin grant and select a
     * store with X-STORE. Current membership and role permissions apply to every request. Sales,
     * tax and customer reports require invoice store permission. Affiliate payout reports require
     * affiliate and affiliate:payout permissions. The category selects the applicable permission
     * checks; listing returns only authorized categories.
     *
     * @param export The export identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetExportResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        export: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetExportResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/exports/${encodePathSegment(export)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetExportResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getExport",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetExportResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        export: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetExportResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(export, capturingOptions)
        return captured as? RawResponse<SdkGetExportResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        export: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetExportResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(export, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        export: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetExportResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(export, requestOptions)
        }

    /**
     * Download an export
     *
     * Validate the five-minute signed API URL, then redirect to a short-lived private
     * object-storage URL. The API does not stream the file. Pending or running exports return 409;
     * expired exports return 410. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request. Sales, tax and
     * customer reports require invoice store permission. Affiliate payout reports require affiliate
     * and affiliate:payout permissions. The category selects the applicable permission checks;
     * listing returns only authorized categories.
     *
     * @param export The export identifier.
     * @param expires Expiry timestamp from the generated download URL.
     * @param signature Copy the signature from the response-supplied download_url. The example is
     *   illustrative and cannot authorize a download.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun download(
        export: String,
        expires: Long,
        signature: String,
        requestOptions: RequestOptions? = null,
    ) {
        val params = mutableListOf<Pair<String, String>>()
        params += "expires" to expires.toString()
        params += "signature" to signature
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/exports/${encodePathSegment(export)}/download",
                queryParams = params,
                requestOptions = requestOptions,
                responseSchema = runtimeSchema("{\"kind\":\"primitive\",\"type\":\"unknown\"}"),
                operationId = "downloadExport",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun downloadWithResponse(
        export: String,
        expires: Long,
        signature: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        download(export, expires, signature, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [download]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [download] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("downloadSuspend")
    suspend fun downloadSuspend(
        export: String,
        expires: Long,
        signature: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            download(export, expires, signature, requestOptions)
        }

    /**
     * Coroutine-aware variant of [downloadWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [downloadWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("downloadWithResponseSuspend")
    suspend fun downloadWithResponseSuspend(
        export: String,
        expires: Long,
        signature: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            downloadWithResponse(export, expires, signature, requestOptions)
        }
}
