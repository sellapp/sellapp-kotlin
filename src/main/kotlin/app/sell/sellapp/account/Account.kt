// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.account

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkCreateStoreResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkGetAccountResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkGetStorePermissionsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkGetStoreResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkListStoresResponseValue200ApplicationJson
import app.sell.sellapp.types.StoreVisibility

/**
 * API accessor for Account.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class Account(internal val sellapp: SellApp) {
    /**
     * Read your profile
     *
     * Read your account identity and credential type with account:read on an API key or the
     * official CLI OAuth grant. No store or X-STORE is needed, including before creating your first
     * store. Browser and customer sessions are rejected.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetAccountResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(requestOptions: RequestOptions? = null): SdkGetAccountResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/me",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetAccountResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getAccount",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetAccountResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetAccountResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(capturingOptions)
        return captured as? RawResponse<SdkGetAccountResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        requestOptions: RequestOptions? = null
    ): SdkGetAccountResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetAccountResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(requestOptions)
        }

    /**
     * List accessible stores
     *
     * List all stores you can currently access, including stores created or joined after login. API
     * keys require account:read; selected-store keys see only the intersection of current
     * membership and their fixed store list. An empty list is a successful result. Use a returned
     * slug in X-STORE for store operations.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkListStoresResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun listStores(
        requestOptions: RequestOptions? = null
    ): SdkListStoresResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/stores",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkListStoresResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "listStores",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkListStoresResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun listStoresWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkListStoresResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        listStores(capturingOptions)
        return captured as? RawResponse<SdkListStoresResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [listStores]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [listStores] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listStoresSuspend")
    suspend fun listStoresSuspend(
        requestOptions: RequestOptions? = null
    ): SdkListStoresResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            listStores(requestOptions)
        }

    /**
     * Coroutine-aware variant of [listStoresWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listStoresWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listStoresWithResponseSuspend")
    suspend fun listStoresWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkListStoresResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            listStoresWithResponse(requestOptions)
        }

    /**
     * Create a store
     *
     * Create a store owned by the authenticated user, with the same defaults as dashboard creation.
     * Requires stores:create on an unrestricted user-owned API key, or the official CLI OAuth admin
     * grant. Customer and browser sessions and direct MCP bearer tokens are rejected; MCP uses
     * reviewed server delegation. Selected-store keys cannot create stores. No existing store or
     * X-STORE header is needed; Name and slug are required; slug is normalized to lowercase.
     * Visibility defaults to PUBLIC. This does not change your dashboard's selected store.
     * Idempotency-Key is required and retained for 24 hours: reuse the same key and identical body
     * after a lost response. A conflicting request returns 409; invalid or duplicate slugs
     * return 422. Retries are bound to the user and operation across token rotation and supported
     * credential types; current authorization is checked before replay. Store defaults and the
     * successful retry receipt commit together. You can create one new store per user every 60
     * seconds, across all credentials. A new creation during that interval returns 429 with
     * Retry-After and rate-limit headers. Wait for that delay and retry the same key and body.
     * Replaying a successful original key recovers its store without creating another or consuming
     * another creation allowance.
     *
     * @param name the name of the request.
     * @param slug Lowercased before validation. Reserved infrastructure names such as api, www and
     *   cdn are unavailable.
     * @param visibility the visibility of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateStoreResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun createStore(
        name: String,
        slug: String,
        visibility: StoreVisibility? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateStoreResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "name" to name,
                "slug" to slug,
                "visibility" to visibility,
            )
        body["name"] = name
        body["slug"] = slug
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/stores",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateStoreRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateStoreResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "createStore",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateStoreResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createStoreWithResponse(
        name: String,
        slug: String,
        visibility: StoreVisibility? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateStoreResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        createStore(name, slug, visibility, capturingOptions)
        return captured as? RawResponse<SdkCreateStoreResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [createStore]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [createStore] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createStoreSuspend")
    suspend fun createStoreSuspend(
        name: String,
        slug: String,
        visibility: StoreVisibility? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateStoreResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            createStore(name, slug, visibility, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createStoreWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createStoreWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createStoreWithResponseSuspend")
    suspend fun createStoreWithResponseSuspend(
        name: String,
        slug: String,
        visibility: StoreVisibility? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateStoreResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createStoreWithResponse(name, slug, visibility, requestOptions)
        }

    /**
     * Read an accessible store
     *
     * Read a currently accessible store by numeric ID with account:read on an API key or official
     * CLI OAuth. No X-STORE is needed. Keys with selected stores must include this ID. Inaccessible
     * stores return 404.
     *
     * @param store the store of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetStoreResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun getStore(
        store: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetStoreResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/stores/${encodePathSegment(store)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetStoreResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getStore",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetStoreResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getStoreWithResponse(
        store: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetStoreResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        getStore(store, capturingOptions)
        return captured as? RawResponse<SdkGetStoreResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [getStore]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [getStore] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getStoreSuspend")
    suspend fun getStoreSuspend(
        store: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetStoreResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            getStore(store, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getStoreWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [getStoreWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getStoreWithResponseSuspend")
    suspend fun getStoreWithResponseSuspend(
        store: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetStoreResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getStoreWithResponse(store, requestOptions)
        }

    /**
     * Inspect effective permissions
     *
     * Inspect authorization for the explicit X-STORE slug. API keys require account:read and return
     * key_abilities, store_permissions, effective_abilities and store_access. OAuth returns its
     * granted scopes and reviewed permission categories. The credential_type identifies the typed
     * response. These checks do not establish record-state, provider or financial eligibility.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetStorePermissionsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun getStorePermissions(
        requestOptions: RequestOptions? = null
    ): SdkGetStorePermissionsResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/permissions",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetStorePermissionsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getStorePermissions",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetStorePermissionsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getStorePermissionsWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetStorePermissionsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        getStorePermissions(capturingOptions)
        return captured as? RawResponse<SdkGetStorePermissionsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [getStorePermissions]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [getStorePermissions] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getStorePermissionsSuspend")
    suspend fun getStorePermissionsSuspend(
        requestOptions: RequestOptions? = null
    ): SdkGetStorePermissionsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            getStorePermissions(requestOptions)
        }

    /**
     * Coroutine-aware variant of [getStorePermissionsWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [getStorePermissionsWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("getStorePermissionsWithResponseSuspend")
    suspend fun getStorePermissionsWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetStorePermissionsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getStorePermissionsWithResponse(requestOptions)
        }
}
