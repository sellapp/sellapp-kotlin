// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.variantserials

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.addIfNotNull
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.SdkAppendVariantSerialInventoryResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkQueueVariantSerialImportResponseValue202ApplicationJson
import app.sell.sellapp.models.SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson
import app.sell.sellapp.types.SdkQueueVariantSerialImportRequestMultipartFormDataMode
import app.sell.sellapp.types.SdkQueueVariantSerialImportRequestMultipartFormDataParsingMode
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for VariantSerials.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class VariantSerials(internal val sellapp: SellApp) {
    /**
     * List variant serial inventory
     *
     * Lists unsold serial inventory for a variant in the current store. Requires the `listing`
     * Sanctum ability. Serial metadata is not exposed. OAuth callers use the admin grant and select
     * a store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param product The product path parameter.
     * @param variant The variant path parameter.
     * @param search Filter inventory by a partial serial value.
     * @param limit Number of inventory entries per page.
     * @param page Page number to return.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        product: String,
        variant: String,
        search: String? = null,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<
                    ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem
                >() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path =
                "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/serials",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listVariantSerialInventory",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models
                    .ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            addIfNotNull("search", search)
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        product: String,
        variant: String,
        search: String? = null,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<
        Page<ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem>
    > {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(product, variant, search, limit, page, capturingOptions)
        return captured
            as?
            RawResponse<
                Page<ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem>
            > ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        product: String,
        variant: String,
        search: String? = null,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            list(product, variant, search, limit, page, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        product: String,
        variant: String,
        search: String? = null,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<
        Page<ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem>
    > =
        sellapp.baseClient.runCancellable {
            listWithResponse(product, variant, search, limit, page, requestOptions)
        }

    /**
     * Append variant serial inventory
     *
     * Atomically appends unsold serials and recalculates variant stock. Requires the `listing`
     * Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param product The product path parameter.
     * @param variant The variant path parameter.
     * @param serials Serial values to append or use as the replacement inventory.
     * @param removeDuplicates Remove duplicate values from this submitted payload.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkAppendVariantSerialInventoryResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun append(
        product: String,
        variant: String,
        serials: List<String>,
        removeDuplicates: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): SdkAppendVariantSerialInventoryResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "serials" to serials,
                "remove_duplicates" to removeDuplicates,
            )
        body["serials"] = serials
        val config =
            RequestConfig(
                method = "POST",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/serials",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkAppendVariantSerialInventoryRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkAppendVariantSerialInventoryResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "appendVariantSerialInventory",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkAppendVariantSerialInventoryResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun appendWithResponse(
        product: String,
        variant: String,
        serials: List<String>,
        removeDuplicates: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkAppendVariantSerialInventoryResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        append(product, variant, serials, removeDuplicates, capturingOptions)
        return captured
            as? RawResponse<SdkAppendVariantSerialInventoryResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [append]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [append] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("appendSuspend")
    suspend fun appendSuspend(
        product: String,
        variant: String,
        serials: List<String>,
        removeDuplicates: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): SdkAppendVariantSerialInventoryResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            append(product, variant, serials, removeDuplicates, requestOptions)
        }

    /**
     * Coroutine-aware variant of [appendWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [appendWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("appendWithResponseSuspend")
    suspend fun appendWithResponseSuspend(
        product: String,
        variant: String,
        serials: List<String>,
        removeDuplicates: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkAppendVariantSerialInventoryResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            appendWithResponse(product, variant, serials, removeDuplicates, requestOptions)
        }

    /**
     * Replace variant serial inventory
     *
     * Atomically replaces all unsold serials and recalculates variant stock. Concurrent inventory
     * mutations are serialized. Requires the `listing` Sanctum ability. OAuth callers use the admin
     * grant and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param product The product path parameter.
     * @param variant The variant path parameter.
     * @param serials Serial values to append or use as the replacement inventory.
     * @param removeDuplicates Remove duplicate values from this submitted payload.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        product: String,
        variant: String,
        serials: List<String>,
        removeDuplicates: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "serials" to serials,
                "remove_duplicates" to removeDuplicates,
            )
        body["serials"] = serials
        val config =
            RequestConfig(
                method = "PUT",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/serials",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkReplaceVariantSerialInventoryRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceVariantSerialInventory",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        product: String,
        variant: String,
        serials: List<String>,
        removeDuplicates: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(product, variant, serials, removeDuplicates, capturingOptions)
        return captured
            as? RawResponse<SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        product: String,
        variant: String,
        serials: List<String>,
        removeDuplicates: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(product, variant, serials, removeDuplicates, requestOptions)
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        product: String,
        variant: String,
        serials: List<String>,
        removeDuplicates: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(product, variant, serials, removeDuplicates, requestOptions)
        }

    /**
     * Queue a variant serial import
     *
     * Uploads a text file to application-owned temporary storage and queues an atomic serial
     * import. Use this endpoint for large imports. Requires the `listing` Sanctum ability. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param product The product path parameter.
     * @param variant The variant path parameter.
     * @param file A text file up to 100 MiB.
     * @param parsingMode How the uploaded file is split into serials. CUSTOM requires
     *   custom_delimiter; sending CUSTOM without it fails validation with a 422.
     * @param customDelimiter The delimiter to split on. Required when parsing_mode is CUSTOM
     *   (omitting it fails validation with a 422); ignored otherwise.
     * @param removeDuplicates the remove duplicates of the request.
     * @param mode the mode of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkQueueVariantSerialImportResponseValue202ApplicationJson
     */
    @JvmOverloads
    fun queue(
        product: String,
        variant: String,
        file: ByteArray,
        parsingMode: SdkQueueVariantSerialImportRequestMultipartFormDataParsingMode,
        customDelimiter: String? = null,
        removeDuplicates: Boolean? = null,
        mode: SdkQueueVariantSerialImportRequestMultipartFormDataMode? = null,
        requestOptions: RequestOptions? = null,
    ): SdkQueueVariantSerialImportResponseValue202ApplicationJson {
        val body =
            bodyOf(
                "file" to file,
                "parsing_mode" to parsingMode,
                "custom_delimiter" to customDelimiter,
                "remove_duplicates" to removeDuplicates,
                "mode" to mode,
            )
        body["file"] = file
        body["parsing_mode"] = parsingMode
        val config =
            RequestConfig(
                method = "POST",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/serials/import",
                body = body,
                bodyEncoding = "form-data",
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkQueueVariantSerialImportRequestMultipartFormData
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkQueueVariantSerialImportResponseValue202ApplicationJson
                        .runtimeSchema,
                operationId = "queueVariantSerialImport",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkQueueVariantSerialImportResponseValue202ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun queueWithResponse(
        product: String,
        variant: String,
        file: ByteArray,
        parsingMode: SdkQueueVariantSerialImportRequestMultipartFormDataParsingMode,
        customDelimiter: String? = null,
        removeDuplicates: Boolean? = null,
        mode: SdkQueueVariantSerialImportRequestMultipartFormDataMode? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkQueueVariantSerialImportResponseValue202ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        queue(
            product,
            variant,
            file,
            parsingMode,
            customDelimiter,
            removeDuplicates,
            mode,
            capturingOptions,
        )
        return captured as? RawResponse<SdkQueueVariantSerialImportResponseValue202ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [queue]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [queue] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("queueSuspend")
    suspend fun queueSuspend(
        product: String,
        variant: String,
        file: ByteArray,
        parsingMode: SdkQueueVariantSerialImportRequestMultipartFormDataParsingMode,
        customDelimiter: String? = null,
        removeDuplicates: Boolean? = null,
        mode: SdkQueueVariantSerialImportRequestMultipartFormDataMode? = null,
        requestOptions: RequestOptions? = null,
    ): SdkQueueVariantSerialImportResponseValue202ApplicationJson =
        sellapp.baseClient.runCancellable {
            queue(
                product,
                variant,
                file,
                parsingMode,
                customDelimiter,
                removeDuplicates,
                mode,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [queueWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [queueWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("queueWithResponseSuspend")
    suspend fun queueWithResponseSuspend(
        product: String,
        variant: String,
        file: ByteArray,
        parsingMode: SdkQueueVariantSerialImportRequestMultipartFormDataParsingMode,
        customDelimiter: String? = null,
        removeDuplicates: Boolean? = null,
        mode: SdkQueueVariantSerialImportRequestMultipartFormDataMode? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkQueueVariantSerialImportResponseValue202ApplicationJson> =
        sellapp.baseClient.runCancellable {
            queueWithResponse(
                product,
                variant,
                file,
                parsingMode,
                customDelimiter,
                removeDuplicates,
                mode,
                requestOptions,
            )
        }

    /**
     * Delete a variant serial
     *
     * Deletes one unsold serial scoped to the current store, product, and variant, then
     * recalculates stock. Requires the `listing` Sanctum ability. OAuth callers use the admin grant
     * and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param product The product path parameter.
     * @param variant The variant path parameter.
     * @param serial The unsold serial entry ULID.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun delete(
        product: String,
        variant: String,
        serial: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/serials/${encodePathSegment(serial)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkDeleteVariantSerialResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "deleteVariantSerial",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun deleteWithResponse(
        product: String,
        variant: String,
        serial: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        delete(product, variant, serial, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [delete]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [delete] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteSuspend")
    suspend fun deleteSuspend(
        product: String,
        variant: String,
        serial: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            delete(product, variant, serial, requestOptions)
        }

    /**
     * Coroutine-aware variant of [deleteWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [deleteWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteWithResponseSuspend")
    suspend fun deleteWithResponseSuspend(
        product: String,
        variant: String,
        serial: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            deleteWithResponse(product, variant, serial, requestOptions)
        }
}
