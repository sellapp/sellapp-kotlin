// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.cashbackrules

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.PatchField
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.http.patchBodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkCreateCashbackRuleResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkListCashbackRulesResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkReplaceCashbackRuleResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkRestoreCashbackRuleResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkUpdateCashbackRuleResponseValue200ApplicationJson

/**
 * API accessor for CashbackRules.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class CashbackRules(internal val sellapp: SellApp) {
    /**
     * List cashback rules
     *
     * List current and archived store records. Requires the `wallet` or `wallet:write` API ability.
     * OAuth callers use the admin grant and select a store with X-STORE. Current membership and
     * role permissions apply to every request.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkListCashbackRulesResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun list(
        requestOptions: RequestOptions? = null
    ): SdkListCashbackRulesResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/cashback-rules",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkListCashbackRulesResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "listCashbackRules",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkListCashbackRulesResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkListCashbackRulesResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(capturingOptions)
        return captured as? RawResponse<SdkListCashbackRulesResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        requestOptions: RequestOptions? = null
    ): SdkListCashbackRulesResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            list(requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkListCashbackRulesResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            listWithResponse(requestOptions)
        }

    /**
     * Create a cashback rule
     *
     * Create a cashback rule. Requires the `wallet:write` API ability and the store wallet
     * permission. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param percentBasis the percent basis of the request.
     * @param maximumCashbackCents the maximum cashback cents of the request.
     * @param isActive the is active of the request.
     * @param productIds the product ids of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateCashbackRuleResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun create(
        percentBasis: Long,
        maximumCashbackCents: Long?,
        isActive: Boolean,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkCreateCashbackRuleResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "percent_basis" to percentBasis,
                "maximum_cashback_cents" to maximumCashbackCents,
                "is_active" to isActive,
                "product_ids" to productIds,
            )
        body["percent_basis"] = percentBasis
        body["maximum_cashback_cents"] = maximumCashbackCents
        body["is_active"] = isActive
        body["product_ids"] = productIds
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/cashback-rules",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateCashbackRuleRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateCashbackRuleResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "createCashbackRule",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateCashbackRuleResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createWithResponse(
        percentBasis: Long,
        maximumCashbackCents: Long?,
        isActive: Boolean,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateCashbackRuleResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        create(percentBasis, maximumCashbackCents, isActive, productIds, capturingOptions)
        return captured as? RawResponse<SdkCreateCashbackRuleResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [create]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [create] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createSuspend")
    suspend fun createSuspend(
        percentBasis: Long,
        maximumCashbackCents: Long?,
        isActive: Boolean,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkCreateCashbackRuleResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            create(percentBasis, maximumCashbackCents, isActive, productIds, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createWithResponseSuspend")
    suspend fun createWithResponseSuspend(
        percentBasis: Long,
        maximumCashbackCents: Long?,
        isActive: Boolean,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateCashbackRuleResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createWithResponse(
                percentBasis,
                maximumCashbackCents,
                isActive,
                productIds,
                requestOptions,
            )
        }

    /**
     * Update a cashback rule
     *
     * Update a cashback rule. Requires the `wallet:write` API ability and the store wallet
     * permission. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param cashbackRule The cashbackRule path parameter.
     * @param percentBasis the percent basis of the request.
     * @param maximumCashbackCents the maximum cashback cents of the request.
     * @param isActive the is active of the request.
     * @param productIds the product ids of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceCashbackRuleResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        cashbackRule: String,
        percentBasis: Long,
        maximumCashbackCents: Long?,
        isActive: Boolean,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceCashbackRuleResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "percent_basis" to percentBasis,
                "maximum_cashback_cents" to maximumCashbackCents,
                "is_active" to isActive,
                "product_ids" to productIds,
            )
        body["percent_basis"] = percentBasis
        body["maximum_cashback_cents"] = maximumCashbackCents
        body["is_active"] = isActive
        body["product_ids"] = productIds
        val config =
            RequestConfig(
                method = "PUT",
                path = "/v2/cashback-rules/${encodePathSegment(cashbackRule)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkReplaceCashbackRuleRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkReplaceCashbackRuleResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceCashbackRule",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceCashbackRuleResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        cashbackRule: String,
        percentBasis: Long,
        maximumCashbackCents: Long?,
        isActive: Boolean,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceCashbackRuleResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(
            cashbackRule,
            percentBasis,
            maximumCashbackCents,
            isActive,
            productIds,
            capturingOptions,
        )
        return captured as? RawResponse<SdkReplaceCashbackRuleResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        cashbackRule: String,
        percentBasis: Long,
        maximumCashbackCents: Long?,
        isActive: Boolean,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceCashbackRuleResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(
                cashbackRule,
                percentBasis,
                maximumCashbackCents,
                isActive,
                productIds,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        cashbackRule: String,
        percentBasis: Long,
        maximumCashbackCents: Long?,
        isActive: Boolean,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceCashbackRuleResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(
                cashbackRule,
                percentBasis,
                maximumCashbackCents,
                isActive,
                productIds,
                requestOptions,
            )
        }

    /**
     * Update a cashback rule
     *
     * Update a cashback rule. Requires the `wallet:write` API ability and the store wallet
     * permission. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param cashbackRule The cashbackRule path parameter.
     * @param percentBasis the percent basis of the request.
     * @param maximumCashbackCents the maximum cashback cents of the request.
     * @param isActive the is active of the request.
     * @param productIds the product ids of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkUpdateCashbackRuleResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun update(
        cashbackRule: String,
        percentBasis: PatchField<Long> = PatchField.Absent,
        maximumCashbackCents: PatchField<Long?> = PatchField.Absent,
        isActive: PatchField<Boolean> = PatchField.Absent,
        productIds: PatchField<List<Long>> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateCashbackRuleResponseValue200ApplicationJson {
        val body =
            patchBodyOf(
                "percent_basis" to percentBasis,
                "maximum_cashback_cents" to maximumCashbackCents,
                "is_active" to isActive,
                "product_ids" to productIds,
            )
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v2/cashback-rules/${encodePathSegment(cashbackRule)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkUpdateCashbackRuleRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkUpdateCashbackRuleResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "updateCashbackRule",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkUpdateCashbackRuleResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun updateWithResponse(
        cashbackRule: String,
        percentBasis: PatchField<Long> = PatchField.Absent,
        maximumCashbackCents: PatchField<Long?> = PatchField.Absent,
        isActive: PatchField<Boolean> = PatchField.Absent,
        productIds: PatchField<List<Long>> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateCashbackRuleResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        update(
            cashbackRule,
            percentBasis,
            maximumCashbackCents,
            isActive,
            productIds,
            capturingOptions,
        )
        return captured as? RawResponse<SdkUpdateCashbackRuleResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [update]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [update] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateSuspend")
    suspend fun updateSuspend(
        cashbackRule: String,
        percentBasis: PatchField<Long> = PatchField.Absent,
        maximumCashbackCents: PatchField<Long?> = PatchField.Absent,
        isActive: PatchField<Boolean> = PatchField.Absent,
        productIds: PatchField<List<Long>> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateCashbackRuleResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            update(
                cashbackRule,
                percentBasis,
                maximumCashbackCents,
                isActive,
                productIds,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [updateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [updateWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateWithResponseSuspend")
    suspend fun updateWithResponseSuspend(
        cashbackRule: String,
        percentBasis: PatchField<Long> = PatchField.Absent,
        maximumCashbackCents: PatchField<Long?> = PatchField.Absent,
        isActive: PatchField<Boolean> = PatchField.Absent,
        productIds: PatchField<List<Long>> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateCashbackRuleResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            updateWithResponse(
                cashbackRule,
                percentBasis,
                maximumCashbackCents,
                isActive,
                productIds,
                requestOptions,
            )
        }

    /**
     * Archive a cashback rule
     *
     * Archive a cashback rule. Requires the `wallet:write` API ability and the store wallet
     * permission. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param cashbackRule The cashbackRule path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun archive(
        cashbackRule: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/cashback-rules/${encodePathSegment(cashbackRule)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkArchiveCashbackRuleResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "archiveCashbackRule",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun archiveWithResponse(
        cashbackRule: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        archive(cashbackRule, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [archive]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [archive] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("archiveSuspend")
    suspend fun archiveSuspend(
        cashbackRule: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            archive(cashbackRule, requestOptions)
        }

    /**
     * Coroutine-aware variant of [archiveWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [archiveWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("archiveWithResponseSuspend")
    suspend fun archiveWithResponseSuspend(
        cashbackRule: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            archiveWithResponse(cashbackRule, requestOptions)
        }

    /**
     * Restore a cashback rule
     *
     * Restore a cashback rule. Requires the `wallet:write` API ability and the store wallet
     * permission. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param cashbackRule The cashbackRule path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkRestoreCashbackRuleResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun restore(
        cashbackRule: String,
        requestOptions: RequestOptions? = null,
    ): SdkRestoreCashbackRuleResponseValue200ApplicationJson {
        val body = linkedMapOf<String, Any?>()
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/cashback-rules/${encodePathSegment(cashbackRule)}/restore",
                body = body,
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkRestoreCashbackRuleResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "restoreCashbackRule",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkRestoreCashbackRuleResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun restoreWithResponse(
        cashbackRule: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkRestoreCashbackRuleResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        restore(cashbackRule, capturingOptions)
        return captured as? RawResponse<SdkRestoreCashbackRuleResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [restore]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [restore] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("restoreSuspend")
    suspend fun restoreSuspend(
        cashbackRule: String,
        requestOptions: RequestOptions? = null,
    ): SdkRestoreCashbackRuleResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            restore(cashbackRule, requestOptions)
        }

    /**
     * Coroutine-aware variant of [restoreWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [restoreWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("restoreWithResponseSuspend")
    suspend fun restoreWithResponseSuspend(
        cashbackRule: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkRestoreCashbackRuleResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            restoreWithResponse(cashbackRule, requestOptions)
        }
}
