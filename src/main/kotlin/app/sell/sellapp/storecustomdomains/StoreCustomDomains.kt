// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.storecustomdomains

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkConnectCustomDomainResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkGetCustomDomainResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkListCustomDomainsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkRefreshCustomDomainStatusResponseValue200ApplicationJson

/**
 * API accessor for StoreCustomDomains.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class StoreCustomDomains(internal val sellapp: SellApp) {
    /**
     * List custom domains
     *
     * List the current store's custom-domain connection states and DNS verification records. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkListCustomDomainsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun list(
        requestOptions: RequestOptions? = null
    ): SdkListCustomDomainsResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/store/custom-domains",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkListCustomDomainsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "listCustomDomains",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkListCustomDomainsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkListCustomDomainsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(capturingOptions)
        return captured as? RawResponse<SdkListCustomDomainsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        requestOptions: RequestOptions? = null
    ): SdkListCustomDomainsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            list(requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkListCustomDomainsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            listWithResponse(requestOptions)
        }

    /**
     * Connect a custom domain
     *
     * Connect a hostname through SellApp's managed Cloudflare custom-hostname flow. IP addresses,
     * wildcard domains, credentials, ports, queries, and fragments are rejected. OAuth callers use
     * the admin grant and select a store with X-STORE. Current membership and role permissions
     * apply to every request.
     *
     * @param domain The custom domain as a hostname or URL.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkConnectCustomDomainResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun connect(
        domain: String,
        requestOptions: RequestOptions? = null,
    ): SdkConnectCustomDomainResponseValue201ApplicationJson {
        val body = bodyOf("domain" to domain)
        body["domain"] = domain
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/store/custom-domains",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkConnectCustomDomainRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkConnectCustomDomainResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "connectCustomDomain",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkConnectCustomDomainResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun connectWithResponse(
        domain: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkConnectCustomDomainResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        connect(domain, capturingOptions)
        return captured as? RawResponse<SdkConnectCustomDomainResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [connect]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [connect] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("connectSuspend")
    suspend fun connectSuspend(
        domain: String,
        requestOptions: RequestOptions? = null,
    ): SdkConnectCustomDomainResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            connect(domain, requestOptions)
        }

    /**
     * Coroutine-aware variant of [connectWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [connectWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("connectWithResponseSuspend")
    suspend fun connectWithResponseSuspend(
        domain: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkConnectCustomDomainResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            connectWithResponse(domain, requestOptions)
        }

    /**
     * Retrieve a custom domain
     *
     * Retrieve a custom domain. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param customDomain The customDomain path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetCustomDomainResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetCustomDomainResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/store/custom-domains/${encodePathSegment(customDomain)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetCustomDomainResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getCustomDomain",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetCustomDomainResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetCustomDomainResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(customDomain, capturingOptions)
        return captured as? RawResponse<SdkGetCustomDomainResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetCustomDomainResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(customDomain, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetCustomDomainResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(customDomain, requestOptions)
        }

    /**
     * Disconnect a custom domain
     *
     * Disconnect a custom domain. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param customDomain The customDomain path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun disconnect(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/store/custom-domains/${encodePathSegment(customDomain)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkDisconnectCustomDomainResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "disconnectCustomDomain",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun disconnectWithResponse(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        disconnect(customDomain, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [disconnect]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [disconnect] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("disconnectSuspend")
    suspend fun disconnectSuspend(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            disconnect(customDomain, requestOptions)
        }

    /**
     * Coroutine-aware variant of [disconnectWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [disconnectWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("disconnectWithResponseSuspend")
    suspend fun disconnectWithResponseSuspend(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            disconnectWithResponse(customDomain, requestOptions)
        }

    /**
     * Refresh custom domain status
     *
     * Ask Cloudflare to re-run custom-hostname validation and persist the latest DNS and SSL state.
     * OAuth callers use the admin grant and select a store with X-STORE. Current membership and
     * role permissions apply to every request.
     *
     * @param customDomain The customDomain path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkRefreshCustomDomainStatusResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun refresh(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ): SdkRefreshCustomDomainStatusResponseValue200ApplicationJson {
        val body = linkedMapOf<String, Any?>()
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/store/custom-domains/${encodePathSegment(customDomain)}/refresh",
                body = body,
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkRefreshCustomDomainStatusResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "refreshCustomDomainStatus",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkRefreshCustomDomainStatusResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun refreshWithResponse(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkRefreshCustomDomainStatusResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        refresh(customDomain, capturingOptions)
        return captured as? RawResponse<SdkRefreshCustomDomainStatusResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [refresh]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [refresh] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("refreshSuspend")
    suspend fun refreshSuspend(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ): SdkRefreshCustomDomainStatusResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            refresh(customDomain, requestOptions)
        }

    /**
     * Coroutine-aware variant of [refreshWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [refreshWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("refreshWithResponseSuspend")
    suspend fun refreshWithResponseSuspend(
        customDomain: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkRefreshCustomDomainStatusResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            refreshWithResponse(customDomain, requestOptions)
        }
}
