// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.groupsproducts

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.ListProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.SdkAddProductsToGroupResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkGetProductWithinGroupResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkSyncGroupProductsResponseValue200ApplicationJson
import app.sell.sellapp.models.SearchProductsWithinGroupRequestApplicationJsonPropertyFiltersItem
import app.sell.sellapp.models.SearchProductsWithinGroupRequestApplicationJsonPropertyIncludesItem
import app.sell.sellapp.models.SearchProductsWithinGroupRequestApplicationJsonPropertySearch
import app.sell.sellapp.models.SearchProductsWithinGroupRequestApplicationJsonPropertySortItem
import app.sell.sellapp.models.SearchProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for GroupsProducts.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class GroupsProducts(internal val sellapp: SellApp) {
    /**
     * Add products to group
     *
     * Add existing products to a group. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param group The group path parameter.
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkAddProductsToGroupResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun add(
        group: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkAddProductsToGroupResponseValue200ApplicationJson {
        val body = bodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/groups/${encodePathSegment(group)}/products/attach",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkAddProductsToGroupRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkAddProductsToGroupResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "addProductsToGroup",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkAddProductsToGroupResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun addWithResponse(
        group: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkAddProductsToGroupResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        add(group, resources, capturingOptions)
        return captured as? RawResponse<SdkAddProductsToGroupResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [add]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [add] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("addSuspend")
    suspend fun addSuspend(
        group: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkAddProductsToGroupResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            add(group, resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [addWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [addWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("addWithResponseSuspend")
    suspend fun addWithResponseSuspend(
        group: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkAddProductsToGroupResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            addWithResponse(group, resources, requestOptions)
        }

    /**
     * Remove products from group
     *
     * Remove products from a group without deleting the products. OAuth callers use the admin grant
     * and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param group The group path parameter.
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun remove(
        group: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ) {
        val body = bodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/groups/${encodePathSegment(group)}/products/detach",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkRemoveProductsFromGroupRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkRemoveProductsFromGroupResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "removeProductsFromGroup",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun removeWithResponse(
        group: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        remove(group, resources, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [remove]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [remove] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("removeSuspend")
    suspend fun removeSuspend(
        group: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            remove(group, resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [removeWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [removeWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("removeWithResponseSuspend")
    suspend fun removeWithResponseSuspend(
        group: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            removeWithResponse(group, resources, requestOptions)
        }

    /**
     * List all products within group
     *
     * List the products in a group, 15 per page by default. OAuth callers use the admin grant and
     * select a store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param group The group path parameter.
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        group: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<
                    ListProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem
                >() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/groups/${encodePathSegment(group)}/products",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listProductsWithinGroup",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models
                    .ListProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        group: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(group, limit, page, capturingOptions)
        return captured
            as?
            RawResponse<
                Page<ListProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem>
            > ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        group: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            list(group, limit, page, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        group: String,
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(group, limit, page, requestOptions)
        }

    /**
     * Replace ordered group products
     *
     * Replace a group's complete product membership in one transaction. Requires the group API-key
     * ability and store group permission. The order of product_ids becomes display order. An empty
     * array clears the group. Missing or draft products return 422; products from another store are
     * rejected. Retrying the same list has the same result. Private products remain assigned but
     * are omitted from the returned group summary. OAuth callers use the admin grant and select a
     * store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param group the group of the request.
     * @param productIds Ordered IDs of published standard products in this store. Supply an empty
     *   array to remove every product. Missing, draft, or foreign products are rejected.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkSyncGroupProductsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        group: String,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkSyncGroupProductsResponseValue200ApplicationJson {
        val body = bodyOf("product_ids" to productIds)
        body["product_ids"] = productIds
        val config =
            RequestConfig(
                method = "PUT",
                path = "/v2/groups/${encodePathSegment(group)}/products",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkSyncGroupProductsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkSyncGroupProductsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "syncGroupProducts",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkSyncGroupProductsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        group: String,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkSyncGroupProductsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(group, productIds, capturingOptions)
        return captured as? RawResponse<SdkSyncGroupProductsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        group: String,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkSyncGroupProductsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(group, productIds, requestOptions)
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        group: String,
        productIds: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkSyncGroupProductsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(group, productIds, requestOptions)
        }

    /**
     * List specific product within group
     *
     * Retrieve one product from a group. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param group The group path parameter.
     * @param product The product path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetProductWithinGroupResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        group: String,
        product: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetProductWithinGroupResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path =
                    "/v2/groups/${encodePathSegment(group)}/products/${encodePathSegment(product)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetProductWithinGroupResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getProductWithinGroup",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetProductWithinGroupResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        group: String,
        product: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetProductWithinGroupResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(group, product, capturingOptions)
        return captured as? RawResponse<SdkGetProductWithinGroupResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        group: String,
        product: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetProductWithinGroupResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(group, product, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        group: String,
        product: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetProductWithinGroupResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(group, product, requestOptions)
        }

    /**
     * Search products within group
     *
     * Search the products in a group using JSON body filters, search terms, includes, and sort
     * instructions. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param group The group path parameter.
     * @param filters the filters of the request.
     * @param sort the sort of the request.
     * @param search the search of the request.
     * @param includes the includes of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun search(
        group: String,
        filters: List<SearchProductsWithinGroupRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchProductsWithinGroupRequestApplicationJsonPropertySortItem>? = null,
        search: SearchProductsWithinGroupRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchProductsWithinGroupRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SearchProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<
                    SearchProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem
                >() {}
        return sellapp.baseClient.requestPage(
            method = "POST",
            path = "/v2/groups/${encodePathSegment(group)}/products/search",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "searchProductsWithinGroup",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models
                    .SearchProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {}
    }

    @Suppress("UNCHECKED_CAST")
    fun searchWithResponse(
        group: String,
        filters: List<SearchProductsWithinGroupRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchProductsWithinGroupRequestApplicationJsonPropertySortItem>? = null,
        search: SearchProductsWithinGroupRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchProductsWithinGroupRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SearchProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        search(group, filters, sort, search, includes, capturingOptions)
        return captured
            as?
            RawResponse<
                Page<SearchProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem>
            > ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [search]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [search] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchSuspend")
    suspend fun searchSuspend(
        group: String,
        filters: List<SearchProductsWithinGroupRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchProductsWithinGroupRequestApplicationJsonPropertySortItem>? = null,
        search: SearchProductsWithinGroupRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchProductsWithinGroupRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SearchProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            search(group, filters, sort, search, includes, requestOptions)
        }

    /**
     * Coroutine-aware variant of [searchWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [searchWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchWithResponseSuspend")
    suspend fun searchWithResponseSuspend(
        group: String,
        filters: List<SearchProductsWithinGroupRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchProductsWithinGroupRequestApplicationJsonPropertySortItem>? = null,
        search: SearchProductsWithinGroupRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchProductsWithinGroupRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SearchProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            searchWithResponse(group, filters, sort, search, includes, requestOptions)
        }
}
