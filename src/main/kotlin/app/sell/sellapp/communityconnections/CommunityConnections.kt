// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.communityconnections

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkCompleteCommunityConnectionResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkListCommunityConnectionsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkPollCommunityConnectionResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkStartCommunityConnectionResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkVerifyCommunityConnectionResponseValue200ApplicationJson
import app.sell.sellapp.types.SdkStartCommunityConnectionRequestApplicationJsonMode

/**
 * API accessor for CommunityConnections.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class CommunityConnections(internal val sellapp: SellApp) {
    /**
     * List community connections
     *
     * List Discord, Telegram, Slack, and WhatsApp connection and persisted health status for the
     * authenticated store without contacting providers. Credentials and provider secrets are never
     * returned. Requires the `community` Sanctum ability. OAuth callers use the admin grant and
     * select a store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkListCommunityConnectionsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun list(
        requestOptions: RequestOptions? = null
    ): SdkListCommunityConnectionsResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/community-connections",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkListCommunityConnectionsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "listCommunityConnections",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkListCommunityConnectionsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkListCommunityConnectionsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(capturingOptions)
        return captured as? RawResponse<SdkListCommunityConnectionsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        requestOptions: RequestOptions? = null
    ): SdkListCommunityConnectionsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            list(requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkListCommunityConnectionsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            listWithResponse(requestOptions)
        }

    /**
     * Start a community connection
     *
     * Start the provider-specific connection flow. OAuth providers return a connect URL containing
     * encrypted state bound to the authenticated actor, store, platform, and purpose; that state
     * expires after 15 minutes and securely authorizes the provider callback without forwarding the
     * API bearer token or requiring a SellApp dashboard session. Open the URL exactly as issued,
     * then poll the connection list with the original bearer token after the provider redirects. Do
     * not append credentials, reconstruct, or reuse the URL. Legacy unsigned Slack URLs are
     * rejected, so clients must call this endpoint again to restart those flows. Telegram returns a
     * short-lived verification token, and WhatsApp returns a pollable QR session. The provider
     * application must be configured, and Discord server connections require a connected Discord
     * account. Requires the `community` Sanctum ability. OAuth callers use the admin grant and
     * select a store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param platform The community platform.
     * @param mode Discord-only connection mode.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkStartCommunityConnectionResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun start(
        platform: String,
        mode: SdkStartCommunityConnectionRequestApplicationJsonMode? = null,
        requestOptions: RequestOptions? = null,
    ): SdkStartCommunityConnectionResponseValue200ApplicationJson {
        val body = bodyOf("mode" to mode)
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/community-connections/${encodePathSegment(platform)}/connect",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkStartCommunityConnectionRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkStartCommunityConnectionResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "startCommunityConnection",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkStartCommunityConnectionResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun startWithResponse(
        platform: String,
        mode: SdkStartCommunityConnectionRequestApplicationJsonMode? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkStartCommunityConnectionResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        start(platform, mode, capturingOptions)
        return captured as? RawResponse<SdkStartCommunityConnectionResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [start]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [start] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("startSuspend")
    suspend fun startSuspend(
        platform: String,
        mode: SdkStartCommunityConnectionRequestApplicationJsonMode? = null,
        requestOptions: RequestOptions? = null,
    ): SdkStartCommunityConnectionResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            start(platform, mode, requestOptions)
        }

    /**
     * Coroutine-aware variant of [startWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [startWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("startWithResponseSuspend")
    suspend fun startWithResponseSuspend(
        platform: String,
        mode: SdkStartCommunityConnectionRequestApplicationJsonMode? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkStartCommunityConnectionResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            startWithResponse(platform, mode, requestOptions)
        }

    /**
     * Poll a community connection
     *
     * Poll a pending Telegram verification or WhatsApp QR connection using the encrypted status
     * token returned by the connect operation. Tokens are bound to the authenticated store and
     * initiating user. Requires the `community` Sanctum ability. OAuth callers use the admin grant
     * and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param platform The community platform.
     * @param statusToken the status token of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkPollCommunityConnectionResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun poll(
        platform: String,
        statusToken: String,
        requestOptions: RequestOptions? = null,
    ): SdkPollCommunityConnectionResponseValue200ApplicationJson {
        val params = mutableListOf<Pair<String, String>>()
        params += "status_token" to statusToken
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/community-connections/${encodePathSegment(platform)}/status",
                queryParams = params,
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkPollCommunityConnectionResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "pollCommunityConnection",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkPollCommunityConnectionResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun pollWithResponse(
        platform: String,
        statusToken: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkPollCommunityConnectionResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        poll(platform, statusToken, capturingOptions)
        return captured as? RawResponse<SdkPollCommunityConnectionResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [poll]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [poll] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("pollSuspend")
    suspend fun pollSuspend(
        platform: String,
        statusToken: String,
        requestOptions: RequestOptions? = null,
    ): SdkPollCommunityConnectionResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            poll(platform, statusToken, requestOptions)
        }

    /**
     * Coroutine-aware variant of [pollWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [pollWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("pollWithResponseSuspend")
    suspend fun pollWithResponseSuspend(
        platform: String,
        statusToken: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkPollCommunityConnectionResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            pollWithResponse(platform, statusToken, requestOptions)
        }

    /**
     * Complete a community connection
     *
     * Complete an authenticated WhatsApp QR connection by selecting a group returned by the status
     * operation. The encrypted status token binds the session to the authenticated store and
     * initiating user, and the server ID is revalidated against the provider response. Requires the
     * `community` Sanctum ability. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param platform The community platform.
     * @param statusToken the status token of the request.
     * @param serverId the server id of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCompleteCommunityConnectionResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun complete(
        platform: String,
        statusToken: String,
        serverId: String,
        requestOptions: RequestOptions? = null,
    ): SdkCompleteCommunityConnectionResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "status_token" to statusToken,
                "server_id" to serverId,
            )
        body["status_token"] = statusToken
        body["server_id"] = serverId
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/community-connections/${encodePathSegment(platform)}/complete",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCompleteCommunityConnectionRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkCompleteCommunityConnectionResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "completeCommunityConnection",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkCompleteCommunityConnectionResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun completeWithResponse(
        platform: String,
        statusToken: String,
        serverId: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCompleteCommunityConnectionResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        complete(platform, statusToken, serverId, capturingOptions)
        return captured
            as? RawResponse<SdkCompleteCommunityConnectionResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [complete]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [complete] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("completeSuspend")
    suspend fun completeSuspend(
        platform: String,
        statusToken: String,
        serverId: String,
        requestOptions: RequestOptions? = null,
    ): SdkCompleteCommunityConnectionResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            complete(platform, statusToken, serverId, requestOptions)
        }

    /**
     * Coroutine-aware variant of [completeWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [completeWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("completeWithResponseSuspend")
    suspend fun completeWithResponseSuspend(
        platform: String,
        statusToken: String,
        serverId: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCompleteCommunityConnectionResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            completeWithResponse(platform, statusToken, serverId, requestOptions)
        }

    /**
     * Verify a community connection
     *
     * Recheck provider and server health, then safely requeue eligible failed community grants.
     * Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a
     * store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param platform The community platform.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkVerifyCommunityConnectionResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun verify(
        platform: String,
        requestOptions: RequestOptions? = null,
    ): SdkVerifyCommunityConnectionResponseValue200ApplicationJson {
        val body = linkedMapOf<String, Any?>()
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/community-connections/${encodePathSegment(platform)}/verify",
                body = body,
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkVerifyCommunityConnectionResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "verifyCommunityConnection",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkVerifyCommunityConnectionResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun verifyWithResponse(
        platform: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkVerifyCommunityConnectionResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        verify(platform, capturingOptions)
        return captured as? RawResponse<SdkVerifyCommunityConnectionResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [verify]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [verify] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("verifySuspend")
    suspend fun verifySuspend(
        platform: String,
        requestOptions: RequestOptions? = null,
    ): SdkVerifyCommunityConnectionResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            verify(platform, requestOptions)
        }

    /**
     * Coroutine-aware variant of [verifyWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [verifyWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("verifyWithResponseSuspend")
    suspend fun verifyWithResponseSuspend(
        platform: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkVerifyCommunityConnectionResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            verifyWithResponse(platform, requestOptions)
        }

    /**
     * Disconnect a community platform
     *
     * Idempotently disconnect the selected platform for the authenticated store and remove only
     * that store's associated servers, channels, groups, or account links. Requires the `community`
     * Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param platform The community platform.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun disconnect(
        platform: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/community-connections/${encodePathSegment(platform)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkDisconnectCommunityPlatformResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "disconnectCommunityPlatform",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun disconnectWithResponse(
        platform: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        disconnect(platform, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [disconnect]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [disconnect] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("disconnectSuspend")
    suspend fun disconnectSuspend(
        platform: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            disconnect(platform, requestOptions)
        }

    /**
     * Coroutine-aware variant of [disconnectWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [disconnectWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("disconnectWithResponseSuspend")
    suspend fun disconnectWithResponseSuspend(
        platform: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            disconnectWithResponse(platform, requestOptions)
        }
}
