// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.oauthmanagement

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkGetOAuthInstallationResponseValue200ApplicationJson

/**
 * API accessor for OauthManagement.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class OAuthManagement(internal val sellapp: SellApp) {
    /**
     * Read your CLI connection
     *
     * Read your active official CLI connection, its account-wide grant model and granted scopes.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetOAuthInstallationResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun getInstallation(
        requestOptions: RequestOptions? = null
    ): SdkGetOAuthInstallationResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/oauth/installation",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetOAuthInstallationResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getOAuthInstallation",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetOAuthInstallationResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getInstallationWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetOAuthInstallationResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        getInstallation(capturingOptions)
        return captured as? RawResponse<SdkGetOAuthInstallationResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [getInstallation]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getInstallation] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getInstallationSuspend")
    suspend fun getInstallationSuspend(
        requestOptions: RequestOptions? = null
    ): SdkGetOAuthInstallationResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            getInstallation(requestOptions)
        }

    /**
     * Coroutine-aware variant of [getInstallationWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [getInstallationWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getInstallationWithResponseSuspend")
    suspend fun getInstallationWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetOAuthInstallationResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getInstallationWithResponse(requestOptions)
        }

    /**
     * Disconnect your CLI connection
     *
     * Disconnect your official CLI connection and revoke its authorization codes and token
     * families. Run sellapp login to reconnect.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun deleteInstallation(requestOptions: RequestOptions? = null) {
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/oauth/installation",
                requestOptions = requestOptions,
                responseSchema = runtimeSchema("{\"kind\":\"primitive\",\"type\":\"unknown\"}"),
                operationId = "deleteOAuthInstallation",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun deleteInstallationWithResponse(requestOptions: RequestOptions? = null): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        deleteInstallation(capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [deleteInstallation]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [deleteInstallation] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteInstallationSuspend")
    suspend fun deleteInstallationSuspend(requestOptions: RequestOptions? = null) =
        sellapp.baseClient.runCancellable {
            deleteInstallation(requestOptions)
        }

    /**
     * Coroutine-aware variant of [deleteInstallationWithResponse]. Use this from a `suspend`
     * function or coroutine scope.
     *
     * Delegates to the blocking [deleteInstallationWithResponse] under
     * `withContext(Dispatchers.IO)`, so this is safe to call from any coroutine dispatcher
     * (including `Dispatchers.Main`).
     */
    @JvmName("deleteInstallationWithResponseSuspend")
    suspend fun deleteInstallationWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            deleteInstallationWithResponse(requestOptions)
        }
}
