// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.addonsparentproducts

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkListAddOnSParentProductsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for AddOnsParentProducts.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class AddOnsParentProducts(internal val sellapp: SellApp) {
    /**
     * List an add-on's parent products
     *
     * Retrieve the ordered products, courses, and bookings that currently offer this add-on. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param addon The addon path parameter.
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param pagination Set to false to return at most 100 matching items in one data array without
     *   pagination links or metadata.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        addon: String,
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkListAddOnSParentProductsResponseValue200ApplicationJson> {
        val itemType =
            object : TypeReference<SdkListAddOnSParentProductsResponseValue200ApplicationJson>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/addons/${encodePathSegment(addon)}/parent-products",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listAddOnSParentProducts",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.SdkListAddOnSParentProductsResponseValue200ApplicationJson
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
            pagination?.let { add("pagination" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        addon: String,
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkListAddOnSParentProductsResponseValue200ApplicationJson>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(addon, limit, page, pagination, capturingOptions)
        return captured
            as? RawResponse<Page<SdkListAddOnSParentProductsResponseValue200ApplicationJson>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        addon: String,
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkListAddOnSParentProductsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            list(addon, limit, page, pagination, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        addon: String,
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkListAddOnSParentProductsResponseValue200ApplicationJson>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(addon, limit, page, pagination, requestOptions)
        }

    /**
     * Replace an add-on's parent products
     *
     * Replace the complete ordered parent-product assignment. IDs must belong to the same store and
     * may reference non-subscription products, courses, or bookings. OAuth callers use the admin
     * grant and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param addon The addon path parameter.
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        addon: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson {
        val body = bodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "PUT",
                path = "/v2/addons/${encodePathSegment(addon)}/parent-products",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkReplaceAddOnSParentProductsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceAddOnSParentProducts",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        addon: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(addon, resources, capturingOptions)
        return captured
            as? RawResponse<SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        addon: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(addon, resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        addon: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(addon, resources, requestOptions)
        }
}
