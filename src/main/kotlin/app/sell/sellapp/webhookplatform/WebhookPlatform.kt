// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.webhookplatform

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.SdkGetWebhookDeliveryResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkListWebhookEventTypesResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkReplayWebhookDeliveryResponseValue201ApplicationJson
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for WebhookPlatform.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class WebhookPlatform(internal val sellapp: SellApp) {
    /**
     * List webhook event types
     *
     * List versioned outbound event types. OAuth callers use the admin grant and select a store
     * with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkListWebhookEventTypesResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun listEventTypes(
        requestOptions: RequestOptions? = null
    ): SdkListWebhookEventTypesResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/webhook-event-types",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkListWebhookEventTypesResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "listWebhookEventTypes",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkListWebhookEventTypesResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun listEventTypesWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkListWebhookEventTypesResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        listEventTypes(capturingOptions)
        return captured as? RawResponse<SdkListWebhookEventTypesResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [listEventTypes]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [listEventTypes] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listEventTypesSuspend")
    suspend fun listEventTypesSuspend(
        requestOptions: RequestOptions? = null
    ): SdkListWebhookEventTypesResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            listEventTypes(requestOptions)
        }

    /**
     * Coroutine-aware variant of [listEventTypesWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [listEventTypesWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listEventTypesWithResponseSuspend")
    suspend fun listEventTypesWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkListWebhookEventTypesResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            listEventTypesWithResponse(requestOptions)
        }

    /**
     * List webhook deliveries
     *
     * Destination origin only; response bodies are redacted and capped. OAuth callers use the admin
     * grant and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun listDeliveries(
        requestOptions: RequestOptions? = null
    ): Page<ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<
                    ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem
                >() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/webhook-deliveries",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listWebhookDeliveries",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models
                    .ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {}
    }

    @Suppress("UNCHECKED_CAST")
    fun listDeliveriesWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<Page<ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        listDeliveries(capturingOptions)
        return captured
            as?
            RawResponse<Page<ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [listDeliveries]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [listDeliveries] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listDeliveriesSuspend")
    suspend fun listDeliveriesSuspend(
        requestOptions: RequestOptions? = null
    ): Page<ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            listDeliveries(requestOptions)
        }

    /**
     * Coroutine-aware variant of [listDeliveriesWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [listDeliveriesWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listDeliveriesWithResponseSuspend")
    suspend fun listDeliveriesWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<Page<ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            listDeliveriesWithResponse(requestOptions)
        }

    /**
     * Retrieve a webhook delivery
     *
     * Retrieve one redacted delivery. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param delivery The delivery identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetWebhookDeliveryResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun getDelivery(
        delivery: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetWebhookDeliveryResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/webhook-deliveries/${encodePathSegment(delivery)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetWebhookDeliveryResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getWebhookDelivery",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetWebhookDeliveryResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getDeliveryWithResponse(
        delivery: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetWebhookDeliveryResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        getDelivery(delivery, capturingOptions)
        return captured as? RawResponse<SdkGetWebhookDeliveryResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [getDelivery]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getDelivery] under `withContext(Dispatchers.IO)`, so this is safe
     * to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getDeliverySuspend")
    suspend fun getDeliverySuspend(
        delivery: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetWebhookDeliveryResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            getDelivery(delivery, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getDeliveryWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [getDeliveryWithResponse] under `withContext(Dispatchers.IO)`, so
     * this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getDeliveryWithResponseSuspend")
    suspend fun getDeliveryWithResponseSuspend(
        delivery: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetWebhookDeliveryResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getDeliveryWithResponse(delivery, requestOptions)
        }

    /**
     * Replay a webhook delivery
     *
     * Accept no URL, payload, or query overrides. Resolve the current channel server-side, create a
     * new delivery ID, retain the integration-event ID, and sign with a new timestamp. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param delivery The delivery identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplayWebhookDeliveryResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun replayDelivery(
        delivery: String,
        requestOptions: RequestOptions? = null,
    ): SdkReplayWebhookDeliveryResponseValue201ApplicationJson {
        val body = linkedMapOf<String, Any?>()
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/webhook-deliveries/${encodePathSegment(delivery)}/replay",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkReplayWebhookDeliveryRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkReplayWebhookDeliveryResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "replayWebhookDelivery",
                supportsIdempotency = true,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplayWebhookDeliveryResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replayDeliveryWithResponse(
        delivery: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplayWebhookDeliveryResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replayDelivery(delivery, capturingOptions)
        return captured as? RawResponse<SdkReplayWebhookDeliveryResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replayDelivery]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [replayDelivery] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replayDeliverySuspend")
    suspend fun replayDeliverySuspend(
        delivery: String,
        requestOptions: RequestOptions? = null,
    ): SdkReplayWebhookDeliveryResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            replayDelivery(delivery, requestOptions)
        }

    /**
     * Coroutine-aware variant of [replayDeliveryWithResponse]. Use this from a `suspend` function
     * or coroutine scope.
     *
     * Delegates to the blocking [replayDeliveryWithResponse] under `withContext(Dispatchers.IO)`,
     * so this is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replayDeliveryWithResponseSuspend")
    suspend fun replayDeliveryWithResponseSuspend(
        delivery: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplayWebhookDeliveryResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replayDeliveryWithResponse(delivery, requestOptions)
        }
}
