// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.variantdeliverablefolders

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.PatchField
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.http.patchBodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkGetVariantDeliverableFolderResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkListVariantDeliverableFoldersResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson

/**
 * API accessor for VariantDeliverableFolders.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class VariantDeliverableFolders(internal val sellapp: SellApp) {
    /**
     * List variant deliverable folders
     *
     * List variant deliverable folders. Requires the listing Sanctum ability. Returns folders
     * ordered by sort order. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkListVariantDeliverableFoldersResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun list(
        product: String,
        variant: String,
        requestOptions: RequestOptions? = null,
    ): SdkListVariantDeliverableFoldersResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/folders",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkListVariantDeliverableFoldersResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "listVariantDeliverableFolders",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkListVariantDeliverableFoldersResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        product: String,
        variant: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkListVariantDeliverableFoldersResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(product, variant, capturingOptions)
        return captured
            as? RawResponse<SdkListVariantDeliverableFoldersResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        product: String,
        variant: String,
        requestOptions: RequestOptions? = null,
    ): SdkListVariantDeliverableFoldersResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            list(product, variant, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        product: String,
        variant: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkListVariantDeliverableFoldersResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            listWithResponse(product, variant, requestOptions)
        }

    /**
     * Create a variant deliverable folder
     *
     * Create a variant deliverable folder. Requires the listing Sanctum ability. Parent folders are
     * validated against the same variant. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param name the name of the request.
     * @param description the description of the request.
     * @param parentId the parent id of the request.
     * @param sortOrder the sort order of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun create(
        product: String,
        variant: String,
        name: String,
        description: String? = null,
        parentId: Long? = null,
        sortOrder: Long? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "name" to name,
                "description" to description,
                "parent_id" to parentId,
                "sort_order" to sortOrder,
            )
        body["name"] = name
        val config =
            RequestConfig(
                method = "POST",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/folders",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateVariantDeliverableFolderRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "createVariantDeliverableFolder",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createWithResponse(
        product: String,
        variant: String,
        name: String,
        description: String? = null,
        parentId: Long? = null,
        sortOrder: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        create(product, variant, name, description, parentId, sortOrder, capturingOptions)
        return captured
            as? RawResponse<SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [create]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [create] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createSuspend")
    suspend fun createSuspend(
        product: String,
        variant: String,
        name: String,
        description: String? = null,
        parentId: Long? = null,
        sortOrder: Long? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            create(product, variant, name, description, parentId, sortOrder, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createWithResponseSuspend")
    suspend fun createWithResponseSuspend(
        product: String,
        variant: String,
        name: String,
        description: String? = null,
        parentId: Long? = null,
        sortOrder: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createWithResponse(
                product,
                variant,
                name,
                description,
                parentId,
                sortOrder,
                requestOptions,
            )
        }

    /**
     * Retrieve a variant deliverable folder
     *
     * Retrieve a variant deliverable folder. Requires the listing Sanctum ability. Folder and
     * parent IDs are scoped through the authenticated store, product, and variant; cycles are
     * rejected. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param folder The deliverable folder identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetVariantDeliverableFolderResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        product: String,
        variant: String,
        folder: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetVariantDeliverableFolderResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/folders/${encodePathSegment(folder)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkGetVariantDeliverableFolderResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getVariantDeliverableFolder",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetVariantDeliverableFolderResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        product: String,
        variant: String,
        folder: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetVariantDeliverableFolderResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(product, variant, folder, capturingOptions)
        return captured
            as? RawResponse<SdkGetVariantDeliverableFolderResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        product: String,
        variant: String,
        folder: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetVariantDeliverableFolderResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(product, variant, folder, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        product: String,
        variant: String,
        folder: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetVariantDeliverableFolderResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(product, variant, folder, requestOptions)
        }

    /**
     * Replace variant deliverable folder settings
     *
     * Replace variant deliverable folder settings. Requires the listing Sanctum ability. Folder and
     * parent IDs are scoped through the authenticated store, product, and variant; cycles are
     * rejected. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param folder The deliverable folder identifier.
     * @param name the name of the request.
     * @param description the description of the request.
     * @param parentId the parent id of the request.
     * @param sortOrder the sort order of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        product: String,
        variant: String,
        folder: String,
        name: String? = null,
        description: String? = null,
        parentId: Long? = null,
        sortOrder: Long? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "name" to name,
                "description" to description,
                "parent_id" to parentId,
                "sort_order" to sortOrder,
            )
        val config =
            RequestConfig(
                method = "PUT",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/folders/${encodePathSegment(folder)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models
                        .SdkReplaceVariantDeliverableFolderSettingsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceVariantDeliverableFolderSettings",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        product: String,
        variant: String,
        folder: String,
        name: String? = null,
        description: String? = null,
        parentId: Long? = null,
        sortOrder: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(product, variant, folder, name, description, parentId, sortOrder, capturingOptions)
        return captured
            as?
            RawResponse<SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        product: String,
        variant: String,
        folder: String,
        name: String? = null,
        description: String? = null,
        parentId: Long? = null,
        sortOrder: Long? = null,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(
                product,
                variant,
                folder,
                name,
                description,
                parentId,
                sortOrder,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        product: String,
        variant: String,
        folder: String,
        name: String? = null,
        description: String? = null,
        parentId: Long? = null,
        sortOrder: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(
                product,
                variant,
                folder,
                name,
                description,
                parentId,
                sortOrder,
                requestOptions,
            )
        }

    /**
     * Update a variant deliverable folder
     *
     * Update a variant deliverable folder. Requires the listing Sanctum ability. Folder and parent
     * IDs are scoped through the authenticated store, product, and variant; cycles are rejected.
     * OAuth callers use the admin grant and select a store with X-STORE. Current membership and
     * role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param folder The deliverable folder identifier.
     * @param name the name of the request.
     * @param description the description of the request.
     * @param parentId the parent id of the request.
     * @param sortOrder the sort order of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun update(
        product: String,
        variant: String,
        folder: String,
        name: PatchField<String> = PatchField.Absent,
        description: PatchField<String?> = PatchField.Absent,
        parentId: PatchField<Long?> = PatchField.Absent,
        sortOrder: PatchField<Long> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson {
        val body =
            patchBodyOf(
                "name" to name,
                "description" to description,
                "parent_id" to parentId,
                "sort_order" to sortOrder,
            )
        val config =
            RequestConfig(
                method = "PATCH",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/folders/${encodePathSegment(folder)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkUpdateVariantDeliverableFolderRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "updateVariantDeliverableFolder",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun updateWithResponse(
        product: String,
        variant: String,
        folder: String,
        name: PatchField<String> = PatchField.Absent,
        description: PatchField<String?> = PatchField.Absent,
        parentId: PatchField<Long?> = PatchField.Absent,
        sortOrder: PatchField<Long> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        update(product, variant, folder, name, description, parentId, sortOrder, capturingOptions)
        return captured
            as? RawResponse<SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [update]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [update] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateSuspend")
    suspend fun updateSuspend(
        product: String,
        variant: String,
        folder: String,
        name: PatchField<String> = PatchField.Absent,
        description: PatchField<String?> = PatchField.Absent,
        parentId: PatchField<Long?> = PatchField.Absent,
        sortOrder: PatchField<Long> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            update(product, variant, folder, name, description, parentId, sortOrder, requestOptions)
        }

    /**
     * Coroutine-aware variant of [updateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [updateWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateWithResponseSuspend")
    suspend fun updateWithResponseSuspend(
        product: String,
        variant: String,
        folder: String,
        name: PatchField<String> = PatchField.Absent,
        description: PatchField<String?> = PatchField.Absent,
        parentId: PatchField<Long?> = PatchField.Absent,
        sortOrder: PatchField<Long> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            updateWithResponse(
                product,
                variant,
                folder,
                name,
                description,
                parentId,
                sortOrder,
                requestOptions,
            )
        }

    /**
     * Delete a variant deliverable folder
     *
     * Delete a variant deliverable folder. Requires the listing Sanctum ability. Folder and parent
     * IDs are scoped through the authenticated store, product, and variant; cycles are rejected.
     * OAuth callers use the admin grant and select a store with X-STORE. Current membership and
     * role permissions apply to every request.
     *
     * @param product The product identifier or slug. The product must belong to the authenticated
     *   store.
     * @param variant The product variant identifier.
     * @param folder The deliverable folder identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun delete(
        product: String,
        variant: String,
        folder: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path =
                    "/v2/products/${encodePathSegment(product)}/variants/${encodePathSegment(variant)}/deliverable/folders/${encodePathSegment(folder)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkDeleteVariantDeliverableFolderResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "deleteVariantDeliverableFolder",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun deleteWithResponse(
        product: String,
        variant: String,
        folder: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        delete(product, variant, folder, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [delete]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [delete] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteSuspend")
    suspend fun deleteSuspend(
        product: String,
        variant: String,
        folder: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            delete(product, variant, folder, requestOptions)
        }

    /**
     * Coroutine-aware variant of [deleteWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [deleteWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteWithResponseSuspend")
    suspend fun deleteWithResponseSuspend(
        product: String,
        variant: String,
        folder: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            deleteWithResponse(product, variant, folder, requestOptions)
        }
}
