// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.productsaddons

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkListProductSAddOnsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkReplaceProductSAddOnsResponseValue200ApplicationJson
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for ProductsAddons.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class ProductsAddons(internal val sellapp: SellApp) {
    /**
     * List a product's add-ons
     *
     * Retrieve the ordered add-ons assigned to a product, course, or booking. OAuth callers use the
     * admin grant and select a store with X-STORE. Current membership and role permissions apply to
     * every request.
     *
     * @param product The product path parameter.
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param pagination Set to false to return at most 100 matching items in one data array without
     *   pagination links or metadata.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        product: String,
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkListProductSAddOnsResponseValue200ApplicationJson> {
        val itemType =
            object : TypeReference<SdkListProductSAddOnsResponseValue200ApplicationJson>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/products/${encodePathSegment(product)}/addons",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listProductSAddOns",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.SdkListProductSAddOnsResponseValue200ApplicationJson
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
            pagination?.let { add("pagination" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        product: String,
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkListProductSAddOnsResponseValue200ApplicationJson>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(product, limit, page, pagination, capturingOptions)
        return captured as? RawResponse<Page<SdkListProductSAddOnsResponseValue200ApplicationJson>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        product: String,
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkListProductSAddOnsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            list(product, limit, page, pagination, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        product: String,
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkListProductSAddOnsResponseValue200ApplicationJson>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(product, limit, page, pagination, requestOptions)
        }

    /**
     * Replace a product's add-ons
     *
     * Replace the complete ordered add-on assignment for a non-subscription product, course, or
     * booking. Newly assigned add-ons must be published, public, and owned by the same store;
     * already assigned non-public add-ons may be retained for safe editing. OAuth callers use the
     * admin grant and select a store with X-STORE. Current membership and role permissions apply to
     * every request.
     *
     * @param product The product path parameter.
     * @param resources the resources of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceProductSAddOnsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        product: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceProductSAddOnsResponseValue200ApplicationJson {
        val body = bodyOf("resources" to resources)
        body["resources"] = resources
        val config =
            RequestConfig(
                method = "PUT",
                path = "/v2/products/${encodePathSegment(product)}/addons",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkReplaceProductSAddOnsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkReplaceProductSAddOnsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceProductSAddOns",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceProductSAddOnsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        product: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceProductSAddOnsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(product, resources, capturingOptions)
        return captured as? RawResponse<SdkReplaceProductSAddOnsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        product: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceProductSAddOnsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(product, resources, requestOptions)
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        product: String,
        resources: List<Long>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceProductSAddOnsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(product, resources, requestOptions)
        }
}
