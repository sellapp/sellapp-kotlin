// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.groups

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.PatchField
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.http.patchBodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.ListGroupsResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.SdkCreateGroupResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkGetGroupResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkUpdateGroupResponseValue200ApplicationJson
import app.sell.sellapp.models.SearchGroupsRequestApplicationJsonPropertyFiltersItem
import app.sell.sellapp.models.SearchGroupsRequestApplicationJsonPropertyIncludesItem
import app.sell.sellapp.models.SearchGroupsRequestApplicationJsonPropertySearch
import app.sell.sellapp.models.SearchGroupsRequestApplicationJsonPropertySortItem
import app.sell.sellapp.models.SearchGroupsResponseValue200ApplicationJsonPropertyDataItem
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for Groups.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class Groups(internal val sellapp: SellApp) {
    /**
     * List all groups
     *
     * List your store's groups, 15 per page by default. OAuth callers use the admin grant and
     * select a store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListGroupsResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object : TypeReference<ListGroupsResponseValue200ApplicationJsonPropertyDataItem>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/groups",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listGroups",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.ListGroupsResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListGroupsResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(limit, page, capturingOptions)
        return captured
            as? RawResponse<Page<ListGroupsResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListGroupsResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            list(limit, page, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        limit: Int? = null,
        page: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListGroupsResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(limit, page, requestOptions)
        }

    /**
     * Create a group
     *
     * Create a group for related products. Supply product_ids and section_id to save the group,
     * ordered product membership, and section together. The whole change rolls back if any selected
     * product is invalid. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param title the title of the request.
     * @param unlisted the unlisted of the request.
     * @param order the order of the request.
     * @param productIds Ordered IDs of published standard products in this store. Supply an empty
     *   array to remove every product. Missing, draft, or foreign products are rejected.
     * @param sectionId Section in this store. Omit to preserve it; null removes the section.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateGroupResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun create(
        title: String,
        unlisted: Boolean,
        order: Long? = null,
        productIds: List<Long>? = null,
        sectionId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateGroupResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "title" to title,
                "unlisted" to unlisted,
                "order" to order,
                "product_ids" to productIds,
                "section_id" to sectionId,
            )
        body["title"] = title
        body["unlisted"] = unlisted
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/groups",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateGroupRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateGroupResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "createGroup",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateGroupResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createWithResponse(
        title: String,
        unlisted: Boolean,
        order: Long? = null,
        productIds: List<Long>? = null,
        sectionId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateGroupResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        create(title, unlisted, order, productIds, sectionId, capturingOptions)
        return captured as? RawResponse<SdkCreateGroupResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [create]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [create] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createSuspend")
    suspend fun createSuspend(
        title: String,
        unlisted: Boolean,
        order: Long? = null,
        productIds: List<Long>? = null,
        sectionId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateGroupResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            create(title, unlisted, order, productIds, sectionId, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createWithResponseSuspend")
    suspend fun createWithResponseSuspend(
        title: String,
        unlisted: Boolean,
        order: Long? = null,
        productIds: List<Long>? = null,
        sectionId: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateGroupResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createWithResponse(title, unlisted, order, productIds, sectionId, requestOptions)
        }

    /**
     * Retrieve a group
     *
     * Retrieve a group by its ID. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param group The group path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetGroupResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        group: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetGroupResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/groups/${encodePathSegment(group)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetGroupResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getGroup",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetGroupResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        group: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetGroupResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(group, capturingOptions)
        return captured as? RawResponse<SdkGetGroupResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        group: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetGroupResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(group, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        group: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetGroupResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(group, requestOptions)
        }

    /**
     * Update a group
     *
     * Update a group's details. Supply product_ids and section_id to save the group, ordered
     * product membership, and section together. The whole change rolls back if any selected product
     * is invalid. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param group The group path parameter.
     * @param title the title of the request.
     * @param unlisted the unlisted of the request.
     * @param order the order of the request.
     * @param productIds Ordered IDs of published standard products in this store. Supply an empty
     *   array to remove every product. Missing, draft, or foreign products are rejected.
     * @param sectionId Section in this store. Omit to preserve it; null removes the section.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkUpdateGroupResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun update(
        group: String,
        title: PatchField<String> = PatchField.Absent,
        unlisted: PatchField<Boolean> = PatchField.Absent,
        order: PatchField<Long?> = PatchField.Absent,
        productIds: PatchField<List<Long>> = PatchField.Absent,
        sectionId: PatchField<Long?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateGroupResponseValue200ApplicationJson {
        val body =
            patchBodyOf(
                "title" to title,
                "unlisted" to unlisted,
                "order" to order,
                "product_ids" to productIds,
                "section_id" to sectionId,
            )
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v2/groups/${encodePathSegment(group)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkUpdateGroupRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkUpdateGroupResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "updateGroup",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkUpdateGroupResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun updateWithResponse(
        group: String,
        title: PatchField<String> = PatchField.Absent,
        unlisted: PatchField<Boolean> = PatchField.Absent,
        order: PatchField<Long?> = PatchField.Absent,
        productIds: PatchField<List<Long>> = PatchField.Absent,
        sectionId: PatchField<Long?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateGroupResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        update(group, title, unlisted, order, productIds, sectionId, capturingOptions)
        return captured as? RawResponse<SdkUpdateGroupResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [update]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [update] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateSuspend")
    suspend fun updateSuspend(
        group: String,
        title: PatchField<String> = PatchField.Absent,
        unlisted: PatchField<Boolean> = PatchField.Absent,
        order: PatchField<Long?> = PatchField.Absent,
        productIds: PatchField<List<Long>> = PatchField.Absent,
        sectionId: PatchField<Long?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateGroupResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            update(group, title, unlisted, order, productIds, sectionId, requestOptions)
        }

    /**
     * Coroutine-aware variant of [updateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [updateWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateWithResponseSuspend")
    suspend fun updateWithResponseSuspend(
        group: String,
        title: PatchField<String> = PatchField.Absent,
        unlisted: PatchField<Boolean> = PatchField.Absent,
        order: PatchField<Long?> = PatchField.Absent,
        productIds: PatchField<List<Long>> = PatchField.Absent,
        sectionId: PatchField<Long?> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateGroupResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            updateWithResponse(group, title, unlisted, order, productIds, sectionId, requestOptions)
        }

    /**
     * Delete a group
     *
     * Deletes a group. This will permanently delete the group and its details. OAuth callers use
     * the admin grant and select a store with X-STORE. Current membership and role permissions
     * apply to every request.
     *
     * @param group The group path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun delete(
        group: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/groups/${encodePathSegment(group)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkDeleteGroupResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "deleteGroup",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun deleteWithResponse(
        group: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        delete(group, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [delete]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [delete] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteSuspend")
    suspend fun deleteSuspend(
        group: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            delete(group, requestOptions)
        }

    /**
     * Coroutine-aware variant of [deleteWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [deleteWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteWithResponseSuspend")
    suspend fun deleteWithResponseSuspend(
        group: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            deleteWithResponse(group, requestOptions)
        }

    /**
     * Search groups
     *
     * Search groups using JSON body filters, search terms, includes, and sort instructions. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param filters the filters of the request.
     * @param sort the sort of the request.
     * @param search the search of the request.
     * @param includes the includes of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun search(
        filters: List<SearchGroupsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchGroupsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchGroupsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchGroupsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SearchGroupsResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object : TypeReference<SearchGroupsResponseValue200ApplicationJsonPropertyDataItem>() {}
        return sellapp.baseClient.requestPage(
            method = "POST",
            path = "/v2/groups/search",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "searchGroups",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.SearchGroupsResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {}
    }

    @Suppress("UNCHECKED_CAST")
    fun searchWithResponse(
        filters: List<SearchGroupsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchGroupsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchGroupsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchGroupsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SearchGroupsResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        search(filters, sort, search, includes, capturingOptions)
        return captured
            as? RawResponse<Page<SearchGroupsResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [search]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [search] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchSuspend")
    suspend fun searchSuspend(
        filters: List<SearchGroupsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchGroupsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchGroupsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchGroupsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SearchGroupsResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            search(filters, sort, search, includes, requestOptions)
        }

    /**
     * Coroutine-aware variant of [searchWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [searchWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchWithResponseSuspend")
    suspend fun searchWithResponseSuspend(
        filters: List<SearchGroupsRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchGroupsRequestApplicationJsonPropertySortItem>? = null,
        search: SearchGroupsRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchGroupsRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SearchGroupsResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            searchWithResponse(filters, sort, search, includes, requestOptions)
        }
}
