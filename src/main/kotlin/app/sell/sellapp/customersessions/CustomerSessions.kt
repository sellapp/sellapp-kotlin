// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.customersessions

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkCreateCustomerSessionResponseValue201ApplicationJson

/**
 * API accessor for CustomerSessions.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class CustomerSessions(internal val sellapp: SellApp) {
    /**
     * Create a customer session
     *
     * Return a 15-minute bearer token once plus a separate single-use hosted redemption URL.
     * Opening the URL signs the customer into this store's customer portal only. It does not sign
     * them into the dashboard or other stores. Browser access and wallet credentials issued from it
     * stop working when the session expires or is revoked. This response is never cached or
     * replayed. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateCustomerSessionResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun create(
        requestBody: Any,
        requestOptions: RequestOptions? = null,
    ): SdkCreateCustomerSessionResponseValue201ApplicationJson {
        val body = requestBody
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/customer-sessions",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    runtimeSchema(
                        "{\"kind\":\"union\",\"variants\":[{\"kind\":\"object\",\"modelName\":\"CreateCustomerSessionRequestApplicationJsonOneOfValue1\",\"properties\":{\"customer_id\":{\"kind\":\"primitive\",\"type\":\"integer\"}},\"required\":[\"customer_id\"],\"additionalProperties\":false},{\"kind\":\"object\",\"modelName\":\"CreateCustomerSessionRequestApplicationJsonOneOfValue2\",\"properties\":{\"external_customer_id\":{\"kind\":\"primitive\",\"type\":\"string\"}},\"required\":[\"external_customer_id\"],\"additionalProperties\":false}],\"composition\":\"oneOf\"}"
                    ),
                responseSchema =
                    app.sell.sellapp.models.SdkCreateCustomerSessionResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "createCustomerSession",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateCustomerSessionResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createWithResponse(
        requestBody: Any,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateCustomerSessionResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        create(requestBody, capturingOptions)
        return captured as? RawResponse<SdkCreateCustomerSessionResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [create]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [create] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createSuspend")
    suspend fun createSuspend(
        requestBody: Any,
        requestOptions: RequestOptions? = null,
    ): SdkCreateCustomerSessionResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            create(requestBody, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createWithResponseSuspend")
    suspend fun createWithResponseSuspend(
        requestBody: Any,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateCustomerSessionResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createWithResponse(requestBody, requestOptions)
        }

    /**
     * Revoke a customer session
     *
     * Revoke immediately. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param session The session identifier.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun revoke(
        session: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/customer-sessions/${encodePathSegment(session)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkRevokeCustomerSessionResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "revokeCustomerSession",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun revokeWithResponse(
        session: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        revoke(session, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [revoke]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [revoke] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("revokeSuspend")
    suspend fun revokeSuspend(
        session: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            revoke(session, requestOptions)
        }

    /**
     * Coroutine-aware variant of [revokeWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [revokeWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("revokeWithResponseSuspend")
    suspend fun revokeWithResponseSuspend(
        session: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            revokeWithResponse(session, requestOptions)
        }
}
