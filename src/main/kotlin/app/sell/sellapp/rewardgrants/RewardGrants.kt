// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.rewardgrants

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkGetRewardGrantResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkIssueEligibleRewardGrantResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkListRewardGrantsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkSearchRewardGrantsResponseValue200ApplicationJson
import app.sell.sellapp.models.SearchRewardRulesRequestApplicationJsonPropertyFiltersItem
import app.sell.sellapp.models.SearchRewardRulesRequestApplicationJsonPropertyIncludesItem
import app.sell.sellapp.models.SearchRewardRulesRequestApplicationJsonPropertySearch
import app.sell.sellapp.models.SearchRewardRulesRequestApplicationJsonPropertySortItem
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for RewardGrants.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class RewardGrants(internal val sellapp: SellApp) {
    /**
     * List reward grants
     *
     * List immutable reward grant history for the authenticated store. OAuth callers use the admin
     * grant and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param pagination Set to false to return at most 100 matching items in one data array without
     *   pagination links or metadata.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkListRewardGrantsResponseValue200ApplicationJson> {
        val itemType =
            object : TypeReference<SdkListRewardGrantsResponseValue200ApplicationJson>() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/reward-grants",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listRewardGrants",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.SdkListRewardGrantsResponseValue200ApplicationJson
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
            pagination?.let { add("pagination" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkListRewardGrantsResponseValue200ApplicationJson>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(limit, page, pagination, capturingOptions)
        return captured as? RawResponse<Page<SdkListRewardGrantsResponseValue200ApplicationJson>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkListRewardGrantsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            list(limit, page, pagination, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        limit: Int? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkListRewardGrantsResponseValue200ApplicationJson>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(limit, page, pagination, requestOptions)
        }

    /**
     * Issue an eligible reward grant
     *
     * Evaluate one store-owned customer against one active reward rule and issue its outputs only
     * when eligible. Repeating the same rule and customer returns the existing grant instead of
     * creating a duplicate. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param rewardRuleId the reward rule id of the request.
     * @param customerId the customer id of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkIssueEligibleRewardGrantResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun issue(
        rewardRuleId: Long,
        customerId: Long,
        requestOptions: RequestOptions? = null,
    ): SdkIssueEligibleRewardGrantResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "reward_rule_id" to rewardRuleId,
                "customer_id" to customerId,
            )
        body["reward_rule_id"] = rewardRuleId
        body["customer_id"] = customerId
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/reward-grants",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkIssueEligibleRewardGrantRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkIssueEligibleRewardGrantResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "issueEligibleRewardGrant",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkIssueEligibleRewardGrantResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun issueWithResponse(
        rewardRuleId: Long,
        customerId: Long,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkIssueEligibleRewardGrantResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        issue(rewardRuleId, customerId, capturingOptions)
        return captured as? RawResponse<SdkIssueEligibleRewardGrantResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [issue]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [issue] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("issueSuspend")
    suspend fun issueSuspend(
        rewardRuleId: Long,
        customerId: Long,
        requestOptions: RequestOptions? = null,
    ): SdkIssueEligibleRewardGrantResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            issue(rewardRuleId, customerId, requestOptions)
        }

    /**
     * Coroutine-aware variant of [issueWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [issueWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("issueWithResponseSuspend")
    suspend fun issueWithResponseSuspend(
        rewardRuleId: Long,
        customerId: Long,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkIssueEligibleRewardGrantResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            issueWithResponse(rewardRuleId, customerId, requestOptions)
        }

    /**
     * Search reward grants
     *
     * Search grant history by customer email or reward rule name. OAuth callers use the admin grant
     * and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param limit Number of items to return per page.
     * @param page Page number to return.
     * @param pagination Set to false to return at most 100 matching items in one data array without
     *   pagination links or metadata.
     * @param filters the filters of the request.
     * @param sort the sort of the request.
     * @param search the search of the request.
     * @param includes the includes of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun search(
        limit: Long? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        filters: List<SearchRewardRulesRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchRewardRulesRequestApplicationJsonPropertySortItem>? = null,
        search: SearchRewardRulesRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchRewardRulesRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkSearchRewardGrantsResponseValue200ApplicationJson> {
        val itemType =
            object : TypeReference<SdkSearchRewardGrantsResponseValue200ApplicationJson>() {}
        return sellapp.baseClient.requestPage(
            method = "POST",
            path = "/v2/reward-grants/search",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "searchRewardGrants",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models.SdkSearchRewardGrantsResponseValue200ApplicationJson
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            limit?.let { add("limit" to it.toString()) }
            page?.let { add("page" to it.toString()) }
            pagination?.let { add("pagination" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun searchWithResponse(
        limit: Long? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        filters: List<SearchRewardRulesRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchRewardRulesRequestApplicationJsonPropertySortItem>? = null,
        search: SearchRewardRulesRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchRewardRulesRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkSearchRewardGrantsResponseValue200ApplicationJson>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        search(limit, page, pagination, filters, sort, search, includes, capturingOptions)
        return captured as? RawResponse<Page<SdkSearchRewardGrantsResponseValue200ApplicationJson>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [search]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [search] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchSuspend")
    suspend fun searchSuspend(
        limit: Long? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        filters: List<SearchRewardRulesRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchRewardRulesRequestApplicationJsonPropertySortItem>? = null,
        search: SearchRewardRulesRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchRewardRulesRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SdkSearchRewardGrantsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            search(limit, page, pagination, filters, sort, search, includes, requestOptions)
        }

    /**
     * Coroutine-aware variant of [searchWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [searchWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchWithResponseSuspend")
    suspend fun searchWithResponseSuspend(
        limit: Long? = null,
        page: Long? = null,
        pagination: Boolean? = null,
        filters: List<SearchRewardRulesRequestApplicationJsonPropertyFiltersItem>? = null,
        sort: List<SearchRewardRulesRequestApplicationJsonPropertySortItem>? = null,
        search: SearchRewardRulesRequestApplicationJsonPropertySearch? = null,
        includes: List<SearchRewardRulesRequestApplicationJsonPropertyIncludesItem>? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SdkSearchRewardGrantsResponseValue200ApplicationJson>> =
        sellapp.baseClient.runCancellable {
            searchWithResponse(
                limit,
                page,
                pagination,
                filters,
                sort,
                search,
                includes,
                requestOptions,
            )
        }

    /**
     * Retrieve a reward grant
     *
     * Retrieve one store-owned reward grant without exposing its internal idempotency key or
     * evaluation snapshot. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param rewardGrant The rewardGrant path parameter.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetRewardGrantResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        rewardGrant: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetRewardGrantResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/reward-grants/${encodePathSegment(rewardGrant)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetRewardGrantResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getRewardGrant",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetRewardGrantResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        rewardGrant: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetRewardGrantResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(rewardGrant, capturingOptions)
        return captured as? RawResponse<SdkGetRewardGrantResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        rewardGrant: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetRewardGrantResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(rewardGrant, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        rewardGrant: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetRewardGrantResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(rewardGrant, requestOptions)
        }
}
