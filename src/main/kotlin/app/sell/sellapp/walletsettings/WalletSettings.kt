// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.walletsettings

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.SdkGetWalletSettingsResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkReplaceWalletSettingsResponseValue200ApplicationJson

/**
 * API accessor for WalletSettings.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class WalletSettings(internal val sellapp: SellApp) {
    /**
     * Retrieve wallet settings
     *
     * Retrieve wallet settings for the authenticated store. Requires the `wallet` or `wallet:write`
     * API ability. Payment methods are returned as their effective configurable values when no
     * explicit restriction is stored. OAuth callers use the admin grant and select a store with
     * X-STORE. Current membership and role permissions apply to every request.
     *
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetWalletSettingsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        requestOptions: RequestOptions? = null
    ): SdkGetWalletSettingsResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/wallet/settings",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetWalletSettingsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getWalletSettings",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetWalletSettingsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetWalletSettingsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(capturingOptions)
        return captured as? RawResponse<SdkGetWalletSettingsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        requestOptions: RequestOptions? = null
    ): SdkGetWalletSettingsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        requestOptions: RequestOptions? = null
    ): RawResponse<SdkGetWalletSettingsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(requestOptions)
        }

    /**
     * Update wallet settings
     *
     * Update wallet settings. Requires the `wallet:write` API ability and the store wallet
     * permission. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param enabled the enabled of the request.
     * @param minimumTopUpCents the minimum top up cents of the request.
     * @param maximumTopUpCents the maximum top up cents of the request.
     * @param expirationDays the expiration days of the request.
     * @param paymentMethods Effective top-up gateway groups. CUSTOM_PAYMENT_METHOD enables all
     *   usable custom methods; customers choose an individual method in the portal. With no saved
     *   restriction, every currently configurable group is returned, including custom methods. An
     *   empty array disables all methods. Custom top-ups charge and credit the exact USD deposit
     *   amount, plus any wallet bonus, without custom-method discounts or fees.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceWalletSettingsResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        enabled: Boolean,
        minimumTopUpCents: Long?,
        maximumTopUpCents: Long?,
        expirationDays: Long?,
        paymentMethods: List<String>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceWalletSettingsResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "enabled" to enabled,
                "minimum_top_up_cents" to minimumTopUpCents,
                "maximum_top_up_cents" to maximumTopUpCents,
                "expiration_days" to expirationDays,
                "payment_methods" to paymentMethods,
            )
        body["enabled"] = enabled
        body["minimum_top_up_cents"] = minimumTopUpCents
        body["maximum_top_up_cents"] = maximumTopUpCents
        body["expiration_days"] = expirationDays
        body["payment_methods"] = paymentMethods
        val config =
            RequestConfig(
                method = "PUT",
                path = "/v2/wallet/settings",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkReplaceWalletSettingsRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkReplaceWalletSettingsResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceWalletSettings",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceWalletSettingsResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        enabled: Boolean,
        minimumTopUpCents: Long?,
        maximumTopUpCents: Long?,
        expirationDays: Long?,
        paymentMethods: List<String>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceWalletSettingsResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(
            enabled,
            minimumTopUpCents,
            maximumTopUpCents,
            expirationDays,
            paymentMethods,
            capturingOptions,
        )
        return captured as? RawResponse<SdkReplaceWalletSettingsResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        enabled: Boolean,
        minimumTopUpCents: Long?,
        maximumTopUpCents: Long?,
        expirationDays: Long?,
        paymentMethods: List<String>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceWalletSettingsResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(
                enabled,
                minimumTopUpCents,
                maximumTopUpCents,
                expirationDays,
                paymentMethods,
                requestOptions,
            )
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        enabled: Boolean,
        minimumTopUpCents: Long?,
        maximumTopUpCents: Long?,
        expirationDays: Long?,
        paymentMethods: List<String>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceWalletSettingsResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(
                enabled,
                minimumTopUpCents,
                maximumTopUpCents,
                expirationDays,
                paymentMethods,
                requestOptions,
            )
        }
}
