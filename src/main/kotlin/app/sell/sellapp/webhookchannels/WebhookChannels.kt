// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.webhookchannels

import app.sell.sellapp.SellApp
import app.sell.sellapp.common.http.Page
import app.sell.sellapp.common.http.PatchField
import app.sell.sellapp.common.http.RawResponse
import app.sell.sellapp.common.http.RequestConfig
import app.sell.sellapp.common.http.RequestOptions
import app.sell.sellapp.common.http.bodyOf
import app.sell.sellapp.common.http.encodePathSegment
import app.sell.sellapp.common.http.patchBodyOf
import app.sell.sellapp.common.json.runtimeSchema
import app.sell.sellapp.models.ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.models.SdkCreateWebhookChannelResponseValue201ApplicationJson
import app.sell.sellapp.models.SdkGetWebhookChannelResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkReplaceWebhookChannelResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkRotateWebhookSigningSecretResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkSendTestWebhookResponseValue200ApplicationJson
import app.sell.sellapp.models.SdkUpdateWebhookChannelResponseValue200ApplicationJson
import app.sell.sellapp.models.SearchWebhookChannelsRequestApplicationJsonPropertySearch
import app.sell.sellapp.models.SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem
import app.sell.sellapp.types.Event
import com.fasterxml.jackson.core.type.TypeReference

/**
 * API accessor for WebhookChannels.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`)
 * and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the
 * blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine
 * dispatcher (including `Dispatchers.Main`).
 */
class WebhookChannels(internal val sellapp: SellApp) {
    /**
     * List webhook channels
     *
     * Retrieve the authenticated store's outbound developer webhook destinations. Signing secrets
     * and delivery headers are never included. OAuth callers use the admin grant and select a store
     * with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param page Page number.
     * @param limit Number of webhook channels to return per page.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun list(
        page: Long? = null,
        limit: Int? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<
                    ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem
                >() {}
        return sellapp.baseClient.requestPage(
            method = "GET",
            path = "/v2/webhook-channels",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "listWebhookChannels",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models
                    .ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {
            page?.let { add("page" to it.toString()) }
            limit?.let { add("limit" to it.toString()) }
        }
    }

    @Suppress("UNCHECKED_CAST")
    fun listWithResponse(
        page: Long? = null,
        limit: Int? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        list(page, limit, capturingOptions)
        return captured
            as?
            RawResponse<Page<ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [list]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [list] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listSuspend")
    suspend fun listSuspend(
        page: Long? = null,
        limit: Int? = null,
        requestOptions: RequestOptions? = null,
    ): Page<ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            list(page, limit, requestOptions)
        }

    /**
     * Coroutine-aware variant of [listWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [listWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("listWithResponseSuspend")
    suspend fun listWithResponseSuspend(
        page: Long? = null,
        limit: Int? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            listWithResponse(page, limit, requestOptions)
        }

    /**
     * Create a webhook channel
     *
     * Create an outbound webhook destination with a stable UUID and an explicit event filter. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param url Public HTTP or HTTPS destination. The same canonical URL cannot be configured
     *   twice for one store.
     * @param allowedNotifications Complete set of public webhook event names enabled for this
     *   destination.
     * @param name Optional seller-facing reference name.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkCreateWebhookChannelResponseValue201ApplicationJson
     */
    @JvmOverloads
    fun create(
        url: String,
        allowedNotifications: List<Event>,
        name: String? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateWebhookChannelResponseValue201ApplicationJson {
        val body =
            bodyOf(
                "url" to url,
                "allowed_notifications" to allowedNotifications,
                "name" to name,
            )
        body["url"] = url
        body["allowed_notifications"] = allowedNotifications
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/webhook-channels",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkCreateWebhookChannelRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkCreateWebhookChannelResponseValue201ApplicationJson
                        .runtimeSchema,
                operationId = "createWebhookChannel",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkCreateWebhookChannelResponseValue201ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun createWithResponse(
        url: String,
        allowedNotifications: List<Event>,
        name: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateWebhookChannelResponseValue201ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        create(url, allowedNotifications, name, capturingOptions)
        return captured as? RawResponse<SdkCreateWebhookChannelResponseValue201ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [create]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [create] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createSuspend")
    suspend fun createSuspend(
        url: String,
        allowedNotifications: List<Event>,
        name: String? = null,
        requestOptions: RequestOptions? = null,
    ): SdkCreateWebhookChannelResponseValue201ApplicationJson =
        sellapp.baseClient.runCancellable {
            create(url, allowedNotifications, name, requestOptions)
        }

    /**
     * Coroutine-aware variant of [createWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [createWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("createWithResponseSuspend")
    suspend fun createWithResponseSuspend(
        url: String,
        allowedNotifications: List<Event>,
        name: String? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkCreateWebhookChannelResponseValue201ApplicationJson> =
        sellapp.baseClient.runCancellable {
            createWithResponse(url, allowedNotifications, name, requestOptions)
        }

    /**
     * Search webhook channels
     *
     * Search destinations by name or URL and optionally filter by a subscribed event. OAuth callers
     * use the admin grant and select a store with X-STORE. Current membership and role permissions
     * apply to every request.
     *
     * @param search the search of the request.
     * @param event Public event name delivered in webhook payloads. SellApp maps it to the
     *   corresponding allowed_notifications filter.
     * @param page the page of the request.
     * @param limit the limit of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return a [app.sell.sellapp.common.http.Page] of results
     */
    @JvmOverloads
    fun search(
        search: SearchWebhookChannelsRequestApplicationJsonPropertySearch? = null,
        event: Event? = null,
        page: Long? = null,
        limit: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem> {
        val itemType =
            object :
                TypeReference<
                    SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem
                >() {}
        return sellapp.baseClient.requestPage(
            method = "POST",
            path = "/v2/webhook-channels/search",
            itemType = itemType,
            requestOptions = requestOptions,
            operationId = "searchWebhookChannels",
            supportsIdempotency = false,
            itemSchema =
                app.sell.sellapp.models
                    .SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem
                    .runtimeSchema,
            before = null,
            after = null,
        ) {}
    }

    @Suppress("UNCHECKED_CAST")
    fun searchWithResponse(
        search: SearchWebhookChannelsRequestApplicationJsonPropertySearch? = null,
        event: Event? = null,
        page: Long? = null,
        limit: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem>> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        search(search, event, page, limit, capturingOptions)
        return captured
            as?
            RawResponse<Page<SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem>>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [search]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [search] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchSuspend")
    suspend fun searchSuspend(
        search: SearchWebhookChannelsRequestApplicationJsonPropertySearch? = null,
        event: Event? = null,
        page: Long? = null,
        limit: Long? = null,
        requestOptions: RequestOptions? = null,
    ): Page<SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem> =
        sellapp.baseClient.runCancellable {
            search(search, event, page, limit, requestOptions)
        }

    /**
     * Coroutine-aware variant of [searchWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [searchWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("searchWithResponseSuspend")
    suspend fun searchWithResponseSuspend(
        search: SearchWebhookChannelsRequestApplicationJsonPropertySearch? = null,
        event: Event? = null,
        page: Long? = null,
        limit: Long? = null,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Page<SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem>> =
        sellapp.baseClient.runCancellable {
            searchWithResponse(search, event, page, limit, requestOptions)
        }

    /**
     * Rotate the webhook signing secret
     *
     * Replace the store-wide outbound webhook signing secret. The value is write-only and is never
     * returned or written to delivery logs. OAuth callers use the admin grant and select a store
     * with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param signingSecret the signing secret of the request.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkRotateWebhookSigningSecretResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun rotate(
        signingSecret: String,
        requestOptions: RequestOptions? = null,
    ): SdkRotateWebhookSigningSecretResponseValue200ApplicationJson {
        val body = bodyOf("signing_secret" to signingSecret)
        body["signing_secret"] = signingSecret
        val config =
            RequestConfig(
                method = "PUT",
                path = "/v2/webhook-channels/signing-secret",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkRotateWebhookSigningSecretRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models
                        .SdkRotateWebhookSigningSecretResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "rotateWebhookSigningSecret",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkRotateWebhookSigningSecretResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun rotateWithResponse(
        signingSecret: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkRotateWebhookSigningSecretResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        rotate(signingSecret, capturingOptions)
        return captured
            as? RawResponse<SdkRotateWebhookSigningSecretResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [rotate]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [rotate] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("rotateSuspend")
    suspend fun rotateSuspend(
        signingSecret: String,
        requestOptions: RequestOptions? = null,
    ): SdkRotateWebhookSigningSecretResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            rotate(signingSecret, requestOptions)
        }

    /**
     * Coroutine-aware variant of [rotateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [rotateWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("rotateWithResponseSuspend")
    suspend fun rotateWithResponseSuspend(
        signingSecret: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkRotateWebhookSigningSecretResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            rotateWithResponse(signingSecret, requestOptions)
        }

    /**
     * Retrieve a webhook channel
     *
     * Retrieve one outbound webhook destination by its stable UUID. OAuth callers use the admin
     * grant and select a store with X-STORE. Current membership and role permissions apply to every
     * request.
     *
     * @param webhookChannel The stable webhook channel UUID.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkGetWebhookChannelResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun get(
        webhookChannel: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetWebhookChannelResponseValue200ApplicationJson {
        val config =
            RequestConfig(
                method = "GET",
                path = "/v2/webhook-channels/${encodePathSegment(webhookChannel)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkGetWebhookChannelResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "getWebhookChannel",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkGetWebhookChannelResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun getWithResponse(
        webhookChannel: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetWebhookChannelResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        get(webhookChannel, capturingOptions)
        return captured as? RawResponse<SdkGetWebhookChannelResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [get]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [get] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getSuspend")
    suspend fun getSuspend(
        webhookChannel: String,
        requestOptions: RequestOptions? = null,
    ): SdkGetWebhookChannelResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            get(webhookChannel, requestOptions)
        }

    /**
     * Coroutine-aware variant of [getWithResponse]. Use this from a `suspend` function or coroutine
     * scope.
     *
     * Delegates to the blocking [getWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("getWithResponseSuspend")
    suspend fun getWithResponseSuspend(
        webhookChannel: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkGetWebhookChannelResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            getWithResponse(webhookChannel, requestOptions)
        }

    /**
     * Replace a webhook channel
     *
     * Replace a destination with a complete representation. name, url, and allowed_notifications
     * must all be present. OAuth callers use the admin grant and select a store with X-STORE.
     * Current membership and role permissions apply to every request.
     *
     * @param webhookChannel The stable webhook channel UUID.
     * @param name Optional seller-facing reference name.
     * @param url Public HTTP or HTTPS destination. The same canonical URL cannot be configured
     *   twice for one store.
     * @param allowedNotifications Complete set of public webhook event names enabled for this
     *   destination.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkReplaceWebhookChannelResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun replace(
        webhookChannel: String,
        name: String?,
        url: String,
        allowedNotifications: List<Event>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceWebhookChannelResponseValue200ApplicationJson {
        val body =
            bodyOf(
                "name" to name,
                "url" to url,
                "allowed_notifications" to allowedNotifications,
            )
        body["name"] = name
        body["url"] = url
        body["allowed_notifications"] = allowedNotifications
        val config =
            RequestConfig(
                method = "PUT",
                path = "/v2/webhook-channels/${encodePathSegment(webhookChannel)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkReplaceWebhookChannelRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkReplaceWebhookChannelResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "replaceWebhookChannel",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkReplaceWebhookChannelResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun replaceWithResponse(
        webhookChannel: String,
        name: String?,
        url: String,
        allowedNotifications: List<Event>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceWebhookChannelResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        replace(webhookChannel, name, url, allowedNotifications, capturingOptions)
        return captured as? RawResponse<SdkReplaceWebhookChannelResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [replace]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [replace] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceSuspend")
    suspend fun replaceSuspend(
        webhookChannel: String,
        name: String?,
        url: String,
        allowedNotifications: List<Event>,
        requestOptions: RequestOptions? = null,
    ): SdkReplaceWebhookChannelResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            replace(webhookChannel, name, url, allowedNotifications, requestOptions)
        }

    /**
     * Coroutine-aware variant of [replaceWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [replaceWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("replaceWithResponseSuspend")
    suspend fun replaceWithResponseSuspend(
        webhookChannel: String,
        name: String?,
        url: String,
        allowedNotifications: List<Event>,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkReplaceWebhookChannelResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            replaceWithResponse(webhookChannel, name, url, allowedNotifications, requestOptions)
        }

    /**
     * Update a webhook channel
     *
     * Partially update a destination. Supplying allowed_notifications replaces the complete event
     * filter; omitted fields retain their current values. OAuth callers use the admin grant and
     * select a store with X-STORE. Current membership and role permissions apply to every request.
     *
     * @param webhookChannel The stable webhook channel UUID.
     * @param name Optional seller-facing reference name.
     * @param url Public HTTP or HTTPS destination. The same canonical URL cannot be configured
     *   twice for one store.
     * @param allowedNotifications Complete set of public webhook event names enabled for this
     *   destination.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkUpdateWebhookChannelResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun update(
        webhookChannel: String,
        name: PatchField<String?> = PatchField.Absent,
        url: PatchField<String> = PatchField.Absent,
        allowedNotifications: PatchField<List<Event>> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateWebhookChannelResponseValue200ApplicationJson {
        val body =
            patchBodyOf(
                "name" to name,
                "url" to url,
                "allowed_notifications" to allowedNotifications,
            )
        val config =
            RequestConfig(
                method = "PATCH",
                path = "/v2/webhook-channels/${encodePathSegment(webhookChannel)}",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkUpdateWebhookChannelRequestApplicationJson
                        .runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkUpdateWebhookChannelResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "updateWebhookChannel",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkUpdateWebhookChannelResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun updateWithResponse(
        webhookChannel: String,
        name: PatchField<String?> = PatchField.Absent,
        url: PatchField<String> = PatchField.Absent,
        allowedNotifications: PatchField<List<Event>> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateWebhookChannelResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        update(webhookChannel, name, url, allowedNotifications, capturingOptions)
        return captured as? RawResponse<SdkUpdateWebhookChannelResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [update]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [update] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateSuspend")
    suspend fun updateSuspend(
        webhookChannel: String,
        name: PatchField<String?> = PatchField.Absent,
        url: PatchField<String> = PatchField.Absent,
        allowedNotifications: PatchField<List<Event>> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): SdkUpdateWebhookChannelResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            update(webhookChannel, name, url, allowedNotifications, requestOptions)
        }

    /**
     * Coroutine-aware variant of [updateWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [updateWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("updateWithResponseSuspend")
    suspend fun updateWithResponseSuspend(
        webhookChannel: String,
        name: PatchField<String?> = PatchField.Absent,
        url: PatchField<String> = PatchField.Absent,
        allowedNotifications: PatchField<List<Event>> = PatchField.Absent,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkUpdateWebhookChannelResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            updateWithResponse(webhookChannel, name, url, allowedNotifications, requestOptions)
        }

    /**
     * Delete a webhook channel
     *
     * Delete a destination. Other channel UUIDs remain unchanged and the deleted UUID is not
     * reassigned. OAuth callers use the admin grant and select a store with X-STORE. Current
     * membership and role permissions apply to every request.
     *
     * @param webhookChannel The stable webhook channel UUID.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     */
    @JvmOverloads
    fun delete(
        webhookChannel: String,
        requestOptions: RequestOptions? = null,
    ) {
        val config =
            RequestConfig(
                method = "DELETE",
                path = "/v2/webhook-channels/${encodePathSegment(webhookChannel)}",
                requestOptions = requestOptions,
                responseSchema =
                    app.sell.sellapp.models.SdkDeleteWebhookChannelResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "deleteWebhookChannel",
                supportsIdempotency = false,
            )
        sellapp.baseClient.requestVoid(config)
    }

    @Suppress("UNCHECKED_CAST")
    fun deleteWithResponse(
        webhookChannel: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        delete(webhookChannel, capturingOptions)
        return captured as? RawResponse<Unit> ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [delete]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [delete] under `withContext(Dispatchers.IO)`, so this is safe to
     * call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteSuspend")
    suspend fun deleteSuspend(
        webhookChannel: String,
        requestOptions: RequestOptions? = null,
    ) =
        sellapp.baseClient.runCancellable {
            delete(webhookChannel, requestOptions)
        }

    /**
     * Coroutine-aware variant of [deleteWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [deleteWithResponse] under `withContext(Dispatchers.IO)`, so this
     * is safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("deleteWithResponseSuspend")
    suspend fun deleteWithResponseSuspend(
        webhookChannel: String,
        requestOptions: RequestOptions? = null,
    ): RawResponse<Unit> =
        sellapp.baseClient.runCancellable {
            deleteWithResponse(webhookChannel, requestOptions)
        }

    /**
     * Send a test webhook
     *
     * Synchronously send one mock delivery to the stored destination. `event` is the public
     * delivery event name and the same name must already be enabled in `allowed_notifications`. The
     * test payload uses the same top-level event/version contract as production. Test deliveries do
     * not retry or follow redirects and never return response bodies or signing headers. OAuth
     * callers use the admin grant and select a store with X-STORE. Current membership and role
     * permissions apply to every request.
     *
     * @param webhookChannel The stable webhook channel UUID.
     * @param event Public event name used by channel configuration, search, tests, and delivered
     *   webhook payloads.
     * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
     * @return the SdkSendTestWebhookResponseValue200ApplicationJson
     */
    @JvmOverloads
    fun send(
        webhookChannel: String,
        event: Event,
        requestOptions: RequestOptions? = null,
    ): SdkSendTestWebhookResponseValue200ApplicationJson {
        val body = bodyOf("event" to event)
        body["event"] = event
        val config =
            RequestConfig(
                method = "POST",
                path = "/v2/webhook-channels/${encodePathSegment(webhookChannel)}/test",
                body = body,
                requestOptions = requestOptions,
                requestSchema =
                    app.sell.sellapp.models.SdkSendTestWebhookRequestApplicationJson.runtimeSchema,
                responseSchema =
                    app.sell.sellapp.models.SdkSendTestWebhookResponseValue200ApplicationJson
                        .runtimeSchema,
                operationId = "sendTestWebhook",
                supportsIdempotency = false,
            )
        return sellapp.baseClient.request(
            config,
            SdkSendTestWebhookResponseValue200ApplicationJson::class.java,
        )
    }

    @Suppress("UNCHECKED_CAST")
    fun sendWithResponse(
        webhookChannel: String,
        event: Event,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkSendTestWebhookResponseValue200ApplicationJson> {
        var captured: RawResponse<Any?>? = null
        val capturingOptions =
            (requestOptions?.copy() ?: RequestOptions()).also { options ->
                options.responseCapture = { response -> captured = response }
            }
        send(webhookChannel, event, capturingOptions)
        return captured as? RawResponse<SdkSendTestWebhookResponseValue200ApplicationJson>
            ?: error("Response metadata was not captured")
    }

    /**
     * Coroutine-aware variant of [send]. Use this from a `suspend` function or coroutine scope.
     *
     * Delegates to the blocking [send] under `withContext(Dispatchers.IO)`, so this is safe to call
     * from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("sendSuspend")
    suspend fun sendSuspend(
        webhookChannel: String,
        event: Event,
        requestOptions: RequestOptions? = null,
    ): SdkSendTestWebhookResponseValue200ApplicationJson =
        sellapp.baseClient.runCancellable {
            send(webhookChannel, event, requestOptions)
        }

    /**
     * Coroutine-aware variant of [sendWithResponse]. Use this from a `suspend` function or
     * coroutine scope.
     *
     * Delegates to the blocking [sendWithResponse] under `withContext(Dispatchers.IO)`, so this is
     * safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
     */
    @JvmName("sendWithResponseSuspend")
    suspend fun sendWithResponseSuspend(
        webhookChannel: String,
        event: Event,
        requestOptions: RequestOptions? = null,
    ): RawResponse<SdkSendTestWebhookResponseValue200ApplicationJson> =
        sellapp.baseClient.runCancellable {
            sendWithResponse(webhookChannel, event, requestOptions)
        }
}
