// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.wallet

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import app.sell.sellapp.types.SdkUpdateCustomerWalletStatusRequestApplicationJsonStatus
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.patchRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class WalletTest : TestBase() {
    private fun api() = Wallet(createSellAppClient())

    @Test
    fun `list returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/wallets",
            200,
            "{\"data\": [], \"list_metadata\": {\"before\": null, \"after\": null}}",
        )
        val result = api().list()
        assertNotNull(result)
    }

    @Test
    fun `get returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/wallets/sample-arg",
            200,
            "{\"data\": {\"id\": 0, \"customer_id\": 0, \"balance_cents\": 0, \"status\": \"active\"}}",
        )
        val result = api().get("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `adjust returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/wallets/sample-arg/adjustments",
            200,
            "{\"data\": {\"id\": 0, \"wallet_id\": 0, \"type\": \"TOP_UP\", \"amount_cents\": 0, \"balance_after_cents\": 0}}",
        )
        val result =
            api()
                .adjust(
                    "sample-arg",
                    amountCents = 0,
                    idempotencyKey = "sample-arg",
                    note = "sample-arg",
                )
        assertNotNull(result)
        wireMockRule.verify(
            postRequestedFor(urlPathMatching("/v2/wallets/sample-arg/adjustments"))
                .withRequestBody(matchingJsonPath("\$.amount_cents"))
                .withRequestBody(matchingJsonPath("\$.idempotency_key"))
                .withRequestBody(matchingJsonPath("\$.note"))
        )
    }

    @Test
    fun `updateStatus returns a typed response`() {
        stubResponse(
            "PATCH",
            "/v2/wallets/sample-arg/status",
            200,
            "{\"data\": {\"id\": 0, \"customer_id\": 0, \"balance_cents\": 0, \"status\": \"active\"}}",
        )
        val result =
            api()
                .updateStatus(
                    "sample-arg",
                    status =
                        SdkUpdateCustomerWalletStatusRequestApplicationJsonStatus.values().first {
                            it != SdkUpdateCustomerWalletStatusRequestApplicationJsonStatus.Unknown
                        },
                )
        assertNotNull(result)
        wireMockRule.verify(
            patchRequestedFor(urlPathMatching("/v2/wallets/sample-arg/status"))
                .withRequestBody(matchingJsonPath("\$.status"))
        )
    }

    @Test
    fun `list translates 401 to UnauthorizedException`() {
        stubResponse("GET", "/v2/wallets", 401)
        assertThrows(UnauthorizedException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 404 to NotFoundException`() {
        stubResponse("GET", "/v2/wallets", 404)
        assertThrows(NotFoundException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 429 to RateLimitException`() {
        stubResponse("GET", "/v2/wallets", 429)
        assertThrows(RateLimitException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 500 to GenericServerException`() {
        stubResponse("GET", "/v2/wallets", 500)
        assertThrows(GenericServerException::class.java) {
            api().list()
        }
    }
}
