// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.storecustomdomains

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class StoreCustomDomainsTest : TestBase() {
    private fun api() = StoreCustomDomains(createSellAppClient())

    @Test
    fun `list returns a typed response`() {
        stubResponse("GET", "/v2/store/custom-domains", 200, "{\"data\": []}")
        val result = api().list()
        assertNotNull(result)
    }

    @Test
    fun `connect returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/store/custom-domains",
            200,
            "{\"data\": {\"id\": 0, \"domain\": \"sample\", \"status\": \"pending\", \"cname_target\": \"sample\"}}",
        )
        val result = api().connect(domain = "sample-arg")
        assertNotNull(result)
        wireMockRule.verify(
            postRequestedFor(urlPathMatching("/v2/store/custom-domains"))
                .withRequestBody(matchingJsonPath("\$.domain"))
        )
    }

    @Test
    fun `get returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/store/custom-domains/sample-arg",
            200,
            "{\"data\": {\"id\": 0, \"domain\": \"sample\", \"status\": \"pending\", \"cname_target\": \"sample\"}}",
        )
        val result = api().get("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `disconnect completes without throwing`() {
        stubResponse(
            "DELETE",
            "/v2/store/custom-domains/sample-arg",
            204,
            "{\"data\": {\"id\": 0, \"domain\": \"sample\", \"status\": \"pending\", \"cname_target\": \"sample\"}}",
        )
        api().disconnect("sample-arg")
    }

    @Test
    fun `refresh returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/store/custom-domains/sample-arg/refresh",
            200,
            "{\"data\": {\"id\": 0, \"domain\": \"sample\", \"status\": \"pending\", \"cname_target\": \"sample\"}}",
        )
        val result = api().refresh("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `list translates 401 to UnauthorizedException`() {
        stubResponse("GET", "/v2/store/custom-domains", 401)
        assertThrows(UnauthorizedException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 404 to NotFoundException`() {
        stubResponse("GET", "/v2/store/custom-domains", 404)
        assertThrows(NotFoundException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 429 to RateLimitException`() {
        stubResponse("GET", "/v2/store/custom-domains", 429)
        assertThrows(RateLimitException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 500 to GenericServerException`() {
        stubResponse("GET", "/v2/store/custom-domains", 500)
        assertThrows(GenericServerException::class.java) {
            api().list()
        }
    }
}
