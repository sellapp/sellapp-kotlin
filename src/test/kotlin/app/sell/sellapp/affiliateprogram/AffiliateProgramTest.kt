// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.affiliateprogram

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class AffiliateProgramTest : TestBase() {
    private fun api() = AffiliateProgram(createSellAppClient())

    @Test
    fun `get returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/affiliate-program",
            200,
            "{\"data\": {\"id\": null, \"enabled\": false, \"settings\": {\"auto_approve_affiliates\": false, \"minimum_payout\": null, " +
                "\"commission\": {\"type\": \"percentage\", \"amount\": null}, \"referrer_type\": \"first_referrer\", \"tracking_length\": 1, " +
                "\"subscription_commission\": false, \"enabled_specific_products\": false, \"payout_methods\": [], \"enable_hub\": false}, " +
                "\"products\": [], \"created_at\": null, \"updated_at\": null}}",
        )
        val result = api().get()
        assertNotNull(result)
    }

    @Test
    fun `list returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/affiliate-invitations",
            200,
            "{\"data\": [], \"list_metadata\": {\"before\": null, \"after\": null}}",
        )
        val result = api().list()
        assertNotNull(result)
    }

    @Test
    fun `invite returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/affiliate-invitations",
            200,
            "{\"data\": {\"id\": 0, \"email\": \"sample\", \"status\": \"pending\", \"invited_at\": null, \"application_submitted_at\": null," +
                " \"created_at\": null}}",
        )
        val result = api().invite(email = "sample-arg")
        assertNotNull(result)
        wireMockRule.verify(
            postRequestedFor(urlPathMatching("/v2/affiliate-invitations"))
                .withRequestBody(matchingJsonPath("\$.email"))
        )
    }

    @Test
    fun `get translates 401 to UnauthorizedException`() {
        stubResponse("GET", "/v2/affiliate-program", 401)
        assertThrows(UnauthorizedException::class.java) {
            api().get()
        }
    }

    @Test
    fun `get translates 404 to NotFoundException`() {
        stubResponse("GET", "/v2/affiliate-program", 404)
        assertThrows(NotFoundException::class.java) {
            api().get()
        }
    }

    @Test
    fun `get translates 429 to RateLimitException`() {
        stubResponse("GET", "/v2/affiliate-program", 429)
        assertThrows(RateLimitException::class.java) {
            api().get()
        }
    }

    @Test
    fun `get translates 500 to GenericServerException`() {
        stubResponse("GET", "/v2/affiliate-program", 500)
        assertThrows(GenericServerException::class.java) {
            api().get()
        }
    }
}
