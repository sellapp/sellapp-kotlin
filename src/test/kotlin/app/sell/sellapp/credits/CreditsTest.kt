// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.credits

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import app.sell.sellapp.types.SdkRecordCreditTransactionRequestApplicationJsonKind
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class CreditsTest : TestBase() {
    private fun api() = Credits(createSellAppClient())

    @Test
    fun `record returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/credit-transactions",
            200,
            "{\"data\": {\"id\": 0, \"store_id\": 0, \"customer_id\": 0, \"product_id\": 0, \"kind\": \"grant\", \"amount_units\": 0, " +
                "\"balance_after_units\": 0, \"idempotency_key\": \"sample\"}}",
        )
        val result =
            api()
                .record(
                    customerId = 0,
                    productId = 0,
                    kind =
                        SdkRecordCreditTransactionRequestApplicationJsonKind.values().first {
                            it != SdkRecordCreditTransactionRequestApplicationJsonKind.Unknown
                        },
                    amountUnits = 0,
                    idempotencyKey = "sample-arg",
                )
        assertNotNull(result)
        wireMockRule.verify(
            postRequestedFor(urlPathMatching("/v2/credit-transactions"))
                .withRequestBody(matchingJsonPath("\$.customer_id"))
                .withRequestBody(matchingJsonPath("\$.product_id"))
                .withRequestBody(matchingJsonPath("\$.kind"))
                .withRequestBody(matchingJsonPath("\$.amount_units"))
                .withRequestBody(matchingJsonPath("\$.idempotency_key"))
        )
    }

    @Test
    fun `record translates 401 to UnauthorizedException`() {
        stubResponse("POST", "/v2/credit-transactions", 401)
        assertThrows(UnauthorizedException::class.java) {
            api()
                .record(
                    customerId = 0,
                    productId = 0,
                    kind =
                        SdkRecordCreditTransactionRequestApplicationJsonKind.values().first {
                            it != SdkRecordCreditTransactionRequestApplicationJsonKind.Unknown
                        },
                    amountUnits = 0,
                    idempotencyKey = "sample-arg",
                )
        }
    }

    @Test
    fun `record translates 404 to NotFoundException`() {
        stubResponse("POST", "/v2/credit-transactions", 404)
        assertThrows(NotFoundException::class.java) {
            api()
                .record(
                    customerId = 0,
                    productId = 0,
                    kind =
                        SdkRecordCreditTransactionRequestApplicationJsonKind.values().first {
                            it != SdkRecordCreditTransactionRequestApplicationJsonKind.Unknown
                        },
                    amountUnits = 0,
                    idempotencyKey = "sample-arg",
                )
        }
    }

    @Test
    fun `record translates 429 to RateLimitException`() {
        stubResponse("POST", "/v2/credit-transactions", 429)
        assertThrows(RateLimitException::class.java) {
            api()
                .record(
                    customerId = 0,
                    productId = 0,
                    kind =
                        SdkRecordCreditTransactionRequestApplicationJsonKind.values().first {
                            it != SdkRecordCreditTransactionRequestApplicationJsonKind.Unknown
                        },
                    amountUnits = 0,
                    idempotencyKey = "sample-arg",
                )
        }
    }

    @Test
    fun `record translates 500 to GenericServerException`() {
        stubResponse("POST", "/v2/credit-transactions", 500)
        assertThrows(GenericServerException::class.java) {
            api()
                .record(
                    customerId = 0,
                    productId = 0,
                    kind =
                        SdkRecordCreditTransactionRequestApplicationJsonKind.values().first {
                            it != SdkRecordCreditTransactionRequestApplicationJsonKind.Unknown
                        },
                    amountUnits = 0,
                    idempotencyKey = "sample-arg",
                )
        }
    }
}
