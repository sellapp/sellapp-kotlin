// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.walletbonustiers

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import app.sell.sellapp.types.SdkCreateWalletBonusTierRequestApplicationJsonBonusKind
import app.sell.sellapp.types.SdkReplaceWalletBonusTierRequestApplicationJsonBonusKind
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.putRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class WalletBonusTiersTest : TestBase() {
    private fun api() = WalletBonusTiers(createSellAppClient())

    @Test
    fun `list returns a typed response`() {
        stubResponse("GET", "/v2/wallet-bonus-tiers", 200, "{\"data\": []}")
        val result = api().list()
        assertNotNull(result)
    }

    @Test
    fun `create returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/wallet-bonus-tiers",
            200,
            "{\"data\": {\"id\": 0, \"minimum_top_up_cents\": 1, \"bonus_kind\": \"fixed\", \"fixed_bonus_cents\": null, \"percent_basis\": " +
                "null, \"maximum_bonus_cents\": null, \"priority\": 0, \"is_active\": false, \"superseded_by_id\": null, \"restorable\": false}}",
        )
        val result =
            api()
                .create(
                    minimumTopUpCents = 1,
                    bonusKind =
                        SdkCreateWalletBonusTierRequestApplicationJsonBonusKind.values().first {
                            it != SdkCreateWalletBonusTierRequestApplicationJsonBonusKind.Unknown
                        },
                    fixedBonusCents = null,
                    percentBasis = null,
                    maximumBonusCents = null,
                    priority = 0,
                    isActive = false,
                )
        assertNotNull(result)
        wireMockRule.verify(
            postRequestedFor(urlPathMatching("/v2/wallet-bonus-tiers"))
                .withRequestBody(matchingJsonPath("\$.minimum_top_up_cents"))
                .withRequestBody(matchingJsonPath("\$.bonus_kind"))
                .withRequestBody(matchingJsonPath("\$[?(@.fixed_bonus_cents == null)]"))
                .withRequestBody(matchingJsonPath("\$[?(@.percent_basis == null)]"))
                .withRequestBody(matchingJsonPath("\$[?(@.maximum_bonus_cents == null)]"))
                .withRequestBody(matchingJsonPath("\$.priority"))
                .withRequestBody(matchingJsonPath("\$.is_active"))
        )
    }

    @Test
    fun `replace returns a typed response`() {
        stubResponse(
            "PUT",
            "/v2/wallet-bonus-tiers/sample-arg",
            200,
            "{\"data\": {\"id\": 0, \"minimum_top_up_cents\": 1, \"bonus_kind\": \"fixed\", \"fixed_bonus_cents\": null, \"percent_basis\": " +
                "null, \"maximum_bonus_cents\": null, \"priority\": 0, \"is_active\": false, \"superseded_by_id\": null, \"restorable\": false}}",
        )
        val result =
            api()
                .replace(
                    "sample-arg",
                    minimumTopUpCents = 1,
                    bonusKind =
                        SdkReplaceWalletBonusTierRequestApplicationJsonBonusKind.values().first {
                            it != SdkReplaceWalletBonusTierRequestApplicationJsonBonusKind.Unknown
                        },
                    fixedBonusCents = null,
                    percentBasis = null,
                    maximumBonusCents = null,
                    priority = 0,
                    isActive = false,
                )
        assertNotNull(result)
        wireMockRule.verify(
            putRequestedFor(urlPathMatching("/v2/wallet-bonus-tiers/sample-arg"))
                .withRequestBody(matchingJsonPath("\$.minimum_top_up_cents"))
                .withRequestBody(matchingJsonPath("\$.bonus_kind"))
                .withRequestBody(matchingJsonPath("\$[?(@.fixed_bonus_cents == null)]"))
                .withRequestBody(matchingJsonPath("\$[?(@.percent_basis == null)]"))
                .withRequestBody(matchingJsonPath("\$[?(@.maximum_bonus_cents == null)]"))
                .withRequestBody(matchingJsonPath("\$.priority"))
                .withRequestBody(matchingJsonPath("\$.is_active"))
        )
    }

    @Test
    fun `update returns a typed response`() {
        stubResponse(
            "PATCH",
            "/v2/wallet-bonus-tiers/sample-arg",
            200,
            "{\"data\": {\"id\": 0, \"minimum_top_up_cents\": 1, \"bonus_kind\": \"fixed\", \"fixed_bonus_cents\": null, \"percent_basis\": " +
                "null, \"maximum_bonus_cents\": null, \"priority\": 0, \"is_active\": false, \"superseded_by_id\": null, \"restorable\": false}}",
        )
        val result = api().update("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `archive completes without throwing`() {
        stubResponse(
            "DELETE",
            "/v2/wallet-bonus-tiers/sample-arg",
            204,
            "{\"data\": {\"id\": 0, \"minimum_top_up_cents\": 1, \"bonus_kind\": \"fixed\", \"fixed_bonus_cents\": null, \"percent_basis\": null, \"maximum_bonus_cents\": null, \"priority\": 0, \"is_active\": false, \"superseded_by_id\": null, \"restorable\": false}}",
        )
        api().archive("sample-arg")
    }

    @Test
    fun `restore returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/wallet-bonus-tiers/sample-arg/restore",
            200,
            "{\"data\": {\"id\": 0, \"minimum_top_up_cents\": 1, \"bonus_kind\": \"fixed\", \"fixed_bonus_cents\": null, \"percent_basis\": " +
                "null, \"maximum_bonus_cents\": null, \"priority\": 0, \"is_active\": false, \"superseded_by_id\": null, \"restorable\": false}}",
        )
        val result = api().restore("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `list translates 401 to UnauthorizedException`() {
        stubResponse("GET", "/v2/wallet-bonus-tiers", 401)
        assertThrows(UnauthorizedException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 404 to NotFoundException`() {
        stubResponse("GET", "/v2/wallet-bonus-tiers", 404)
        assertThrows(NotFoundException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 429 to RateLimitException`() {
        stubResponse("GET", "/v2/wallet-bonus-tiers", 429)
        assertThrows(RateLimitException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 500 to GenericServerException`() {
        stubResponse("GET", "/v2/wallet-bonus-tiers", 500)
        assertThrows(GenericServerException::class.java) {
            api().list()
        }
    }
}
