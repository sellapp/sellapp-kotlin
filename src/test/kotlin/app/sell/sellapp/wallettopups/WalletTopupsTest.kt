// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.wallettopups

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import app.sell.sellapp.types.SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class WalletTopupsTest : TestBase() {
    private fun api() = WalletTopups(createSellAppClient())

    @Test
    fun `create returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/wallets/sample-arg/top-ups",
            200,
            "{\"data\": {\"charge_id\": 0, \"amount_cents\": 1, \"currency\": \"USD\", \"checkout_url\": \"sample\", \"payment_method\": " +
                "\"AUTHNET\", \"custom_payment_method_id\": null}}",
        )
        val result =
            api()
                .create(
                    "sample-arg",
                    amountCents = 1,
                    paymentMethod =
                        SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod.values().first {
                            it != SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod.Unknown
                        },
                )
        assertNotNull(result)
        wireMockRule.verify(
            postRequestedFor(urlPathMatching("/v2/wallets/sample-arg/top-ups"))
                .withRequestBody(matchingJsonPath("\$.amount_cents"))
                .withRequestBody(matchingJsonPath("\$.payment_method"))
        )
    }

    @Test
    fun `create translates 401 to UnauthorizedException`() {
        stubResponse("POST", "/v2/wallets/sample-arg/top-ups", 401)
        assertThrows(UnauthorizedException::class.java) {
            api()
                .create(
                    "sample-arg",
                    amountCents = 1,
                    paymentMethod =
                        SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod.values().first {
                            it != SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod.Unknown
                        },
                )
        }
    }

    @Test
    fun `create translates 404 to NotFoundException`() {
        stubResponse("POST", "/v2/wallets/sample-arg/top-ups", 404)
        assertThrows(NotFoundException::class.java) {
            api()
                .create(
                    "sample-arg",
                    amountCents = 1,
                    paymentMethod =
                        SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod.values().first {
                            it != SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod.Unknown
                        },
                )
        }
    }

    @Test
    fun `create translates 429 to RateLimitException`() {
        stubResponse("POST", "/v2/wallets/sample-arg/top-ups", 429)
        assertThrows(RateLimitException::class.java) {
            api()
                .create(
                    "sample-arg",
                    amountCents = 1,
                    paymentMethod =
                        SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod.values().first {
                            it != SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod.Unknown
                        },
                )
        }
    }

    @Test
    fun `create translates 500 to GenericServerException`() {
        stubResponse("POST", "/v2/wallets/sample-arg/top-ups", 500)
        assertThrows(GenericServerException::class.java) {
            api()
                .create(
                    "sample-arg",
                    amountCents = 1,
                    paymentMethod =
                        SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod.values().first {
                            it != SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod.Unknown
                        },
                )
        }
    }
}
