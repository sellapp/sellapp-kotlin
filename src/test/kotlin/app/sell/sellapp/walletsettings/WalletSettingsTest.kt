// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.walletsettings

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.putRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class WalletSettingsTest : TestBase() {
    private fun api() = WalletSettings(createSellAppClient())

    @Test
    fun `get returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/wallet/settings",
            200,
            "{\"data\": {\"enabled\": false, \"minimum_top_up_cents\": null, \"maximum_top_up_cents\": null, \"expiration_days\": null, " +
                "\"payment_methods\": []}}",
        )
        val result = api().get()
        assertNotNull(result)
    }

    @Test
    fun `replace returns a typed response`() {
        stubResponse(
            "PUT",
            "/v2/wallet/settings",
            200,
            "{\"data\": {\"enabled\": false, \"minimum_top_up_cents\": null, \"maximum_top_up_cents\": null, \"expiration_days\": null, " +
                "\"payment_methods\": []}}",
        )
        val result =
            api()
                .replace(
                    enabled = false,
                    minimumTopUpCents = null,
                    maximumTopUpCents = null,
                    expirationDays = null,
                    paymentMethods = emptyList<String>(),
                )
        assertNotNull(result)
        wireMockRule.verify(
            putRequestedFor(urlPathMatching("/v2/wallet/settings"))
                .withRequestBody(matchingJsonPath("\$.enabled"))
                .withRequestBody(matchingJsonPath("\$[?(@.minimum_top_up_cents == null)]"))
                .withRequestBody(matchingJsonPath("\$[?(@.maximum_top_up_cents == null)]"))
                .withRequestBody(matchingJsonPath("\$[?(@.expiration_days == null)]"))
        )
    }

    @Test
    fun `get translates 401 to UnauthorizedException`() {
        stubResponse("GET", "/v2/wallet/settings", 401)
        assertThrows(UnauthorizedException::class.java) {
            api().get()
        }
    }

    @Test
    fun `get translates 404 to NotFoundException`() {
        stubResponse("GET", "/v2/wallet/settings", 404)
        assertThrows(NotFoundException::class.java) {
            api().get()
        }
    }

    @Test
    fun `get translates 429 to RateLimitException`() {
        stubResponse("GET", "/v2/wallet/settings", 429)
        assertThrows(RateLimitException::class.java) {
            api().get()
        }
    }

    @Test
    fun `get translates 500 to GenericServerException`() {
        stubResponse("GET", "/v2/wallet/settings", 500)
        assertThrows(GenericServerException::class.java) {
            api().get()
        }
    }
}
