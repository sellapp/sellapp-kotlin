// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.oauthmanagement

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class OAuthManagementTest : TestBase() {
    private fun api() = OAuthManagement(createSellAppClient("oauth_test"))

    @Test
    fun `getInstallation returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/oauth/installation",
            200,
            "{\"data\": {\"id\": \"sample\", \"client_id\": \"sample\", \"grant_model\": \"account\", \"scopes\": [], \"created_at\": null}}",
        )
        val result = api().getInstallation()
        assertNotNull(result)
    }

    @Test
    fun `deleteInstallation completes without throwing`() {
        stubResponse("DELETE", "/v2/oauth/installation", 204)
        api().deleteInstallation()
    }

    @Test
    fun `getInstallation translates 401 to UnauthorizedException`() {
        stubResponse("GET", "/v2/oauth/installation", 401)
        assertThrows(UnauthorizedException::class.java) {
            api().getInstallation()
        }
    }

    @Test
    fun `getInstallation translates 404 to NotFoundException`() {
        stubResponse("GET", "/v2/oauth/installation", 404)
        assertThrows(NotFoundException::class.java) {
            api().getInstallation()
        }
    }

    @Test
    fun `getInstallation translates 429 to RateLimitException`() {
        stubResponse("GET", "/v2/oauth/installation", 429)
        assertThrows(RateLimitException::class.java) {
            api().getInstallation()
        }
    }

    @Test
    fun `getInstallation translates 500 to GenericServerException`() {
        stubResponse("GET", "/v2/oauth/installation", 500)
        assertThrows(GenericServerException::class.java) {
            api().getInstallation()
        }
    }
}
