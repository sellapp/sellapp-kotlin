// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.subscriptions

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.patchRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import java.time.OffsetDateTime
import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class SubscriptionsTest : TestBase() {
    private fun api() = Subscriptions(createSellAppClient())

    @Test
    fun `cancel returns a typed response`() {
        stubResponse(
            "PATCH",
            "/v2/subscriptions/sample-arg/cancel",
            200,
            "{\"data\": {\"id\": 0, \"invoice_id\": null, \"provider\": \"sample\", \"subscription_id\": \"sample\", \"customer_id\": " +
                "\"sample\", \"customer_email\": \"sample\", \"status\": \"sample\", \"current_period_start\": \"2024-01-01T00:00:00Z\", " +
                "\"current_period_end\": \"2024-01-01T00:00:00Z\", \"cancel_at_period_end\": false, \"cancellation_in_progress\": false, " +
                "\"store_id\": 0, \"product_variant_id\": 0, \"created_at\": \"2024-01-01T00:00:00Z\", \"updated_at\": \"2024-01-01T00:00:00Z\"}," +
                " \"message\": \"sample\"}",
        )
        val result = api().cancel("sample-arg", cancelAtPeriodEnd = false)
        assertNotNull(result)
        assertEquals("sample", result.message)
        wireMockRule.verify(
            patchRequestedFor(urlPathMatching("/v2/subscriptions/sample-arg/cancel"))
                .withRequestBody(matchingJsonPath("\$.cancel_at_period_end"))
        )
    }

    @Test
    fun `getCapabilities returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/subscriptions/sample-arg/capabilities",
            200,
            "{\"data\": {\"id\": 0, \"subscription_id\": null, \"capabilities\": {}}}",
        )
        val result = api().getCapabilities("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `cancelAtPeriodEnd returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/subscriptions/sample-arg/actions/cancel-period-end",
            200,
            "{\"data\": {}, \"message\": \"sample\"}",
        )
        val result = api().cancelAtPeriodEnd("sample-arg")
        assertNotNull(result)
        assertEquals("sample", result.message)
    }

    @Test
    fun `cancelImmediately returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/subscriptions/sample-arg/actions/cancel-immediately",
            200,
            "{\"data\": {}, \"message\": \"sample\"}",
        )
        val result = api().cancelImmediately("sample-arg")
        assertNotNull(result)
        assertEquals("sample", result.message)
    }

    @Test
    fun `pause returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/subscriptions/sample-arg/actions/pause",
            200,
            "{\"data\": {}, \"message\": \"sample\"}",
        )
        val result = api().pause("sample-arg")
        assertNotNull(result)
        assertEquals("sample", result.message)
    }

    @Test
    fun `resume returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/subscriptions/sample-arg/actions/resume",
            200,
            "{\"data\": {}, \"message\": \"sample\"}",
        )
        val result = api().resume("sample-arg")
        assertNotNull(result)
        assertEquals("sample", result.message)
    }

    @Test
    fun `updatePaymentMethod completes without throwing`() {
        stubResponse(
            "POST",
            "/v2/subscriptions/sample-arg/actions/update-payment-method",
            200,
            "\"sample\"",
        )
        api().updatePaymentMethod("sample-arg")
    }

    @Test
    fun `previewPlanChange completes without throwing`() {
        stubResponse(
            "POST",
            "/v2/subscriptions/sample-arg/actions/change-plan/preview",
            200,
            "\"sample\"",
        )
        api().previewPlanChange("sample-arg", targetVariantId = 0)
        wireMockRule.verify(
            postRequestedFor(
                    urlPathMatching("/v2/subscriptions/sample-arg/actions/change-plan/preview")
                )
                .withRequestBody(matchingJsonPath("\$.target_variant_id"))
        )
    }

    @Test
    fun `confirmPlanChange completes without throwing`() {
        stubResponse(
            "POST",
            "/v2/subscriptions/sample-arg/actions/change-plan/confirm",
            200,
            "\"sample\"",
        )
        api().confirmPlanChange("sample-arg", targetVariantId = 0, previewToken = "sample-arg")
        wireMockRule.verify(
            postRequestedFor(
                    urlPathMatching("/v2/subscriptions/sample-arg/actions/change-plan/confirm")
                )
                .withRequestBody(matchingJsonPath("\$.target_variant_id"))
                .withRequestBody(matchingJsonPath("\$.preview_token"))
        )
    }

    @Test
    fun `previewRenewalDate returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/subscriptions/sample-arg/actions/change-renewal-date/preview",
            200,
            "{\"data\": {}, \"message\": \"sample\"}",
        )
        val result = api().previewRenewalDate("sample-arg", renewalDate = OffsetDateTime.now())
        assertNotNull(result)
        assertEquals("sample", result.message)
        wireMockRule.verify(
            postRequestedFor(
                    urlPathMatching(
                        "/v2/subscriptions/sample-arg/actions/change-renewal-date/preview"
                    )
                )
                .withRequestBody(matchingJsonPath("\$.renewal_date"))
        )
    }

    @Test
    fun `confirmRenewalDate returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/subscriptions/sample-arg/actions/change-renewal-date/confirm",
            200,
            "{\"data\": {}, \"message\": \"sample\"}",
        )
        val result = api().confirmRenewalDate("sample-arg", renewalDate = OffsetDateTime.now())
        assertNotNull(result)
        assertEquals("sample", result.message)
        wireMockRule.verify(
            postRequestedFor(
                    urlPathMatching(
                        "/v2/subscriptions/sample-arg/actions/change-renewal-date/confirm"
                    )
                )
                .withRequestBody(matchingJsonPath("\$.renewal_date"))
        )
    }

    @Test
    fun `list returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/subscriptions",
            200,
            "{\"data\": [], \"list_metadata\": {\"before\": null, \"after\": null}}",
        )
        val result = api().list()
        assertNotNull(result)
    }

    @Test
    fun `search returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/subscriptions/search",
            200,
            "{\"data\": [], \"list_metadata\": {\"before\": null, \"after\": null}}",
        )
        val result = api().search()
        assertNotNull(result)
    }

    @Test
    fun `get returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/subscriptions/sample-arg",
            200,
            "{\"data\": {\"id\": 0, \"status\": \"sample\", \"provider_subscription_id\": null, \"provider_customer_id\": null, " +
                "\"customer\": {\"id\": 0, \"email\": \"sample\", \"external_id\": null, \"name\": null, \"locale\": null, \"metadata\": {}}}}",
        )
        val result = api().get("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `list translates 401 to UnauthorizedException`() {
        stubResponse("GET", "/v2/subscriptions", 401)
        assertThrows(UnauthorizedException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 404 to NotFoundException`() {
        stubResponse("GET", "/v2/subscriptions", 404)
        assertThrows(NotFoundException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 429 to RateLimitException`() {
        stubResponse("GET", "/v2/subscriptions", 429)
        assertThrows(RateLimitException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 500 to GenericServerException`() {
        stubResponse("GET", "/v2/subscriptions", 500)
        assertThrows(GenericServerException::class.java) {
            api().list()
        }
    }
}
