// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.account

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class AccountTest : TestBase() {
    private fun api() = Account(createSellAppClient())

    @Test
    fun `get returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/me",
            200,
            "{\"data\": {\"id\": \"sample\", \"username\": null, \"email\": \"sample\", \"credential_type\": \"sanctum\"}}",
        )
        val result = api().get()
        assertNotNull(result)
    }

    @Test
    fun `listStores returns a typed response`() {
        stubResponse("GET", "/v2/stores", 200, "{\"data\": []}")
        val result = api().listStores()
        assertNotNull(result)
    }

    @Test
    fun `createStore returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/stores",
            200,
            "{\"data\": {\"id\": \"sample\", \"slug\": \"sample\", \"name\": \"sample\", \"visibility\": \"PUBLIC\", \"storefront_url\": " +
                "\"sample\", \"dashboard_url\": \"sample\"}}",
        )
        val result =
            api()
                .createStore(
                    name = "00000000000000000000000000",
                    slug = "00000000000000000000000000",
                )
        assertNotNull(result)
        wireMockRule.verify(
            postRequestedFor(urlPathMatching("/v2/stores"))
                .withRequestBody(matchingJsonPath("\$.name"))
                .withRequestBody(matchingJsonPath("\$.slug"))
        )
    }

    @Test
    fun `getStore returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/stores/sample-arg",
            200,
            "{\"data\": {\"id\": \"sample\", \"slug\": \"sample\", \"name\": \"sample\"}}",
        )
        val result = api().getStore("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `getStorePermissions returns a typed response`() {
        stubResponse("GET", "/v2/permissions", 200, "{\"data\": {}}")
        val result = api().getStorePermissions()
        assertNotNull(result)
    }

    @Test
    fun `get translates 401 to UnauthorizedException`() {
        stubResponse("GET", "/v2/me", 401)
        assertThrows(UnauthorizedException::class.java) {
            api().get()
        }
    }

    @Test
    fun `get translates 404 to NotFoundException`() {
        stubResponse("GET", "/v2/me", 404)
        assertThrows(NotFoundException::class.java) {
            api().get()
        }
    }

    @Test
    fun `get translates 429 to RateLimitException`() {
        stubResponse("GET", "/v2/me", 429)
        assertThrows(RateLimitException::class.java) {
            api().get()
        }
    }

    @Test
    fun `get translates 500 to GenericServerException`() {
        stubResponse("GET", "/v2/me", 500)
        assertThrows(GenericServerException::class.java) {
            api().get()
        }
    }
}
