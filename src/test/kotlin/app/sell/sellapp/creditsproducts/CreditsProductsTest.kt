// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.creditsproducts

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import app.sell.sellapp.types.CatalogVisibility
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class CreditsProductsTest : TestBase() {
    private fun api() = CreditsProducts(createSellAppClient())

    @Test
    fun `list returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/credit-products",
            200,
            "{\"data\": [], \"list_metadata\": {\"before\": null, \"after\": null}}",
        )
        val result = api().list()
        assertNotNull(result)
    }

    @Test
    fun `create returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/credit-products",
            200,
            "{\"data\": {\"id\": 0, \"store_id\": 0, \"title\": \"sample\", \"slug\": \"sample\", \"visibility\": \"PUBLIC\", \"type\": " +
                "\"credits\", \"is_draft\": false, \"is_discoverable\": false, \"default_variant\": null}}",
        )
        val result =
            api()
                .create(
                    title = "sample-arg",
                    visibility =
                        CatalogVisibility.values().first { it != CatalogVisibility.Unknown },
                )
        assertNotNull(result)
        wireMockRule.verify(
            postRequestedFor(urlPathMatching("/v2/credit-products"))
                .withRequestBody(matchingJsonPath("\$.title"))
                .withRequestBody(matchingJsonPath("\$.visibility"))
        )
    }

    @Test
    fun `search returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/credit-products/search",
            200,
            "{\"data\": [], \"list_metadata\": {\"before\": null, \"after\": null}}",
        )
        val result = api().search()
        assertNotNull(result)
    }

    @Test
    fun `get returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/credit-products/sample-arg",
            200,
            "{\"data\": {\"id\": 0, \"store_id\": 0, \"title\": \"sample\", \"slug\": \"sample\", \"visibility\": \"PUBLIC\", \"type\": " +
                "\"credits\", \"is_draft\": false, \"is_discoverable\": false, \"default_variant\": null}}",
        )
        val result = api().get("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `replace returns a typed response`() {
        stubResponse(
            "PUT",
            "/v2/credit-products/sample-arg",
            200,
            "{\"data\": {\"id\": 0, \"store_id\": 0, \"title\": \"sample\", \"slug\": \"sample\", \"visibility\": \"PUBLIC\", \"type\": " +
                "\"credits\", \"is_draft\": false, \"is_discoverable\": false, \"default_variant\": null}}",
        )
        val result = api().replace("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `update returns a typed response`() {
        stubResponse(
            "PATCH",
            "/v2/credit-products/sample-arg",
            200,
            "{\"data\": {\"id\": 0, \"store_id\": 0, \"title\": \"sample\", \"slug\": \"sample\", \"visibility\": \"PUBLIC\", \"type\": " +
                "\"credits\", \"is_draft\": false, \"is_discoverable\": false, \"default_variant\": null}}",
        )
        val result = api().update("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `delete completes without throwing`() {
        stubResponse(
            "DELETE",
            "/v2/credit-products/sample-arg",
            204,
            "{\"data\": {\"id\": 0, \"store_id\": 0, \"title\": \"sample\", \"slug\": \"sample\", \"visibility\": \"PUBLIC\", \"type\": \"credits\", \"is_draft\": false, \"is_discoverable\": false, \"default_variant\": null}}",
        )
        api().delete("sample-arg")
    }

    @Test
    fun `list translates 401 to UnauthorizedException`() {
        stubResponse("GET", "/v2/credit-products", 401)
        assertThrows(UnauthorizedException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 404 to NotFoundException`() {
        stubResponse("GET", "/v2/credit-products", 404)
        assertThrows(NotFoundException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 429 to RateLimitException`() {
        stubResponse("GET", "/v2/credit-products", 429)
        assertThrows(RateLimitException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 500 to GenericServerException`() {
        stubResponse("GET", "/v2/credit-products", 500)
        assertThrows(GenericServerException::class.java) {
            api().list()
        }
    }
}
