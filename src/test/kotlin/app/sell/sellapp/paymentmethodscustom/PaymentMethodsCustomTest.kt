// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.paymentmethodscustom

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import app.sell.sellapp.types.SdkCreateCustomPaymentMethodRequestApplicationJsonType
import app.sell.sellapp.types.SdkReplaceCustomPaymentMethodRequestApplicationJsonType
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.putRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class PaymentMethodsCustomTest : TestBase() {
    private fun api() = PaymentMethodsCustom(createSellAppClient())

    @Test
    fun `list returns a typed response`() {
        stubResponse("GET", "/v2/payment-methods/custom", 200, "{\"data\": []}")
        val result = api().list()
        assertNotNull(result)
    }

    @Test
    fun `create returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/payment-methods/custom",
            200,
            "{\"data\": {\"id\": \"sample\", \"type\": \"instructions\", \"name\": \"sample\", \"enabled\": false, \"sort_order\": 0}}",
        )
        val result =
            api()
                .create(
                    type =
                        SdkCreateCustomPaymentMethodRequestApplicationJsonType.values().first {
                            it != SdkCreateCustomPaymentMethodRequestApplicationJsonType.Unknown
                        },
                    name = "sample-arg",
                )
        assertNotNull(result)
        wireMockRule.verify(
            postRequestedFor(urlPathMatching("/v2/payment-methods/custom"))
                .withRequestBody(matchingJsonPath("\$.type"))
                .withRequestBody(matchingJsonPath("\$.name"))
        )
    }

    @Test
    fun `get returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/payment-methods/custom/sample-arg",
            200,
            "{\"data\": {\"id\": \"sample\", \"type\": \"instructions\", \"name\": \"sample\", \"enabled\": false, \"sort_order\": 0}}",
        )
        val result = api().get("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `replace returns a typed response`() {
        stubResponse(
            "PUT",
            "/v2/payment-methods/custom/sample-arg",
            200,
            "{\"data\": {\"id\": \"sample\", \"type\": \"instructions\", \"name\": \"sample\", \"enabled\": false, \"sort_order\": 0}}",
        )
        val result =
            api()
                .replace(
                    "sample-arg",
                    type =
                        SdkReplaceCustomPaymentMethodRequestApplicationJsonType.values().first {
                            it != SdkReplaceCustomPaymentMethodRequestApplicationJsonType.Unknown
                        },
                    name = "sample-arg",
                )
        assertNotNull(result)
        wireMockRule.verify(
            putRequestedFor(urlPathMatching("/v2/payment-methods/custom/sample-arg"))
                .withRequestBody(matchingJsonPath("\$.type"))
                .withRequestBody(matchingJsonPath("\$.name"))
        )
    }

    @Test
    fun `update returns a typed response`() {
        stubResponse(
            "PATCH",
            "/v2/payment-methods/custom/sample-arg",
            200,
            "{\"data\": {\"id\": \"sample\", \"type\": \"instructions\", \"name\": \"sample\", \"enabled\": false, \"sort_order\": 0}}",
        )
        val result = api().update("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `delete completes without throwing`() {
        stubResponse(
            "DELETE",
            "/v2/payment-methods/custom/sample-arg",
            204,
            "{\"data\": {\"id\": \"sample\", \"type\": \"instructions\", \"name\": \"sample\", \"enabled\": false, \"sort_order\": 0}}",
        )
        api().delete("sample-arg")
    }

    @Test
    fun `list translates 401 to UnauthorizedException`() {
        stubResponse("GET", "/v2/payment-methods/custom", 401)
        assertThrows(UnauthorizedException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 404 to NotFoundException`() {
        stubResponse("GET", "/v2/payment-methods/custom", 404)
        assertThrows(NotFoundException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 429 to RateLimitException`() {
        stubResponse("GET", "/v2/payment-methods/custom", 429)
        assertThrows(RateLimitException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 500 to GenericServerException`() {
        stubResponse("GET", "/v2/payment-methods/custom", 500)
        assertThrows(GenericServerException::class.java) {
            api().list()
        }
    }
}
