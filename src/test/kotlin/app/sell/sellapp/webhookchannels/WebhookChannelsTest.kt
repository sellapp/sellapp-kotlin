// This file is auto-generated by oagen. Do not edit.

package app.sell.sellapp.webhookchannels

import app.sell.sellapp.common.exceptions.GenericServerException
import app.sell.sellapp.common.exceptions.NotFoundException
import app.sell.sellapp.common.exceptions.RateLimitException
import app.sell.sellapp.common.exceptions.UnauthorizedException
import app.sell.sellapp.test.TestBase
import app.sell.sellapp.types.SdkCreateWebhookChannelRequestApplicationJsonAllowedNotifications
import app.sell.sellapp.types.SdkReplaceWebhookChannelRequestApplicationJsonAllowedNotifications
import app.sell.sellapp.types.SdkSendTestWebhookRequestApplicationJsonEvent
import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.putRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class WebhookChannelsTest : TestBase() {
    private fun api() = WebhookChannels(createSellAppClient())

    @Test
    fun `list returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/webhook-channels",
            200,
            "{\"data\": [], \"list_metadata\": {\"before\": null, \"after\": null}}",
        )
        val result = api().list()
        assertNotNull(result)
    }

    @Test
    fun `create returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/webhook-channels",
            200,
            "{\"data\": {\"id\": \"sample\", \"name\": null, \"url\": \"sample\", \"allowed_notifications\": [], " +
                "\"signing_secret_configured\": false}}",
        )
        val result =
            api()
                .create(
                    url = "sample-arg",
                    allowedNotifications =
                        emptyList<
                            SdkCreateWebhookChannelRequestApplicationJsonAllowedNotifications
                        >(),
                )
        assertNotNull(result)
        wireMockRule.verify(
            postRequestedFor(urlPathMatching("/v2/webhook-channels"))
                .withRequestBody(matchingJsonPath("\$.url"))
        )
    }

    @Test
    fun `search returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/webhook-channels/search",
            200,
            "{\"data\": [], \"list_metadata\": {\"before\": null, \"after\": null}}",
        )
        val result = api().search()
        assertNotNull(result)
    }

    @Test
    fun `rotate returns a typed response`() {
        stubResponse(
            "PUT",
            "/v2/webhook-channels/signing-secret",
            200,
            "{\"data\": {\"signing_secret_configured\": false}}",
        )
        val result = api().rotate(signingSecret = "sample-argxxxxxxxxxxxxxxxxxxxxxx")
        assertNotNull(result)
        wireMockRule.verify(
            putRequestedFor(urlPathMatching("/v2/webhook-channels/signing-secret"))
                .withRequestBody(matchingJsonPath("\$.signing_secret"))
        )
    }

    @Test
    fun `get returns a typed response`() {
        stubResponse(
            "GET",
            "/v2/webhook-channels/sample-arg",
            200,
            "{\"data\": {\"id\": \"sample\", \"name\": null, \"url\": \"sample\", \"allowed_notifications\": [], " +
                "\"signing_secret_configured\": false}}",
        )
        val result = api().get("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `replace returns a typed response`() {
        stubResponse(
            "PUT",
            "/v2/webhook-channels/sample-arg",
            200,
            "{\"data\": {\"id\": \"sample\", \"name\": null, \"url\": \"sample\", \"allowed_notifications\": [], " +
                "\"signing_secret_configured\": false}}",
        )
        val result =
            api()
                .replace(
                    "sample-arg",
                    name = null,
                    url = "sample-arg",
                    allowedNotifications =
                        emptyList<
                            SdkReplaceWebhookChannelRequestApplicationJsonAllowedNotifications
                        >(),
                )
        assertNotNull(result)
        wireMockRule.verify(
            putRequestedFor(urlPathMatching("/v2/webhook-channels/sample-arg"))
                .withRequestBody(matchingJsonPath("\$[?(@.name == null)]"))
                .withRequestBody(matchingJsonPath("\$.url"))
        )
    }

    @Test
    fun `update returns a typed response`() {
        stubResponse(
            "PATCH",
            "/v2/webhook-channels/sample-arg",
            200,
            "{\"data\": {\"id\": \"sample\", \"name\": null, \"url\": \"sample\", \"allowed_notifications\": [], " +
                "\"signing_secret_configured\": false}}",
        )
        val result = api().update("sample-arg")
        assertNotNull(result)
    }

    @Test
    fun `delete completes without throwing`() {
        stubResponse(
            "DELETE",
            "/v2/webhook-channels/sample-arg",
            204,
            "{\"data\": {\"id\": \"sample\", \"name\": null, \"url\": \"sample\", \"allowed_notifications\": [], \"signing_secret_configured\": false}}",
        )
        api().delete("sample-arg")
    }

    @Test
    fun `send returns a typed response`() {
        stubResponse(
            "POST",
            "/v2/webhook-channels/sample-arg/test",
            200,
            "{\"data\": {\"channel_id\": \"sample\", \"event\": \"booking.cancelled\", \"status\": \"delivered\"}}",
        )
        val result =
            api()
                .send(
                    "sample-arg",
                    event =
                        SdkSendTestWebhookRequestApplicationJsonEvent.values().first {
                            it != SdkSendTestWebhookRequestApplicationJsonEvent.Unknown
                        },
                )
        assertNotNull(result)
        wireMockRule.verify(
            postRequestedFor(urlPathMatching("/v2/webhook-channels/sample-arg/test"))
                .withRequestBody(matchingJsonPath("\$.event"))
        )
    }

    @Test
    fun `list translates 401 to UnauthorizedException`() {
        stubResponse("GET", "/v2/webhook-channels", 401)
        assertThrows(UnauthorizedException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 404 to NotFoundException`() {
        stubResponse("GET", "/v2/webhook-channels", 404)
        assertThrows(NotFoundException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 429 to RateLimitException`() {
        stubResponse("GET", "/v2/webhook-channels", 429)
        assertThrows(RateLimitException::class.java) {
            api().list()
        }
    }

    @Test
    fun `list translates 500 to GenericServerException`() {
        stubResponse("GET", "/v2/webhook-channels", 500)
        assertThrows(GenericServerException::class.java) {
            api().list()
        }
    }
}
