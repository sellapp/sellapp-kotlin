// This file is auto-generated by oagen. Do not edit.

plugins {
    kotlin("jvm") version "2.1.20"
    id("maven-publish")
    id("org.jetbrains.dokka") version "2.2.0"
}

group = "app.sell"

version = "0.1.1"

repositories { mavenCentral() }

dependencies {
    implementation("com.squareup.okhttp3:okhttp:4.12.0")
    implementation("com.fasterxml.jackson.module:jackson-module-kotlin:2.18.3")
    implementation("com.fasterxml.jackson.datatype:jackson-datatype-jsr310:2.18.3")
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-core:1.10.1")
    testImplementation(kotlin("test"))
    testImplementation("org.junit.jupiter:junit-jupiter:5.11.4")
    testImplementation("org.wiremock:wiremock:3.12.0")
}

dependencyLocking { lockAllConfigurations() }

kotlin { jvmToolchain(17) }

java { withSourcesJar() }

val javadocJar by
    tasks.registering(Jar::class) {
        archiveClassifier.set("javadoc")
        from(tasks.dokkaGeneratePublicationHtml.flatMap { it.outputDirectory })
        from(listOf("LICENSE.txt", "NOTICE.txt")) { into("META-INF/sellapp") }
    }

tasks.assemble { dependsOn(javadocJar) }

tasks.withType<AbstractArchiveTask>().configureEach {
    isPreserveFileTimestamps = false
    isReproducibleFileOrder = true
}

tasks.named<Jar>("sourcesJar") {
    from(listOf("LICENSE.txt", "NOTICE.txt")) { into("META-INF/sellapp") }
}

sourceSets.test { kotlin.srcDir("examples/src/main/kotlin") }

tasks.test { useJUnitPlatform() }

tasks.jar {
    from(listOf("README.md", "LICENSE.txt", "NOTICE.txt")) { into("META-INF/sellapp") }
    from("docs") { into("META-INF/sellapp/docs") }
    from("examples") {
        include("**/*.kt", "**/*.kts", "README.md")
        exclude("build/**", ".gradle/**")
        into("META-INF/sellapp/examples")
    }
}

publishing {
    publications {
        create<MavenPublication>("maven") {
            from(components["java"])
            artifact(javadocJar)
            artifactId = "sellapp"
            pom {
                name.set("SellApp API Kotlin SDK")
                description.set(
                    "SellApp API client for Kotlin/JVM with typed requests, coroutine support, and pagination"
                )
                url.set("https://sell.app/docs/api")
                scm {
                    url.set("https://github.com/sellapp/sellapp-kotlin")
                    connection.set("scm:git:https://github.com/sellapp/sellapp-kotlin.git")
                    developerConnection.set(
                        "scm:git:ssh://git@github.com/sellapp/sellapp-kotlin.git"
                    )
                    tag.set("v0.1.1")
                }
                developers {
                    developer {
                        id.set("sellapp")
                        name.set("SellApp")
                        url.set("https://sell.app")
                        organization.set("SellApp")
                        organizationUrl.set("https://sell.app")
                    }
                }
                licenses {
                    license {
                        name.set("MIT")
                        url.set("https://github.com/sellapp/sellapp-kotlin/blob/main/LICENSE.txt")
                    }
                }
            }
        }
    }
}
